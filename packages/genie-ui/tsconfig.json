{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["bun"],
    "outDir": "dist"
  },
  "include": ["server/**/*", "client/**/*"],
  "exclude": ["node_modules", "dist"]
}
