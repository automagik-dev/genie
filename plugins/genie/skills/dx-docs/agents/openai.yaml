interface:
  display_name: "DX and Docs Review"
  short_description: "Audit onboarding, docs drift, and developer UX"
  default_prompt: "Use $genie:dx-docs (or $dx-docs only for a separately installed personal copy) to audit this contributor journey against the live product."
