interface:
  display_name: "Supply-Chain Review"
  short_description: "Audit trust boundaries and release provenance"
  default_prompt: "Use $genie:supply-chain (or $supply-chain only for a separately installed personal copy) to audit the trust boundaries and release chain in this change."
