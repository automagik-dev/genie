interface:
  display_name: "Supply-Chain Review"
  short_description: "Audit trust boundaries and release provenance"
  default_prompt: "Use $genie:supply-chain (or $supply-chain only when intentionally selecting its user-tier copy) to audit the trust boundaries and release chain in this change."
