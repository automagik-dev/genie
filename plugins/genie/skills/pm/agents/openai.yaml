interface:
  display_name: "Project Management"
  short_description: "Coordinate backlog, delivery gates, and escalation"
  default_prompt: "Use $pm to coordinate this work through the appropriate delivery gates."
