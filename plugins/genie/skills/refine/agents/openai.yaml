interface:
  display_name: "Prompt Refiner"
  short_description: "Turn rough prompts into execution-ready briefs"
  default_prompt: "Use $genie:refine (or $refine only when intentionally selecting its user-tier copy) to turn this draft into a precise execution-ready prompt."
