interface:
  display_name: "Omni Wiring"
  short_description: "Wire Genie routing and approvals through Omni"
  default_prompt: "Use $genie:omni (or $omni only when intentionally selecting its user-tier copy) to wire this Genie repository to the selected Omni channel."
policy:
  allow_implicit_invocation: false
