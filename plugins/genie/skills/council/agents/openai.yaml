interface:
  display_name: "Council"
  short_description: "Pressure-test decisions through independent lenses"
  default_prompt: "Use $genie:council (or $council only when intentionally selecting its user-tier copy) to pressure-test this decision from independent perspectives."
