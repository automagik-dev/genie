
# Use bd merge for beads JSONL files
.genie/issues.jsonl merge=beads

# INDEX.md is an append-heavy ledger; union-merge keeps both sides' list entries
# instead of conflicting when local and remote sessions add entries to the same section
.genie/INDEX.md merge=union
