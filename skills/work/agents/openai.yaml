interface:
  display_name: "Work"
  short_description: "Execute approved wishes with evidence"
  default_prompt: "Execute this approved wish in dependency order with scoped workers, the selected standalone or Orca authority, independent review, and verified completion."
