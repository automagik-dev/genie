interface:
  display_name: "Fix"
  short_description: "Repair review gaps with bounded retries"
  default_prompt: "Resolve these blocking review gaps, preserve the per-group repair budget and scope, and obtain independent re-review."
