interface:
  display_name: "Wish"
  short_description: "Plan scoped work with criteria and review"
  default_prompt: "Turn this request into a wish using the bundled template, current design evidence, dependency-ordered groups, validation, and independent plan review."
