interface:
  display_name: "Review"
  short_description: "Assess plans, code, PRs, and repository quality"
  default_prompt: "Independently review this target against its criteria, using relevant audit lenses and current evidence. Return severity-tagged findings and a verdict without applying fixes."
