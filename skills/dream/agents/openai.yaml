interface:
  display_name: "Dream Run"
  short_description: "Orchestrate approved wishes in dependency order"
  default_prompt: "Use $genie:dream (or $dream only for a separately installed personal copy) to plan and run these approved wishes in dependency order."
policy:
  allow_implicit_invocation: false
