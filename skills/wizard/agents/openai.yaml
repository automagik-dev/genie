interface:
  display_name: "Genie Setup Wizard"
  short_description: "Guide first-run setup and the first Genie wish"
  default_prompt: "Use $genie:wizard (or $wizard only when intentionally selecting its user-tier copy) to guide this repository through its first Genie setup and wish."
policy:
  allow_implicit_invocation: false
