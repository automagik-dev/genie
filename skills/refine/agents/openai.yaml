interface:
  display_name: "Prompt Refiner"
  short_description: "Turn rough prompts into execution-ready briefs"
  default_prompt: "Use $genie:refine (or $refine only for a separately installed personal copy) to turn this draft into a precise execution-ready prompt."
