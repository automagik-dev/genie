interface:
  display_name: "Prompt Refiner"
  short_description: "Rewrite any prompt to its target model's prompting guide"
  default_prompt: "Rewrite this prompt so it follows the official prompting guidelines of the model that will run it, keeping its intent."
