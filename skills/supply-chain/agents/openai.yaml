interface:
  display_name: "Supply-Chain Review"
  short_description: "Audit trust boundaries and release provenance"
  default_prompt: "Use $supply-chain to audit the trust boundaries and release chain in this change."
