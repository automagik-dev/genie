{
  "ignorePatterns": [
    { "pattern": "^https://automagik\\.dev/" },
    { "pattern": "^mailto:" },
    { "pattern": "^https://revoke\\.cash$" },
    { "pattern": "^https://hub\\.docker\\.com/settings" }
  ],
  "replacementPatterns": [
    {
      "comment": "Mintlify site-absolute route -> the .mdx file behind it. Assumes every /genie/<route> is backed by a same-named docs/<route>.mdx, which is true for every /genie/ link in the linted pages today. A route resolved by a directory index (/genie/config) or written with a trailing slash (/genie/installation/) would map to a path that does not exist and be reported dead; extend this pattern rather than deleting the link if such a route is ever linted.",
      "pattern": "^/genie/([^#]+?)(#.*)?$",
      "replacement": "{{BASEURL}}/docs/$1.mdx$2"
    },
    {
      "pattern": "^/",
      "replacement": "{{BASEURL}}/"
    },
    {
      "pattern": "^(\\.\\.?/[^./][^/]*/.+)\\.md(#.*)?$",
      "replacement": "$1.mdx$2"
    }
  ],
  "httpHeaders": [
    {
      "urls": ["https://github.com/", "https://api.github.com/"],
      "headers": {
        "Accept": "text/html,application/xhtml+xml",
        "User-Agent": "automagik-genie-docs-lint"
      }
    }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [200, 206, 301, 302, 307, 308, 403, 429]
}
