# HANDOFF — 2026-07-11 (session d0554818, Fable 5) — overnight autonomous execution

**For the fresh session.** Predecessor: [HANDOFF-20260710.md](HANDOFF-20260710.md).
Untracked on disk until committed via the overnight docs PR (pattern: #2544).

**Authorization (Felipe, 2026-07-11, this handoff = the grant):** autonomous execution of the
items in §2 while he sleeps. Merge-on-green applies to **dev-target PRs for §2 scope only**;
§19 unchanged (main = human via UI); **no stable promotions** — the pointer moved to
v5.260711.3 today and further promotion is Felipe's. Worktree isolation is law: never work in
the shared checkout (`~/workspace/genie`, parked on stale `wish/agent-sync` branch);
`git worktree add` off `origin/dev` per work item. Watchers/PR monitors DIE with sessions —
first move: `gh pr list --state open`.

## 1. State you inherit (verified 2026-07-11 ~11:15Z)

- **Stable v5.260711.3 LIVE** (latest.json flipped 10:44Z). Dev at v5.260711.5 (`044c1bcd`).
  Zero open PRs. The full stack shipped: codex first-class integration + B1–B7 agent-sync
  hardening + all 16 wave-2 review findings + Felipe's #2548 update-safety fixes.
- **Agent-sync stable-leg QA: iteration 4 clean** (#2554 evidence in
  `.genie/wishes/agent-sync/qa/`). `/council` string-args coercion verified live in the
  stamped council.js (memory `reference_council_workflow_string_args`).
- New memory facts from today: main merge-method footgun FIXED (any merge method publishes);
  bot-head PRs stall in `action_required` — `gh pr close && gh pr reopen` re-triggers as human.
- Review dossiers (session scratchpad — RE-VERIFY anchors against dev before fixing, several
  waves of changes landed since):
  `…/d0554818…/scratchpad/artifacts/ultracode-review-20260710.md` (wave 1: B1–B7 fixed; F1–F10
  fast-follows + deferred list), `codex-integration-map.md` (also committed at
  `plugins/genie/references/codex-integration-map.md` — §3 = ranked codex opportunities).
  Findings tables: `.genie/wishes/agent-sync-hardening/WISH.md` §Review results.

## 2. Autonomous overnight queue (in order)

### 2a. Fast-follow fix wish — `agent-sync-fastfollow` (small, ~1 PR)
Probed OPEN on dev 2026-07-11 (verify each before fixing; #2548/#2554 may have moved things):
- **F1** interrupted-swap debris: sweep `*.genie-sync.staging`/`*.genie-sync.prev` at sync
  START (parent-level); flip the test that currently locks debris in (`agent-sync.test.ts`
  ~:661); crash-window tests for both rename gaps.
- **F2** doctor: skip suffix debris in `summarizeManagedSkills`; kept-modified orphans =
  informational, not actionable-stale. VERIFY FIRST — #2548 fixed "doctor false-stale" and may
  have covered part.
- **F3** validate `~/.hermes/active_profile` against `/^[A-Za-z0-9][A-Za-z0-9._-]*$/` before
  join (path-traversal → rmSync/symlink escape); traversal tests.
- **F4** uninstall: scan `~/.hermes/profiles/*/plugins/genie` symlinks (all profiles) with the
  resolves-into-genieHome ownership check; fixture test.
- **F7** skills-lint chain-continuity: `/\bpackage\.json\b(?:[^&|;]|&&)*&&\s*$/` + the
  broken-chain (`; bun run wishes:lint`) negative fixture.
- **F6** council.js coercion regression tests (string/JSON/audit-prefix branches) — the
  coercion shipped, the test owner didn't (repo rule: broke once → test owns it forever).
- **F8** routing-pin QA runbook: move inline comments off `\`-continued lines
  (`.genie/wishes/routing-matrix/qa/routing-pin-qa-20260710.md:97–99`).
- **F10** CLAUDE.md stamp-root gotcha: name BOTH stamping legs (hook fallback via
  `resolveStampInputs`; CLI via agent-sync `resolveGenieSource`).
- Upstream the sync-tier verifier's flag-REJECTING fake-genie gates into
  `src/lib/smart-install-hook.test.ts` (shipped fake exits 0 for all args — proves argv shape,
  not rejection behavior).
- Optional from wave-1 deferred (do only if cheap): per-orphan try-catch in
  `removeManagedOrphans`; COORDINATION.md hash typo `422cdd6c`→`422dc6dd`.
Discipline: lanes if parallel, single writer commits; gates from script files; PR to dev,
merge on green.

### 2b. Routing-pin re-test — day-2 QA under ENFORCED pins (evidence only, no code)
Yesterday's day-1 pull was inconclusive-by-delivery-gap (Fable share ROSE 46→57% because the
7 role agents never installed). Stable now ships agent-sync → pins should be mechanically live.
- First check: do `engineer-trivial/standard/complex, fixer, reviewer, final-gate, scout`
  appear as subagent types in the fresh session? Record either way (it's the acceptance test
  of agent-sync's lazy delivery).
- Pull the same comparison via the **langwatch CLI** (`npx -y langwatch`, key = Bearer token in
  `OTEL_EXPORTER_OTLP_HEADERS`, `~/.claude/settings.json`): cost/tokens/traces by model,
  07-11 vs 07-10 vs 07-09. Recipes + traps in
  `.genie/wishes/routing-matrix/qa/routing-pin-qa-20260710.md` (analytics effort-filter broken
  → client-side from trace search; NO hand-rolled REST — 403s; cache-read+model groupBy =
  ClickHouse bug). Evidence → `qa/routing-pin-qa-20260711.md`; calibration deltas →
  genie-spend DRAFT. If shares now collapse toward the ~11% dogfood benchmark, mark
  routing-matrix QA CLOSED in INDEX.
- SECRETS: key never in artifacts; scan before commit.

### 2c. genie-spend — draft the WISH (do NOT pour into /work)
Highest-readiness track (~80%). Draft `.genie/wishes/genie-spend/WISH.md` from
`.genie/brainstorms/genie-spend/DRAFT.md` + `genie-spend-calibration-20260710.md` with
RECOMMENDED answers to Felipe's 4 gaps marked `DECISION (provisional — Felipe ratifies)`:
key/endpoint = CC settings env (proven); substrate = langwatch CLI shell-out (proven; REST
broken); cadence/consumers/doctor-burn-threshold = recommend from DRAFT evidence. Phase 1 =
`genie spend` command wrapping proven recipes. Plan-review it (subagent), leave at
"plan review done, execution gated on Felipe's morning ratification". No implementation.

### 2d. DRAFT advancement + docs PR (end of night, single PR like #2544)
- cross-agent-delegate DRAFT: fold the codex-integration-map — Codex install/auth reality is
  now ANSWERED (config.toml, plugins, `codex exec`, SDK, MCP; map §3 = the delegate track's
  menu). Remaining gaps shrink to Hermes `-z` alias + refine style cards.
- skill-absorbs DRAFT: council disposition long done; note report→LangWatch is materially
  unblocked by proven CLI recipes.
- always-on-genie DRAFT: add tonight's evidence chain (collisions ×2, council 3-of-3 takeover
  de-escalation, worktree law held all night) — G10 is wish-ready; note it.
- New brainstorm seed `codex-native-adoption`: from integration-map §3 — notify-hook state
  detection (replaces OTel relay?), `codex exec`/SDK as launch/delegate substrate,
  SubagentStart/Stop + PreCompact/PostCompact hooks, `requirements.toml` managed (fail-closed)
  hooks, `[agents] max_threads` vs genie wave widths. Seed DRAFT.md only, no wish.
- This handoff file + MORNING-BRIEF-20260711.md (see §4) + INDEX touch-ups
  (routing-matrix QA status; agent-sync-hardening EXECUTED; hardening WISH status flip).

### NOT overnight (Felipe-gated — park, don't touch)
Promotions/releases beyond dev · genie-spend execution (2c ratification) · brainstorm-domain-map
oracle-ownership ruling · intent-to-wish-compiler pour (needs domain-map + control-plane
convergence) · dream-replatform substrate choice · council-workflow final execution review IF
the live-QA ritual turns out not to have run (check `.genie/wishes/council-workflow/qa/` — if
evidence exists, the review may be dispatchable; if unclear, park) · rolling-pr PAT mint ·
fable5-revamp omni-side G5/G6 confirm (omni repo).

## 3. Hard-won rules that saved us tonight (obey)
- Freshness-probe EVERY work item before dispatch (`git log origin/dev -- <files>` vs claim) —
  one probe retired a whole wish yesterday (rolling-pr-auth-hardening was already merged).
- Serialize dev merges; rebase onto moved dev + re-green before merging (bit us twice tonight:
  #2546 merged under the wave-2 push → rescue PR #2549; then #2548 landed mid-CI → rebase with
  semantic conflicts in doctor.ts/update.ts import unions).
- Green = `gh pr checks N --watch --fail-fast` EXIT CODE only; one flake-rerun max.
- Fix loops: 2 attempts then park. Validation from script files only; `git grep` not grep.
- Quota outages kill agents mid-flight: empty findings ≠ clean review — resume workflows via
  `resumeFromRunId` (cached prefix replays free); check `journal.jsonl` before diagnosing.
- Bot comments: triage against real code (33 tonight → 7 legit, 14 moot, 4 wrong); the three
  release-breaking HIGHs were found by independent review, not bots.
- If a lane agent asks for a dossier, the ORCHESTRATOR answers (canonical tables only — a
  misrouted lane asked a random reviewer, which correctly refused to invent one).

## 4. Morning brief for Felipe (write as MORNING-BRIEF-20260711.md in 2d)
(1) ratify genie-spend decisions → unpark execution; (2) role agents visible? routing-pin day-2
verdict; (3) council-workflow final review status; (4) oracle-ownership ruling; (5) dream
substrate; (6) rolling-pr PAT; (7) codex-native-adoption seed review; (8) what merged overnight.
