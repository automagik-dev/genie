# HANDOFF — genie token-efficiency program session (2026-07-08 → 07-09)

Everything discussed, decided, produced, and parked in this session. All changes are **staged, nothing committed**. Pre-existing untracked dir `.genie/wishes/omni-branch-drift-sync/` was not touched.

---

## 1. Original ask (Felipe)

1. Model-mix strategy: Fable 5 selectively for complex reasoning, Opus 4.8 at varying efforts for the rest, max reviews, final Fable review — find the most token-efficient combination.
2. Genie is very token-intensive; analyze the recorded LangWatch data (CC OTel → langwatch.khal.ai; creds in `~/.claude/settings.json` env).
3. Break the problem into context domains and brainstorm each (time no constraint).
4. Disposition audit of every genie agent/skill: absorb/delete/keep/refactor — my recommendation + cegonha (Hermes) counter-read on everything.
5. Native Claude Code adoption: bundled workflows, worktrees, agent teams, plugin improvements.
6. Meta: /brainstorm lacks context-domain & gap mapping — the thing that produces a flawless spec. Fix it.
Later additions: split into granular wishes (parallel execution, refine-one-while-dispatching-another); fresh-install resource-shipping bug found live; **burn analysis before any wish dispatch** ("wins on every self-improving loop"); one subject at a time.

## 2. Measured baseline → burn analysis (THE data)

**21d baseline:** $17,857 · 95.77M billable tokens · 1,097 traces (~1,000 in the last 4 days). Fable share of model-attributed cost $13.8k; effort spread xhigh 425 / high 343 / **max 198** / medium 27 / low 2.

**7d decomposition (07-02→07-09, post-DB-fix): $18,175 · 1,113 traces · 63 sessions**
| Bucket | Tokens | ~$ share |
|---|---|---|
| Cache reads | 11.94B | ~55–65% |
| Cache writes | 472M | ~20% |
| Completion | 76.9M | ~20% |
| Fresh prompt | 20M | ~1% |

Findings: ~10.7M cache-read tokens/trace (≈150–250k context × 30–60 API calls/turn); top 10 sessions = $10.1k (55%), all 5–8MB monolith transcripts across genie/khal/khal-fde/omni/desktop; Fable ran 71% of traces yet emits FEWER completion tokens/trace than Opus (57k vs 100k) → **the burn is expensive-model × giant-context × many-calls, not verbosity**. Sonnet 5: Felipe-observed 2× tokens/time → excluded everywhere.

Lever ranking (data-ordered): (1) execution → Opus ≈ **−30% alone**; (2) context diet + subagent isolation + per-stage sessions ≈ −20–40% more; (3) fewer tool calls per turn (batch/script); (4) deterministic session-start-only injects (cache-write discipline).

Full analysis + working curl recipes: `.genie/brainstorms/genie-spend/DRAFT.md` and `.genie/brainstorms/genie-token-efficiency-program/DRAFT.md`.
Known upstream bug: LangWatch groupBy+attribute-metric (`ts.Attributes`) ClickHouse error blocks per-model cache-read split — consider filing.

## 3. Umbrella design (SHIP-reviewed)

`.genie/brainstorms/genie-token-efficiency-program/DESIGN.md` — WRS 100/100, independent review SHIP after 1 fix loop; DRAFT.md holds the full evidence trail.
**Thesis (Hermes-reframed):** genie = control-plane/audit/contract layer; native CC = execution substrate.
15 decisions, 10 risks w/ mitigations, 10 seed groups, 10 success criteria (headline: −40% $/wish in 2 weeks, SHIP rate non-regressing).

### Routing matrix (decided; Felipe + 2 Hermes rounds)
| Stage | Model×effort |
|---|---|
| /brainstorm + /wish sessions, final plan gate, final exec review | Fable·high |
| /work orchestrator | Opus·xhigh (Hermes: never economize on the multiplier) |
| Plan-review loops, per-group exec reviews | Opus·xhigh |
| Engineers | complexity-scored: 0-1 Opus·low (Haiku only non-code chores) / 2-3 med-high / 4-6 xhigh / 7+ xhigh + Fable gate at **max** |
| Fixers | Opus·medium |
| Scouts/chores | Haiku·low |

Hermes amendments (all accepted): verification budget on orchestrator; Fable gate = narrow adversarial risk gate; escalation discipline (4 cause classes — env/tool/spec NEVER escalate; new-evidence rule; caps; disagreement = logged appeal); complexity score rubric; context budgets per role; outcome labels → north-star metric **cost per group accepted without reopening**.
Refinements (Felipe): stage switching = skill-frontmatter pins + `genie launch` pane `--model` flags (manual /model wins; no hard guard); budgets hybrid (HARD 3 Fable calls/wish + 2 escalations/group, $/wish advisory until genie-spend ph2); `max` auto only for 7+ Fable gates; overrides = native `.claude/agents/` shadowing + genie config knobs.

### Skill dispositions (all 17 ruled)
Keep+refactor: brainstorm, wish, work, review, fix(+trace, diagnose mode), genie router (slim, scope-fenced). Keep: docs, genie-hacks, omni.
Absorb: trace→fix; wizard→genie (first-run); pm→work-ref + dream successor (Felipe: YES; more context coming with "real software spec" discussion).
Council → lens library + thin /council route (Hermes: strategy pre-artifact, dissent, appeal court).
Dream → scheduler adapter + genie.db ledger (cron = trigger, never authority; leases/idempotency/human gates).
Report → LangWatch-backed + local fallback.
**refine → KEEP, re-scoped as cross-LLM prompt adapter** (Felipe overruled deletion): backbone method + per-target style cards `refine/targets/{fable,gpt-codex,hermes,haiku}.md` ≤60 lines, auto-applied to cross-agent briefs + `/refine --target`.
**learn → MOVED to `.genie/attic/skills/learn/SKILL.md`** (git mv, staged) for later transfer to the brain (mnemosyne).
Convergence: global `~/.claude/skills/{brainstorm,wish,work,review}` are NEWER dual-backend rewrites; repo copies carry stronger contracts. Merge by layer: global=runtime/adapter base, repo=contract/invariant base; dispatch-contract.md becomes THE single versioned executable reference (kills 6× duplication).

### Key evidence (inventory)
- Same dispatch contract duplicated 6× (work/pm/dream/council/fix/rules).
- Plugin hooks: `session-context.cjs`, `validate-wish.cjs`, `validate-completion.cjs` silently DEAD on v5 shapes (one says "/forge"); `plugins/genie/references/dispatch-contract.md` is v4 (TeamCreate, model:"sonnet"); review-criteria max-3 vs skills max-2; plugin README advertises nonexistent surfaces; jar path drift (`.genie/brainstorm.md` vs real `INDEX.md`); fix calls nonexistent `genie task comment/block`.
- CC-native confirmations: subagent frontmatter pins `model:`+`effort:` (plugin-shippable); skill frontmatter same (cache caveat on mid-session flips); `CLAUDE_CODE_SUBAGENT_MODEL` env overrides ALL pins (→ doctor check); workflows not plugin-shippable; teams: no per-teammate effort; native worktrees rich (isolation:"worktree", .worktreeinclude).
- NousResearch hermes-agent: per-CLI adapter skills (codex needs git repo+PTY+--full-auto; hermes continue-by-title; claude -p JSON); identity = system-prompt injection (SOUL.md), NOT force-loaded skills.

## 4. The 9 granular tracks (`.genie/brainstorms/<slug>/DRAFT.md`, all staged; KNOWN/DECIDED/GAPS format)

| Track | Status | Open gaps (Felipe-owned) |
|---|---|---|
| routing-matrix | **→ WISH, SHIP** | none |
| plugin-resource-shipping | **→ WISH, SHIP** | none |
| cross-agent-delegate | Simmering | Codex auth/roles; hermes `-z` alias confirm; budgets/limits; session retirement; v5-completion Codex-target overlap; prompt-method walkthrough for style cards. Includes decided fail-open degradation policy (learned live) + auto Hermes counter-read at plan gates (decided) |
| control-plane-contract | Raw | sequencing vs pending skills-fable5-revamp execution review; distribution model; contract versioning; PT vs EN headers |
| skill-absorbs | Raw | pm detail awaits "real software spec" context; usage frequency of docs/report/genie-hacks; agent-browser alive?; /council UX |
| always-on-genie | Raw | inject payload + token budget; GENIE_WORKER skip; Warp pane injects; warp-integration wish overlap; non-genie repos |
| genie-spend | Raw (analysis DONE inside) | key source precedence; cadence; consumers; burn-alert threshold |
| brainstorm-domain-map | Raw | **blocked on Felipe's "real software spec" context**; KNOWN/DECIDED/GAPS as standard skeleton?; umbrella split protocol; map→wish inheritance |
| dream-replatform | Raw | substrate (local cron vs cloud); omni approval gates; budget ceiling; DREAM-REPORT form; overnight merge policy |

## 5. SHIP-reviewed wishes (dispatch = user-gated)

**`.genie/wishes/routing-matrix/WISH.md`** — SHIP after 1 fix loop. 4 groups: G1 role agents (7 pinned agents at `plugins/genie/agents/`) + stage pins + work dispatch table; G2 launch pane model flags + doctor env check (`src/term-commands/launch.ts`, `src/lib/v5/warp-launch.ts`, `src/genie-commands/doctor.ts`); G3 template Complexity/Model columns + `scripts/wishes-lint.ts` enforcement (Date ≥ 2026-07-09 grandfathering) + `scripts/wishes-lint.test.ts`; G4 escalation discipline + `templates/genie-config.template.json` knobs. Waves: 1=[G1,G2,G3], 2=[G4]. Review caught: false-passing validation (empirical), lint underspec, stale-dist/env-leak tests.

**`.genie/wishes/plugin-resource-shipping/WISH.md`** — SHIP after 2 fix loops. Fixes live fresh-install failure (shipped wish skill references repo-only `templates/wish-template.md` + `bun run wishes:lint`; plugin cache pinned at 5.260703.5). G1 template → `skills/wish/templates/` via `${CLAUDE_SKILL_DIR}` (council pattern) + probe-guarded invocations + repoints incl. **`tests/e2e/v5-lifecycle.sh:156`** (reviewer-caught CRITICAL: deletion would break required CI) + `skills/README.md:19`; G2 resource-shipping lint in `scripts/skills-lint.ts` (+`.test.ts`); G3 `scripts/fresh-install-smoke.ts` (+test) in `ci.yml` unit job + plugin README release-lag note. **Cross-wish dep: G1 waits on routing-matrix:3.** Sweep evidence: only offenders were wish skill + brainstorm:85 + README + e2e script; `~/.agents/skills/genie` = v4 residue (uninstall.js only).

Native task list mirrors both DAGs (tasks #1–#9; #7 blocked by #4).

## 6. Ordering decision (Felipe, final word this session)

**No wish dispatches until burn analysis** — DONE (task #10, section 2). Data validates routing-matrix first (−30% hypothesis), control-plane-contract promoted (context diets), new dispatch-contract guidance item: batch tool calls. Dispatch of `/work routing-matrix` awaits Felipe's manual review of this handoff.

## 7. Infra incidents (session-observed)

- **cegonha (10.114.1.121) unreachable** since ~01:30 — Hermes counter-reads for BOTH wish gates logged unavailable per the new fail-open degradation policy (recorded in cross-agent-delegate DRAFT + both WISH Review Results). Hermes session `wish-genie-token-efficiency` holds full program context for resumption.
- **LangWatch RDS auth broke** (`langwatch-hml-postgres…rds.amazonaws.com`, user `langwatch`) — Felipe fixed; analysis completed after.
- Not on G10 cluster (checked read-only).

## 8. Memory & meta

- New auto-memory: `~/.claude/projects/-Users-feliperosa-workspace-genie/memory/feedback_one_subject_at_a_time.md` (+ MEMORY.md index line) — never batch contested topics; sequence them.
- Process rule live: auto Hermes counter-read at plan gates; fail-open when host down.
- Reviews this session: umbrella design (FIX-FIRST→SHIP), routing-matrix (FIX-FIRST→SHIP), plugin-resource-shipping (FIX-FIRST→FIX-FIRST→SHIP w/ mechanical fix applied in-doc).

## 9. File manifest (all staged unless noted)

```
.genie/INDEX.md                                              (jar: 2 Poured wishes, umbrella, 6 Raw + 1 Simmering tracks)
.genie/brainstorms/genie-token-efficiency-program/DESIGN.md  (umbrella, SHIP)
.genie/brainstorms/genie-token-efficiency-program/DRAFT.md   (full evidence trail + LangWatch recipes)
.genie/brainstorms/genie-token-efficiency-program/HANDOFF-20260709.md  (this file)
.genie/brainstorms/routing-matrix/DRAFT.md                   (wish-ready, gaps closed)
.genie/brainstorms/plugin-resource-shipping/DRAFT.md         (wish-ready, sweep evidence)
.genie/brainstorms/cross-agent-delegate/DRAFT.md             (+ degradation policy)
.genie/brainstorms/control-plane-contract/DRAFT.md
.genie/brainstorms/skill-absorbs/DRAFT.md
.genie/brainstorms/always-on-genie/DRAFT.md
.genie/brainstorms/genie-spend/DRAFT.md                      (+ 7d burn analysis)
.genie/brainstorms/brainstorm-domain-map/DRAFT.md
.genie/brainstorms/dream-replatform/DRAFT.md
.genie/wishes/routing-matrix/WISH.md                         (SHIP)
.genie/wishes/plugin-resource-shipping/WISH.md               (SHIP)
.genie/attic/skills/learn/SKILL.md                           (git mv from skills/learn — brain transfer pending)
```

## 10. Next actions (awaiting your review)

1. Review this handoff + both WISH.md files → commit when satisfied (suggested: `docs(genie): token-efficiency program — umbrella design, 9 tracks, 2 SHIP wishes, burn analysis`).
2. `/work routing-matrix` (Wave 1 = G1,G2,G3 parallel) — the −30% lever.
3. `/work plugin-resource-shipping` after routing-matrix G3.
4. Next refinements queued: cross-agent-delegate (needs your Codex/prompt-method input) · brainstorm-domain-map (needs your "real software spec" download, also unlocks pm detail) · control-plane-contract (needs skills-fable5-revamp sequencing call — its execution review is still pending).
5. Infra: cegonha still needs a look (Hermes gates degrade until then).
6. Post-dispatch: rerun `genie spend` recipes weekly; matrix is config, LangWatch arbitrates (incl. Sonnet re-entry, only via A/B evidence).
```
