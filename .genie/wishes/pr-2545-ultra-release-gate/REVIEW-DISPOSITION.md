# PR 2545 review finding disposition

This ledger deduplicates every finding from the seven specialist lanes and the native reviewer. Aliases: RH = repository hygiene, AR = architecture, SEC = security/supply chain, CQ = code quality, QA = quality engineering, PERF = performance, DX = developer experience/docs, NR = native review in display order. “Inherited” means unchanged from the PR base; it can still block stable release but is not silently mixed into this remediation.

| ID | Source findings | Origin | Disposition / owner | Acceptance evidence | Final evidence |
|----|-----------------|--------|---------------------|---------------------|----------------|
| F01 | RH1 | External gate | E records; GitHub/human action required | Repository CI jobs run and pass on exact final SHA | **PENDING:** follow-up has no exact-final-SHA GitHub CI result yet |
| F02 | RH2 | External gate | OUT of code; human approval required | Independent APPROVED review on final SHA | **PENDING:** human APPROVED review must target the final follow-up SHA |
| F03 | RH3, DX4 | PR/exposed | E | Migration caveat in create and promotion/edit notes; hardening criteria mapped | `release-publish.yml` idempotently appends marker `genie-agent-sync-migration-v1` on create and existing-release promotion; plugin docs and the B1–B7 map describe the operator-run second hop |
| F04 | RH4, SEC10, DX9 | Mixed | Current version-parity/build checks in B/D1/E; inherited verifier contract to stable-release wish | `release-payload-version.test.ts`, build staging verification, and source/extracted smoke own current parity; inherited publication/verifier redesign remains BLOCKING in the stable-release wish |
| F05 | RH5 | PR | E; **reopened operationally 2026-07-12** | Retired metrics state/README marker removed or superseding decision + schema test | Removed README marker and `.genie/agents/metrics-updater/**`; `release-docs.test.ts` **detects** resurrection but cannot prevent a direct-to-dev push. That happened: the external scheduled metrics-updater session pushed `d0ecc296` (2026-07-12) re-adding the state and README block after the PR merged, turning dev CI red against this gate. Re-retired in merge `af9fcad1`. **Open operational action: disable or re-scope the external metrics-updater routine — no repo commit can close this** |
| F06 | RH6, AR1, DX1, DX6, NR9 | PR/exposed | B | Source copy and extracted plugin contain all valid in-root skills; parity test | Product payload: 23 skills and 19 reference files. Physical plugin mirror: 64 files / 209,675 logical bytes. `sync-plugin-skills`, `skills-lint`, `fresh-install-smoke`, and release inventory tests reject escapes/drift/missing metadata; Darwin ARM64 build/extraction parity passed at 21 MB |
| F07 | RH7, DX8 | PR | E | Contributor/plugin docs match final CLI, skills, hooks, and agents | README/CLAUDE/plugin references now distinguish 23 plugin skills, 7 optional CLI role agents, 36 personal adapted skills, MCP launcher, H3/H4/H6, and orchestrator-only completion |
| F08 | RH8 | PR | E | Build Tarballs PR filters cover all artifact inputs | PR paths now cover `src`, `skills`, `templates`, all `plugins`, metadata/lock/tsconfig, the build script and every direct/transitive TypeScript helper (including `skills-lint.ts`), both marketplaces, and the workflow; `release-docs.test.ts` derives and locks the helper graph |
| F09 | AR2, NR2 | PR | A | Portable launcher fixtures preserve stdin/stdout/exit/signals on POSIX/Windows paths | `dispatch-runtime.test.ts` covers canonical absolute selection, Windows/POSIX, stdin/stdout, nonzero/spawn failure, timeout, SIGTERM→SIGKILL, and signal forwarding |
| F10 | AR3, SEC4, QA1, PERF1, NR1, NR6 | PR | A | Omni only on PermissionRequest, matched exactly once; timeout/failure deny | H4 registry excludes Omni; H6 applies configured matcher once. `omni-approval`, `omni-dispatch`, and manifest tests require reasoned deny and 110s<115s<125s budgets |
| F11 | AR4, QA2, QA10, NR4 | PR | A | Canonical apply_patch paths decoded or file-policy hook removed; fixtures prove behavior | H5/H7 removed; adapter/approval fixtures parse Codex `tool_input.command`, bound extracted paths, and reject malformed shapes |
| F12 | AR5, QA8 | PR | D1 using C API | Manual update refreshes plugin/hooks/agents; sync-only stays bounded | `update-integrations.ts` and update tests cover parent-process convergence, plugin/hook refresh, preserved disabled state, ownership-safe role refresh, and no-network `--sync-only` |
| F13 | AR6, SEC9, CQ4, QA3 | PR | C | One digest-backed classifier; modified role artifacts survive refresh/uninstall | `inspectCodexAgentOwnership` is shared by runtime integration paths; `runtime-integrations.test.ts` covers clean refresh/remove plus user-owned/modified/corrupt/symlink preservation |
| F14 | AR7, DX5 | PR | D2 | Root/nested/worktree fixtures keep exactly one usable plugin/fallback MCP route | `codex-project-mcp.test.ts` covers root discovery, nested/linked worktrees, disabled/unusable plugin fallback, exact config ownership, and one usable route |
| F15 | AR8 | PR/exposed | A + D2 | Hook config writer removed; TypeScript config owner and runtime choice are consistent | Codex manifest owns only H3/H4/H6; setup/init/launch use `codex-project-mcp.ts`, preserve explicit runtime choice, and do not scaffold hook/project instruction files |
| F16 | SEC1 | Inherited CRITICAL | OUT → stable-release-security-gate | Arbitrary-ref stable publish eliminated/protected | Blocking |
| F17 | SEC2 | Inherited HIGH | OUT → stable-release-security-gate | Inputs validated and artifacts bound to approved workflow/ref/SHA | Blocking |
| F18 | SEC3 | Inherited HIGH | OUT → stable-release-security-gate | Third-party actions pinned, frozen installs, least privilege | Blocking |
| F19 | SEC5, SEC7, QA7, PERF2, PERF3, DX7 | PR/exposed | A | H1/H2/H8 removed; H3 bounded; no lifecycle install/config/workspace mutation | Manifest has 3 commands; H1/H2/H5/H7/H8/H9 removed. H3 caps 8 records/256 KiB input/2 KiB output and performs no install, network, update, config, or workspace write |
| F20 | SEC6 | PR/exposed | C | All 36 live skill digests unchanged after isolated fixture tests and read-only comparison | **CLOSED:** containment held (backup restored 22 affected dirs, duplicate `review` quarantined, old trust removed); the final post-test read-only comparison on 2026-07-11 matched 36/36 skill directory digests and 14/14 agent TOML digests against the immutable baseline, with exactly 14 live TOMLs and zero sync markers. The four extra skill-root entries (`agent-browser`, `ralph-tui-create-beads-rust`, `sleepy-monitor`, `workit`) are personal and outside the baseline's selected set, therefore not covered by the comparison |
| F21 | SEC8, QA11, QA12 | PR | A | Event-specific valid envelopes, previous-binary compatibility, malformed-input fail closed | Launcher/dispatcher/adapter tests cover previous-binary env wire, empty/malformed/structurally invalid input, schema-checked output, and event-specific fail-closed responses |
| F22 | CQ1, QA5, DX3, NR3 | PR | B | Manifest formatted; build/version rewrites preserve formatting; full gate green | `plugin.json` is Biome-formatted; `version-format.test.ts` proves token-only rewrites and build/version parity. Stable local `bun run check` at the pre-replay-fix head passed 1,169/60 with complexity 2/7 warnings, max 35/42, 0/8 suppressions; the aggregate at `d9b0afbd` passed 1,382 pass, 1 pre-existing skip, 0 fail across 63 files; later heads are tracked in the validation snapshot |
| F23 | CQ2 | PR | D1 | Final/RC/malformed comparison tests pass | Update tests cover final>RC, RC<final, prerelease ordering, malformed versions, downgrade rejection, and explicit-channel selection |
| F24 | CQ3 | PR | D3 | Non-empty reply invariant rejects unrelated/empty/truncated JSONL | `omni-runner.test.ts` requires a non-empty agent reply and rejects empty, unrelated, unknown-shape, and truncated JSONL |
| F25 | CQ5, QA4, DX2 | PR | C owns removal results; D1/D2 propagate them through install/update/setup/doctor/init/launch | Failure is nonzero, actionable, retryable; no caller prints false success | Structured integration/removal results carry deadlines/failures; install/setup/update/uninstall tests reject false success and preserve retryability. Repository-controlled runtime/lifecycle cross-reviews returned SHIP after fixes, and the final local aggregate passed |
| F26 | CQ6, NR8 | PR | C | Lock I/O/race/future-time tests never sync without ownership | Agent-sync fixtures cover O_EXCL contention, lock I/O failure, stale/future timestamps, ownership loss, and no destructive sync without the lock |
| F27 | CQ7, QA8, QA9, NR7 | PR | D1 | Each tree-swap failure preserves old/fresh artifacts; identical residue removed; digest comparison | `auxiliary-trees.ts` plus install/update failure-injection tests cover content digests, identical cleanup, same/cross-filesystem swap, rollback, and preserved verified fresh artifacts |
| F28 | CQ8 | PR | C (uninstall) + D2 (init) | Wrong-shaped valid JSON returns typed/actionable failure, not crash | Config/init/uninstall fixtures reject arrays/scalars/wrong-shaped valid JSON with typed outcomes and no destructive continuation |
| F29 | CQ9 | PR | D1 | Invalid integration option rejected before cleanup/sync/install finishers | `resolveIntegrationSelection` validates before v4 cleanup, layout normalization, sync, or runtime integration; invalid-option test asserts zero side effects |
| F30 | CQ10 | PR | D2 | One Codex-home resolver with explicit empty-override test | `getCodexHome` is the shared resolver; codex-config/genie-home tests cover missing, explicit, and empty override behavior |
| F31 | QA6 | Inherited HIGH | OUT → stable-release-security-gate; D1 covers only the distinct PR-added auxiliary-tree swap | Transactional live-binary/promotion design independently reviewed in the separate wish | Blocking |
| F32 | PERF4 | PR | D2 | Doctor external probes queried once, timed out, and benchmarked against budget | Doctor tests inject hanging Codex/MCP probes, enforce deadlines/structured diagnostics, and avoid duplicate role inventory work; repository-controlled runtime review returned SHIP and the final local aggregate passed. Exact-final-SHA supported-Bun CI remains F01 |
| F33 | PERF5 | PR | C | Every runtime integration subprocess has a deadline and structured failure result | Runtime integration runner applies per-command deadlines and structured timeout errors; hanging-runner fixtures cover Codex/Claude operations and cleanup |
| F34 | DX10 | PR process | E owns `plugins/genie/codex-agents/genie-reviewer.toml` + tests | Reviewer profile permits temp/cache fixture writes but not repo/live-home mutation | Installed Codex 0.144.1 schema-backed profile selects `default_permissions = "genie-reviewer-temp"`, extends `:read-only`, grants write only to `:tmpdir`/`:slash_tmp`, and sets `approval_policy = "never"`; parsed structural tests reject workspace/repo/home or legacy workspace-write grants |
| F35 | NR5 | PR | D3 | `--version`, `--help`, `-m` delivered as prompt text via `--` separator | `omni-runner.test.ts` asserts `--` before all user prompts and verifies option-like strings arrive as prompt content |
| F36 | NR10 | PR | D3 | Missing resume retries fresh once and atomically replaces/clears stale thread state | Omni runner detects missing sessions, retries fresh once, replaces the stored thread id only on success, and leaves retryable state on failure; fixtures cover both paths |
| F37 | Panel detail: interrupted pending Omni request | PR | A | SIGTERM/interruption fixture expires/clears request and emits deny | Approval/dispatch tests trigger SIGTERM cleanup, expire only the owned pending row, remove listeners, and return a reasoned Codex deny |
| F38 | Panel detail: review-thread setup/doctor/init/launch gaps | PR | D2 | Timeout, git-root fallback, merge fallback create/update, shellQuote and agent-choice tests | Setup/doctor/init/launch and codex-project-mcp suites cover deadlines, git/worktree root, fallback create/update/remove, quoting, preserved disabled plugin, and explicit agent choice |
| F39 | Panel detail: hook/skill cold-fork and payload measurements | PR | A/B | Reduced hook count; before/after benchmark and payload inventory recorded | Nine→three commands; SessionStart 3→1; prompt 1→0. Original 21-run process medians 36.889–49.508 ms; retained H3 19.486 ms median/21.348 ms p95. Product payload: 23 skills + 19 reference files; physical plugin mirror: 64 files / 209,675 logical bytes |
| F40 | Panel detail: release notes/index/wish drift | PR | E | PR body/docs/wishes/index name exact final version, SHA, disposition, and still-blocked gates | WISH/index record merge `6f682e2b` from source `10ceb2c0`, follow-up target `dev`, IN_PROGRESS state, and F16–F18/F31 blockers. Repository-controlled docs review returned SHIP after fixes; PR [#2556](https://github.com/automagik-dev/genie/pull/2556) (since merged into `dev` at `a7c3c7ce`) named code commit `d9b0afbd`, this disposition, and the gates open at its creation; successor draft PR [#2562](https://github.com/automagik-dev/genie/pull/2562) carries the F42–F52 disposition and the currently open gates |
| F41 | Final replay integration audit (2026-07-11) | PR/exposed | Fixed in `d9b0afbd` | Exact version verification and Omni URL redaction correctness | Normalized exact version-token comparison (`v15.9.0` no longer satisfies expected `5.9.0`) and contextual URL query-pair redaction (a short value such as `tls=1` no longer becomes a global one-character replacement that corrupts status JSON); focused tests passed and the final aggregate includes them |
| F42 | Final replay integration audit (2026-07-11) | New HIGH — architecture | **FIXED** in `09b368a5` (2026-07-12) | Durable steal-guard recovery protocol shared by the shell and TypeScript lifecycle acquirers | `install.sh` now reaps a foreign `.steal` guard iff its mtime is aged outside ±600s AND its owner is provably dead (empty/unparseable record counts as dead when aged; symlinked guard never reapable; `ps`-based liveness treats other-user EPERM as alive), matching the TS rule; fresh/live guards stay fail-closed; acquire budget 3→4. Independent review SHIP; the abandoned-guard permanent block was reproduced pre-fix. Accepted documented residuals: the doubly-suspended-process race (pre-existing at TS parity) and shell PID-reuse conservatism (no start-identity in shell; TS can still recover those) |
| F43 | Uninstall second-loop re-review (2026-07-11) | New HIGH — architecture | **FIXED** in `8e147d87` (2026-07-12) | Uninstall retry authorization bound to original physical identity in the durable batch schema | Batch journal schema v2 binds per-kind physical identity (skill tree digests, workflow file+sidecar digests, link target, role `{digest, mode}`) at plan time; removal proceeds only when live identity matches, re-verified on the parked object; mismatches become durable `preserved` receipts that count toward clearing and can never regain authority; authentic v1 journals are discarded and re-recorded post-preview. Independent review SHIP; the pre-fix path-authority deletion was reproduced empirically in a disposable pre-fix worktree. Deliberate contract change: modified-in-place assets and changed role agents are now preserved (batch clears) instead of blocking completion. Accepted residuals: role plan-time double-read (benign false-preserve only), link readlink→unlink microsecond window, rules/home/CLI-symlink members path-authorized by design |
| F44 | Final-tree replay 2026-07-12, RH lane (HIGH) | Replay at `8e147d87` | Operational; see reopened F05 | External metrics-updater resurrection detected and re-retired | The external scheduler's direct-to-dev push (`d0ecc296`) turned dev CI red against the retirement gate; re-retired in merge `af9fcad1`, and dev independently re-retired again in `6495bde4`. The routine itself is still active — disabling/re-scoping it remains the open operational action recorded in F05 |
| F45 | Final-tree replay 2026-07-12, AR lane (MEDIUM) | Replay at `8e147d87` | **FIXED** in `d1814e40` | Shell recovers the empty/unparseable aged lock debris TS deliberately leaves | `lock_record_is_stale` treats an aged empty/unparseable record as dead-owner recoverable (fresh empty locks stay protected by the ± window, covering the TS mid-write race); the under-guard observed-equality re-verify is preserved. Reproduced pre-fix as a permanent `curl|bash` reinstall wedge |
| F46 | Final-tree replay 2026-07-12, AR/CQ/QA/DX lanes (MEDIUM+3×LOW) | Replay at `8e147d87` | **FIXED** in `d1814e40` | Symlinked/non-regular guard and lock are never reaped by either acquirer, as the shared-protocol docblock states | TS `stealStaleLock` now lstat-refuses symlinks/non-regular nodes on BOTH the aged-guard reap and the under-guard lock steal (fail-closed contended), restoring parity with the shell's `! -L` and making the docblock truthful; regression tests pin both paths |
| F47 | Final-tree replay 2026-07-12, CQ lane (LOW) | Replay at `8e147d87` | **FIXED** in `d1814e40` | No-trailing-newline owner records parsed identically by shell and TS | The `IFS= read -r x || [ -n "$x" ] || x=""` idiom at all four shell read sites preserves a torn-write record instead of clobbering it to empty and mis-reaping a live owner; verified under `set -euo pipefail` |
| F48 | Final-tree replay 2026-07-12, AR lane (MEDIUM) | Replay at `8e147d87` | **FIXED** in `d1814e40` | Every Omni-gated tool is routed by a shipped hook manifest | Claude PreToolUse dispatch matcher gains `NotebookEdit` (builtin guards use narrow anchored matchers, so only the approval gate is newly routed); new `omni-approval-manifest-parity.test.ts` derives the gated set from `buildOmniRegistry` and fails closed on drift, proven failing pre-fix; codex manifest verified no-gap (`apply_patch` matched; NotebookEdit is Claude-only) |
| F49 | Final-tree replay 2026-07-12, QA lane (MEDIUM) | Replay at `8e147d87` | **FIXED** in `d1814e40` | The wish's own validation command performs zero live-home writes | The `persistChannel` sticky-channel suite is isolated to a temporary `GENIE_HOME` (was read-modify-writing the live `~/.genie/config.json` on every full test run); no other unisolated live-home writer found in the touched suites |
| F50 | Final-tree replay 2026-07-12, QA lane (LOW) | Replay at `8e147d87` | **FIXED** in `d1814e40` | Guard-debris matrix covers future-mtime, symlink, no-newline, and empty-lock cells on both acquirers | +7 regression tests: shell future-mtime guard reap, shell symlinked guard fail-closed, shell no-newline live guard preserved, shell empty aged lock recovered, TS future-mtime guard reap, TS symlinked guard contended, TS symlinked lock contended |
| F51 | Final-tree replay 2026-07-12, PERF lane (LOW) | Replay at `8e147d87` | **FIXED** in `d1814e40` | Uninstall digesting is O(N) in planned members, authorization semantics unchanged | `collectAgentSyncAssets` gains `restrictToPaths` gating full-tree digests before inspection; per-member calls digest only their planned path. Reviewer confirmed the F43 identity re-verification, park-and-reverify, and kind-mismatch guards are untouched and membership is provably identical |
| F52 | Final-tree replay 2026-07-12, DX lane (HIGH) | Replay at `8e147d87` | **FIXED** in this ledger commit | Release ledger matches shipped code | The F42/F43 rows above now record FIXED with commit, review, and residual evidence (they still said OPEN while `09b368a5`/`8e147d87` shipped the fixes); F05 corrected from "prevents resurrection" to detects-but-cannot-prevent with the operational action |

## Agent-sync hardening supersession map

The existing `.genie/wishes/agent-sync-hardening/WISH.md` remains authoritative history. It is not marked complete wholesale.

| Existing criterion | New evidence owner | Status rule | Current state |
|--------------------|--------------------|-------------|---------------|
| B1/B3/B6 SessionStart delegation and compatibility | A, H1/H4/H6, F09/F10/F19/F21/F37 | Supersede only after manifest fixtures and previous-binary tests pass | **SUPERSEDED:** unattended SessionStart delegation removed; portable launcher/protocol/compatibility fixtures implemented |
| B2 sync lock | C, F26 | Supersede only after race, I/O, stale, and future-time tests pass | **SUPERSEDED:** lock ownership, contention, I/O, stale, and future-time fixtures implemented |
| B4 user-data preservation | C, F13/F20/F25 | Supersede only after fixture and live read-only digest evidence | **SUPERSEDED:** ownership fixtures pass and the final post-test read-only live comparison on 2026-07-11 matched 36/36 skill digests and 14/14 agent TOML digests (F20) |
| B5 fresh reinstall convergence | D1, F27 | Supersede only after every injected failure/identical-residue test passes | **SUPERSEDED:** auxiliary-tree digest/identical/failure/rollback fixtures and final local aggregate pass |
| B7 release caveat | E, F03/F40 | Supersede only after create/edit notes and final documentation evidence | **SUPERSEDED FOR REPOSITORY SCOPE:** create/promotion note and operator docs updated; docs cross-review returned SHIP. Exact-final-SHA CI and human approval remain external gates |
| Wave-2 plugin/skills/runtime items | B/C/D2, F06/F12–F15/F22/F30/F32/F33 | Close individually; never infer completion from version equality | **SUPPORTED LOCALLY:** each row names focused proof; repository-controlled docs/runtime/lifecycle cross-reviews returned SHIP (pre-replay-fix head) and the final aggregate passed at `d9b0afbd`. The live baseline comparison and merge-tree simulation closed on 2026-07-11; F01/F02, the final-tree specialist panel, and the open follow-ups F42/F43 remain |

PR-remediation SHIP closes only non-blocking rows with concrete evidence. Rows F16–F18 and F31 continue to block stable promotion through the separate wish even if this PR-scope review later returns SHIP.

Ledger edits in this wish are adversarially verified before commit by the [`pm-ledger-verify`](../../../.claude/workflows/pm-ledger-verify.js) workflow; runs are recorded in [`FINALIZATION-VERIFICATION.md`](./FINALIZATION-VERIFICATION.md).

## Current validation snapshot

Run on the follow-up worktree at head `e3abf2b5` (merge of `origin/dev` `e9059f41` on top of loop-1 fix commit `d1814e40`) on 2026-07-12. PR #2556 merged into `dev` at `a7c3c7ce` on 2026-07-11; the commits after it (F42 `09b368a5`, F43 `8e147d87`, loop-1 `d1814e40`, harness/ledgers) ship as a successor PR to `dev`.

- Full aggregate `bun run check` — 1,434 pass, 1 pre-existing skip, 0 fail (1,435 ran) across 64 files; exit 0. (Earlier quiet run at `d1814e40`: 1,431 pass / 1 skip of 1,432 ran, green; two transient failures observed only under concurrent-reviewer machine load re-ran clean and are not reproducible quiet.)
- Seven-lane specialist replay — ran at `8e147d87` (the F43 head): repo-hygiene FIX-FIRST, architecture SHIP, security SHIP, code-quality SHIP, QA FIX-FIRST, performance SHIP, dx-docs FIX-FIRST; 12 confirmed findings (each surviving 2-refuter adversarial verification), dispositioned as rows F44–F52; all code-fixable ones closed in `af9fcad1`/`d1814e40` (fix loop 1 of 2), combined independent re-review of the fixes returned SHIP. No full seven-lane re-replay ran after the fixes; the targeted re-review is the closing evidence.
- Merge simulation — `git merge-tree --write-tree origin/dev HEAD` exits 0 with tree `cc6ea068` against `origin/dev` = `e9059f41`; `git diff --check` clean.
- Live user assets — read-only comparison on 2026-07-12 matched 36/36 skill directory digests and 14/14 agent TOML digests; exactly 14 live TOMLs. One genie-owned product `review` skill (with `.genie-sync.json` ownership marker) appeared at `~/.agents/skills/review` on 2026-07-12 00:05 — consistent with an explicit live `genie update` run by the finishing session after the PR merge, before this session's work; it is a product asset, not user data, and every baseline digest is intact.
- F42/F43 — both fixed and independently reviewed SHIP (see rows); F42's permanent block and F43's path-authority deletion were each reproduced pre-fix.

The local Bun is 1.3.9 while the repository declares Bun >=1.3.10, so exact-final-SHA supported-Bun GitHub CI (F01) and independent human approval (F02) remain the external gates — now targeting the successor PR's final head. The outer bare-Node hook/MCP startup probe is **BLOCKED-UPSTREAM** by the current Codex schema; installed hook hashes remain untrusted. The external metrics-updater routine remains active (F05/F44 operational action). None of these residual gates closes inherited F16–F18/F31, which still block stable promotion.
