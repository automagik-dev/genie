# Council — intake of 38 personal skills (2026-09-18)

Run `wf_c6661ffe-5f5`: five Opus lenses (architecture, delivery, product, security, dissent) and one Opus synthesizer;
6 agents, 735k subagent tokens, 135 tool uses, 474 s; nothing `notConvened`. The lenses are role-separated, not
independent: one model family, one runtime. Evidence came from seven read-only Opus characterizer shards (38/38
candidates read whole, 968k subagent tokens, ≈6 min wall clock in parallel) and a deterministic facts pass. A mikro
(DeepSeek flash) overlap pass ran first — 38.6k tokens, 86 s — and read frontmatter only, so it served as a
neighbour cross-check and nothing more. Assess-only: no shipped skill was edited. The procedure is `PROCEDURE.md`
beside this file.

Decision: **revise** — every lens endorses the direction (38 candidates → at most one new always-loaded
description, the rest folded or reduced to clauses) and none ratifies the shard table unchanged.

Per-candidate records, the full lens reports and the shard JSON live OUTSIDE this public repository on the dogfood
host (they quote private residue the council says must never enter tracked paperwork). This file cites candidates
by name only.

## Revised disposition table (shard proposal → council)

| Candidate | Shards | Council | Where it lands / why |
|---|---|---|---|
| `deslop-writing` | ABSORB | ABSORB — **held, lands last** | Gates: owner authorship + MIT grant with a NOTICE row; a non-colliding house name (e.g. `prose`) or the personal copies retired first; one-sentence description; ships without the humanizer taxonomy; the "agent voice: AGENTS.md or skill?" question answered; hop rehearsed on a replica HOME; dev-channel hold until a measured-run bar. If provenance fails: invariants fold into `docs` and `authoring`. |
| `humanizer` | MERGE | MERGE (self-audit loop only) | Into the held prose skill; `references/ai-tells.md` deferred (CC BY-SA lineage of the example pairs). Dissent: PERSONAL/DROP. |
| `safe-shell-cli-payloads` | ABSORB | **MERGE (5/5)** | ~70 % is Orca residue. The quoting rule goes inline at the point of use in `work`; `report`'s "file or stdin, never interpolated" sentence extends to the wish publisher and the work/dream card-comment sites; one `verify` row for read-back of the stored body. |
| `artifact-review-integrity` + `pull-request-review-integrity` | MERGE ×2 | MERGE — **wave one**, ONE fence-free evidence-identity decision in `review` references | The three GitHub facts land once. Fix the `git merge-base` false positive in `REPO_WRITE_PATTERNS` first, or keep git/gh examples out of fences. |
| `static-contract-gates` | MERGE | MERGE — wave one | Independent oracle + mutation probe as lines in the existing qa lens; the probe is implementor-supplied evidence or runs in a throwaway copy (`mutates: none` stays true). |
| `fail-closed-shell-state-machines` | MERGE | MERGE — wave one | The errexit-in-conditional rule as existing-lens lines. |
| `delivery-plan-executability` | MERGE (~35 inline lines) | MERGE as a **references file** + one Plan Review pointer | 81 + 35 breaks review's 90-line ceiling. |
| `isolated-git-commit-workflows` | MERGE | MERGE | `work` `references/exact-path-commit.md`; the commit-first rule lands once. |
| `review-feedback-triage` | MERGE → `fix` | MERGE — **home unresolved** | Floor adopted: the untrusted-data fence sentence in `fix`'s body plus the three-way classification in one on-demand file. Product/delivery: `fix` reference. Architecture: a section of review's pr-evidence. Dissent: one sentence. |
| `grounded-citations` | MERGE → `research` | MERGE → the surviving investigation skill | Cite-from-retrieval rule in the SKILL.md AND the `research-sweep.js` reader prompts in one PR, byte-parity test updated; nothing from its rationale file. |
| `blocked-page-recovery` | MERGE (archive ladder) | MERGE — **one rule only** | "Validate the body, not the status code". The ladder and the script are not taken (contradicts research's contract; a third-party-disclosure policy Genie has not made). |
| `test-driven-development` | MERGE | clause-level | Watch-it-fail generalised in `verify`; tracer-bullet ordering in `work`. `verify` already ships the kernel. |
| `spike` | MERGE | MERGE → `brainstorm` | Risk ordering, three-way verdict, head-to-head; minus the third-party installer line; pinned upstream commit. |
| `sketch` | MERGE | MERGE → `brainstorm` | One-stance-axis variant rule; dissent: waits on the rendered-UI scope decision. |
| `github-issue-to-pr` | MERGE → `wish` SKILL.md | MERGE → **`wish.js` scout brief + hand-dispatch fallback** | Only the keyword duplicate-PR sweep and the `git log -S` design-intent check are new; zero net lines in the 95-line SKILL.md. |
| `dogfood` + `visual-fidelity` | MERGE ×2 | MERGE — ONE observe-only `rendered-ui` lens, **last, conditional** | Needs an explicit owner decision that rendered UI is in product scope; opens with the no-renderer fallback; reuses review's severity vocabulary. Dissent: DEFER. |
| `deslop-code` + `simplify-code` | MERGE ×2 | MERGE — **held** pending the cleanup-lane decision | `fix` accepts only FIX-FIRST gaps and cleanup findings are MEDIUM/LOW, so "apply-half to fix" is dropped. Dissent: `simplify-code` PERSONAL/DROP (duplicates a runtime built-in). |
| `scope-bounded-delivery-planning` | MERGE | **IMPROVE-EXISTING** (4 lenses) | One owner-rejection-reset sentence in `fix`, one stop-work sentence in `work`. |
| `systematic-debugging` | IMPROVE-EXISTING | IMPROVE-EXISTING → `report` | Four clauses; `report` already ships the loop gate, ranked hypotheses, rule of three. |
| `requesting-code-review` | IMPROVE-EXISTING | IMPROVE-EXISTING → `verify` | Baseline-aware failure counting on an already-red gate; nothing from its commit path. |
| `agentic-product-dogfood` | IMPROVE-EXISTING | IMPROVE-EXISTING → `verify` | Mutation/read-back row and FAIL-OPEN vocabulary. |
| `github-pr-workflow` | IMPROVE-EXISTING | IMPROVE-EXISTING → `wish` | The publisher assumes `gh` on every host; the no-`gh` case is fail-closed or git-only. |
| `upstream-contribution-proposals` | IMPROVE-EXISTING | IMPROVE-EXISTING → `report` | Read-back after issue creation. |
| `claude-design` | MERGE | **PERSONAL (5/5)** | Self-declared derivative of a vendor's hosted-product prompt, no upstream or commit; a creation method does not fit a read-only lens. |
| `human-conversation`, `deslop-design`, `concurrency-safe-state-transitions`, `node-inspect-debugger`, `python-debugpy`, `document-to-action-items`, `design-md`, `architecture-diagram`, `popular-web-designs`, `excalidraw` | PERSONAL | PERSONAL (no lens found evidence against) | `popular-web-designs`, `excalidraw` and `python-debugpy` carry an explicit **never ships** note. |
| `codebase-inspection` | DROP | DROP | A cache of one tool's help output. |

Totals after revision: 1 ABSORB (held), 19 MERGE, 6 IMPROVE-EXISTING, 11 PERSONAL, 1 DROP.

Against the owner's prior ("keep five: deslop-writing with deslop-code, systematic-debugging,
artifact-review-integrity, grounded-citations, human-conversation"): four of the five survive — one as the only
ABSORB, three as merges or clauses into skills that already ship their kernel. `human-conversation` is the one
divergence (PERSONAL: a standing response norm whose trigger is a feeling; `work` ships a stronger handoff
contract). The owner's three "sharpest cuts" all shrank to a sentence or a clause; dissent argues the owner's
operating experience should outweigh text judgement there, since no usage data exists.

## Synthesis (relayed unchanged)
Decision: revise
Consensus: Every lens, including dissent, endorses the direction and rejects the table as written. None would ratify it unchanged.

Spot-checks I ran read-only while synthesizing:
- review/SKILL.md is 81 lines, wish is 95, fix is 43 and research is 60. The lens files are 19-21 lines each.
- /home/genie/.genie/skills-install.json records source local:/home/genie/.genie/skills at ref v5.260918.6. /home/genie/.genie/skills now holds 60 entries, where the record lists an inventory of 20.
- The 2026-09-16 audit rules council as 'Merge into review' (line 66) and research as 'Merge into report' (line 77).
- User-owned deslop-code, deslop-design and deslop-writing directories exist in both ~/.claude/skills and ~/.agents/skills.
- In .claude/workflows/wish.js, line 603 ships `gh issue view --comments`, line 611 ships a preflight on DESIGN.md, and line 760 checks only `gh pr list --head <branch>`. A grep finds no `log -S` and no keyword duplicate-PR sweep.

The lenses agree on the following.

1. Shape. Collapsing 38 candidates into at most 1-2 new always-loaded descriptions, folding the rest or reducing them to clauses, matches the house consolidation precedent. The 10 PERSONAL and the 1 DROP are correct, and no lens found evidence against them. IMPROVE-EXISTING is right for systematic-debugging and test-driven-development, because report and verify already ship those kernels.

2. Staging hazard, independent of the plan. The 38 candidates sit inside the recorded install source. inventoryFromSkillsDir enumerates every top-level directory with a SKILL.md. The channel installs with --skill '*', and `skills` is a whole promoted payload member. From the code, a routine install or update would do one of two things:
   - ship all candidates into the recorded agent homes as genie-owned, including the excalidraw upload script, the sudo ptrace_scope text and the 964 KB popular-web-designs payload; or
   - swap the directory out from under them.
   Security adds that 8 house-authored candidates have no second copy on the host.

3. safe-shell-cli-payloads moves from ABSORB to MERGE (5/5). About 70% of it is Orca residue. The portable rule is about 8-15 lines, and report already ships the kernel ('body passed as a file or stdin, never interpolated').

4. claude-design moves from MERGE to PERSONAL (5/5). It is self-declared as derived from a vendor's hosted-product prompt, names no upstream or commit, and is a design-creation method that does not fit a read-only evidence lens.

5. delivery-plan-executability cannot enter review/SKILL.md as ~35 inline lines, because 81+35 breaks the 90-line ceiling. It lands as a references file with one Plan Review pointer.

6. scope-bounded-delivery-planning moves from MERGE to IMPROVE-EXISTING. Four lenses say so and security is silent. fix already says 'without expanding scope' and already has the overdesigned-plan route. Only the owner-rejection reset sentence is new.

7. blocked-page-recovery keeps only the 'validate the body, not the status code' rule. The four lenses that spoke agree. The archive ladder contradicts research's 'record the question as open' and 'primary sources only' contract, and it is a circumvention and third-party-disclosure policy Genie has not made.

8. Nothing mechanical protects the plan's assumptions:
   - skills-lint.ts has no line-count rule, and wish is already over the ceiling.
   - The lint bans only retired Genie tokens. It does not ban vendor, Hermes or hazard vocabulary, so 'rewrite then lint' is a style gate, not a safety gate.
   - The mutates-none rule covers every .md file under review and verify, and it false-positives on `git merge-base`.

9. The targets are changing. All 8 merge targets carry open 2026-09-16 audit verdicts. research→report and council→review change the landing site of 12 or more merges. Those verdicts must be landed or rejected before intake text lands. The unit of landing is one PR per target skill, not one per candidate.

10. refine must not run over existing SKILL.md files. Two contract/parity tests pin dozens of exact strings, and refine's file mode overwrites the source.

11. Provenance is a gate before work starts, and a rewrite does not cure it:
    - seven candidates have no author and no licence;
    - humanizer's example pairs have CC BY-SA lineage;
    - grounded-citations' rationale comes from a leaked prompt;
    - several candidates name a third-party human and no repository.

12. A shipped `deslop-writing` collides by name with user-owned directories already on this host. The installer would replace the owner's copies and take ownership of those paths.

13. github-issue-to-pr's real landing surface is the scout brief in wish.js plus the hand-dispatch fallback. It is not the 95-line wish/SKILL.md, and the issue-thread read is already shipped.

14. Any rendered-UI lens must be observe-only. It must state the no-renderer fallback as a checkable outcome: 'rendering unavailable: gap reported, no visual claim made'.

15. Shared unknowns:
    - whether on-demand references are loaded on Codex, Goose, Windsurf and DSH;
    - no usage data exists for any candidate.
Dissent:
- **dissent:** Opposes ratifying one 27-change plan now. 19 of the 22 taken texts are medium-confidence in the shards' own records, with unchecked flip conditions, and both ABSORBs are medium. Wants a first wave cut to the high-confidence, house-authored, clause-sized items: artifact-review-integrity and pull-request-review-integrity as ONE fence-free evidence-identity reference; the errexit rule and the mutation probe as lines in the existing qa lens; and the five IMPROVE-EXISTING clauses. Before wave two, measure whether references load on at least Claude Code and Codex. Not adopted as the decision, because four lenses hold that a revised table is deliverable. Adopted as the wave-one shape in the conditions.
- **dissent:** The description-cost premise behind merge-by-default is smaller than stated. The 20 shipped descriptions total 2,844 characters, about 35 tokens per skill per turn. The merges spend the expensive axis instead: the 8 lifecycle skills every session loads, plus references that may never load. If references do not load, a few more top-level skills or fewer imports is the more honest choice.
- **dissent:** dogfood and visual-fidelity: MERGE → DEFER (stay personal) until the product decides whether rendered UI is in scope. Resolve this together with deslop-design, which shard A marked PERSONAL with HIGH confidence on the no-renderer argument. CLAUDE.md states 'there is no Genie UI any more'. The shipped tree's lack of rendered-UI vocabulary may be a deliberate boundary and not a gap. If UI is declared in scope, the mutating exploration ladder belongs to a verification surface, not to read-only review. sketch's variant rule waits on the same decision. Architecture, delivery, product and security keep the merge as one observe-only lens, landed last.
- **dissent:** simplify-code: MERGE → PERSONAL/DROP. It declares 'inspired by Claude Code /simplify', and at least one target runtime already ships a built-in with the same description, including 'altitude'. Product's unknowns partly corroborate the duplication with the Claude Code built-in. Delivery keeps it merged with deslop-code as one review references file.
- **dissent:** deslop-code: the cleanup lane dead-ends as planned. fix accepts only a FIX-FIRST review, and review's severity table makes maintainability MEDIUM and style LOW, both non-blocking. A cleanup finding therefore never reaches fix. The plan's own scope-bounded merge into fix also argues against fix taking on a cleanup lane. The preventive half ('code being written') belongs in work's executor guidance and the read-only half in code-quality.md; drop the 'apply-half to fix' wording. Architecture and delivery raise the same missing mutating entry point as an unknown. Delivery still routes the apply half as one sentence in fix.
- **dissent:** deslop-writing is weak on the brief's own ABSORB bar ('changes agent behaviour on most engineering sessions'). It targets general prose ('characterization, continuity, tension'), carries owner-locale residue, and has no author, licence or git history. If provenance does not clear, fold its invariants into docs and authoring (shard A's fallback). humanizer should become PERSONAL/DROP with only the self-audit loop taken as an idea, because a 34-pattern catalogue contradicts the host skill's 'no punctuation ban / contextual clues' stance.
- **dissent:** scope-bounded-delivery-planning, review-feedback-triage and upstream-contribution-proposals are the owner's named 'sharpest cuts'. No usage data exists, so the opinion of the one person who has run these skills should outweigh shard judgement from text. review-feedback-triage becomes IMPROVE-EXISTING, one sentence. systematic-debugging stays IMPROVE-EXISTING only if report ('Investigate; never fix') actually triggers for an engineer debugging in-flight work. That is shard B's own flip condition and is unchecked.
- **dissent:** Replace 'rewritten with the refine skill' entirely with 'written under the authoring skill, gated by skills:lint plus the pinned contract tests'. refine knows nothing of the frontmatter, house size, lint or runtime-neutral voice. It cannot fold text X into file Y. It defaults to one vendor's guidance, and it carries its own open audit verdict.
- **product:** review-feedback-triage stays a MERGE into fix as references/inbound-feedback.md, explicitly against the owner's cut. A grep of all 20 shipped skills finds only dream/SKILL.md:55 on inbound bot comments. This repo's own CLAUDE.md carries a six-rule PR Review Rules section by hand, so other users have no equivalent. Delivery also lists the merge as deliverable as stated.
- **architecture:** review-feedback-triage should be retargeted to a section of review/references/pr-evidence.md. Its sections 2-4 repeat pull-request-review-integrity's three GitHub facts, and the triage is a read-only classification that fits mutates:none. fix gets one pointer sentence. That gives one file instead of two, and the facts land once.
- **product:** deslop-writing is a justified ABSORB as the one new description. Genie's deliverables are mostly prose (WISH.md, DESIGN.md, PR bodies, reports). The docs skill covers workflow and truthfulness but not prose quality. The candidate is already 53 lines, and it is the owner's first pick. It ships alone under a house name such as `prose`, with a one-sentence description free of language-pair residue. references/ai-tells.md is deferred.
- **product:** github-issue-to-pr remains a MERGE, not an IMPROVE, because its flip condition is not met. The saved workflow runs on Claude Code only, so the hand-dispatch fallback prose is the only place the other four runtimes would see the duplicate-PR sweep.
- **product:** Any rule whose trigger is ubiquitous or safety-relevant goes inline at the point of use, not in references/. That covers shell payload quoting, read-back of external writes, and possibly errexit-in-conditional. The GitHub-specific grafts must stay in reference files so review's body remains forge-neutral.
- **security:** The deslop-writing ABSORB is blocked, not approved, until three things happen. First, the owner states authorship and an MIT grant in the PR, with a NOTICE row. Second, ai-tells.md is written without humanizer or the Wikipedia guide in the writer's context, or ships as a separately licensed CC BY-SA file. Third, the owner chooses the name knowingly. The installer treats a recorded agentDir x inventory pair as genie's own by identity, so the personal copy is backed up once, overwritten, and then reported as 'modified locally' on every personal edit.
- **security:** The plan adds attacker-writable inputs to skills that have no data fence: PR threads into review, and inbound bot and reviewer text into repo-writing fix. Every prohibition and fence must live in the target's SKILL.md body, never only in references/. That covers the untrusted-data sentence, authorization-before-mutation, never-in-a-shared-checkout and never-delete-uncommitted-work. A mechanical hazard-and-residue gate is needed before the first merge, because the lint would pass sudo, npx third-party installers, token discovery, git stash, git add -A, permission-bypass flags, Hermes vocabulary and private names today.
- **security:** blocked-page-recovery may keep a read-only Wayback availability lookup alongside the validate-the-body rule. It needs an explicit ban on submitting non-public, credentialed or signed URLs to any third party, and on triggering any save/capture endpoint. The full ladder is a recorded human product-policy decision, not a skill port. Security notes that even the lookup discloses the target URL. Architecture and dissent take the rule only; dissent makes the candidate PERSONAL.
- **security:** The intake paperwork is itself a leak path. The repo is public and .genie/wishes is git-tracked. Shard JSONs quote a private client's identifiers, a named individual and a private memory id. WISH.md, PR bodies and commits must cite candidates by name and line number only. Keep mutates:none true: the static-contract-gates mutation probe is implementor-supplied evidence or runs in a throwaway copy, and verify never originates a write.
- **architecture:** Settle the house question shard A raised before the ABSORB: does agent-output voice belong in AGENTS.md or in a skill? The answer decides whether deslop-writing is an ABSORB at all or a merge into docs and authoring. popular-web-designs, excalidraw and python-debugpy should carry an explicit 'never ships' note so a later re-intake cannot promote them on content quality alone. grounded-citations must land in both research/SKILL.md and the research-sweep.js reader prompts in one PR, or the rule never reaches the sweep.
- **delivery:** claude-design becomes PERSONAL only 'unless provenance is cleared'. If it is cleared, fold it into the single rendered-ui lens. Hold any new shipped directory on the dev channel until a measured-run bar is met, following the `quick` precedent: three recorded sessions where it fires and the output differs. After a stable release, rollback means retirement plus a permanent legacy-catalog entry. Per-candidate PRs would mean about 27 dev releases of skills-only churn, and a failed dev release cannot be re-run.
Conditions:
- PREREQUISITE 0, independent of the plan. Before any `genie install` or `genie update` on this host, the owner relocates all 38 candidates out of /home/genie/.genie/skills, which is the recorded install source and a whole promoted payload member. The ten PERSONAL candidates and the DROP candidate move too. The destination is backed up and lies outside GENIE_HOME/skills and outside every agent skills home. The owner then confirms that skills-install.json still lists 20 skills. Private residue is stripped even from the personal copies before they sync anywhere: the 'For Felipe' line, the named individual, the client reference in concurrency-safe-state-transitions, and the brn_ memory id.
- Revise the disposition table before ratifying it. (a) safe-shell-cli-payloads: ABSORB → MERGE. The core quoting rule goes inline at the point of use in work. report's existing 'file or stdin, never interpolated' sentence is extended to the wish publisher and to the work and dream card-comment sites. One verify 'what proves what' row is added for read-back of the stored body. The Orchestration-Specific Guard, the four orca-* references and all permission-bypass flag names are dropped. (b) claude-design: MERGE → PERSONAL. (c) scope-bounded-delivery-planning: MERGE → IMPROVE-EXISTING, with one sentence in fix (folded into fix's pending audit Update) and one stop-work sentence in work. (d) blocked-page-recovery reduces to the validate-the-body rule in whichever investigation skill survives; the archive ladder and the script are not taken. (e) delivery-plan-executability becomes a references file with one Plan Review pointer. (f) github-issue-to-pr lands in the wish.js scout brief plus the hand-dispatch fallback, with its parity test. It adds only the keyword duplicate-PR sweep and the `git log -S` design-intent check, routes through the existing scout fence, and adds zero net lines to wish/SKILL.md. (g) humanizer contributes only the self-audit loop; references/ai-tells.md is deferred.
- The deslop-writing ABSORB is held, not approved, and lands last. Its gates are as follows. The owner gives a written authorship statement and MIT grant, with a NOTICE row. A non-colliding house name is chosen, or the owner first retires the personal directories in ~/.claude/skills and ~/.agents/skills, with a NOTICE collision-policy row either way. The description is one sentence with no language-pair residue. It ships without the humanizer taxonomy. The 'agent-output voice: AGENTS.md or skill?' question is answered. The hop is rehearsed on a replica HOME that holds a same-named user directory. The skill is held on the dev channel until a measured-run bar is met. If provenance does not clear, its invariants fold into docs and authoring.
- Land or explicitly reject the 2026-09-16 audit verdicts for each target skill before, or in the same PR as, that skill's intake text. This includes research→report and council→review. Retarget grounded-citations and the blocked-page rule to the surviving investigation skill. grounded-citations lands in both the SKILL.md and the research-sweep.js reader prompts, with the byte-parity test updated, and takes nothing from references/grounding-rationale.md.
- Landing unit and order. Land one PR per target skill, about 8-10 in total, each small enough to read whole. Wave one is the high-confidence, house-authored, clause-sized set: evidence-identity and pr-evidence as one fence-free identity decision; the errexit rule and the mutation probe as existing-lens lines; and the five IMPROVE-EXISTING clauses. Later waves cover fix and work; brainstorm (spike and sketch minus the `npx get-shit-done-cc` line, sibling-skill deference and the CDN script); the remaining review lenses; the rendered-ui lens last; and then the ABSORB alone.
- Make the size limit a gate before any review lens PR. Either add a 40-90 line SKILL.md check to scripts/skills-lint.ts, with wish's 95 fixed or explicitly waived, or require before and after `wc -l` in each PR. review/SKILL.md grows by table rows and one-line pointers only, at most 9 lines in total. No target goes past 90 lines. State a maximum line count per lens and a maximum number of reference files per skill.
- Rewrite mechanism. The authoring contract is the oracle. refine, if used at all, runs only on newly written reference text or added clauses. It runs on copies and never in file mode, with candidate content declared as data. It never runs over an existing SKILL.md. Every PR runs skills:lint, scripts/task-conversation-contract.test.ts, wish-workflow-parity.test.ts, research-sweep-workflow-parity.test.ts, skills-inventory-parity.test.ts and skills-lint.test.ts. Before pr-evidence lands, fix the `git merge-base` false positive in REPO_WRITE_PATTERNS, or keep all git and gh examples out of fences in references owned by review and verify.
- Add a mechanical hazard-and-residue gate before the first merge lands, as a lint extension or a per-PR checklist grep. It fails on: sudo; unpinned npx; pip install or --break-system-packages; curl with an Authorization header; GITHUB_TOKEN; .git-credentials; git stash; git add -A; --dangerously; SKIP_TRUST; CDN script hosts; Hermes vocabulary; brn_ ids; and the owner's and client's names. Each landing PR carries a named exclusion list: TDD's 'Delete means delete'; requesting-code-review's stash baseline and the `git add -A`/[verified] commit; github-pr-workflow's token discovery and curl fallback (the no-gh case is fail-closed or git-only); the spoofed User-Agent; every bundled script; and pull-request-review-integrity's two dangling reference citations. No Python and no network-reaching executable enters skills/.
- Security-bearing rules live in SKILL.md bodies, not only in references/. fix gains the untrusted-data fence sentence in its body. review's PR-evidence path records instruction attempts the way wish and research already do. Keep mutates:none true for review and verify. dogfood is observe-only (navigate, snapshot, console, screenshot). The mutation probe is implementor-supplied evidence or runs in a throwaway copy, worded so the pinned strings stay byte-identical. verify reads back a write the actor was already authorized to make and never originates one.
- Rendered UI. dogfood plus visual-fidelity become ONE references/lenses/rendered-ui.md in the house lens shape. It lands last and is conditional on an explicit owner decision that rendered UI is in product scope. It opens with the capability fallback re-authored as an idea from deslop-design ('rendering unavailable: gap reported, no visual claim made'). It reuses review's existing severity and verdict vocabulary. It contains no form submission. There is no second UI lens file.
- Cleanup lane. Decide whether 'improve working code on request' has a shipped mutating entry point BEFORE landing deslop-code and simplify-code. Until that is decided, keep both out of wave one and drop the 'apply-half to fix' wording, because fix accepts only blocking FIX-FIRST gaps and cleanup findings are MEDIUM or LOW. Record dissent's PERSONAL/DROP position on simplify-code against the runtime built-in duplication.
- Provenance gate per landing PR. The no-author candidates still taken as text need an owner declaration. Candidates naming a third-party human with no repository (dogfood, github-issue-to-pr, requesting-code-review) contribute ideas re-expressed from scratch, with zero verbatim text, unless the upstream repo, LICENSE and commit are recorded in the existing NOTICE format. spike and sketch get a pinned commit. Every adapted text carries the house attribution comment.
- Reference reachability. Run one probe per runtime class, with a task that needs a rule stored only in an existing review lens file, before landing the bulk of the references files. Where a runtime does not load the file, that runtime's load-bearing rules stay inline and the plan shrinks to fit.
- Paperwork hygiene. WISH.md, PR bodies, commits and review evidence cite candidates by name and line number only. They never quote the client file, the named individual or the memory id, and the client reference is copied nowhere under the repository. popular-web-designs, excalidraw and python-debugpy carry an explicit 'never ships' note.
Evidence gaps:
- Do agents on Codex, Goose, Windsurf and DSH open references/ and lens files on demand? All five lenses flag this. If they do not, most of the ~20 merges change no behaviour. The answer then moves toward fewer inline clauses or, per dissent, a few more top-level skills.
- No usage data exists for any candidate. Every 'distinctive value' claim is judgement from text, the owner's prior is the only operating evidence, and the ABSORB's measured-run bar has no baseline.
- Authorship and licensability of the seven no-author candidates are unknown. Open questions: can the owner grant MIT for all of their text; does CC BY-SA share-alike reach a freshly written AI-tells taxonomy; and how much of claude-design and simplify-code is verbatim vendor text.
- Will the 2026-09-16 audit verdicts research→report and council→review be ratified? That decides the targets for 12 or more merges and review's remaining line budget. It is also unknown which of the 18 open verdicts are already in flight. Delivery and product found no landing wish or ledger for them.
- What a routine `genie install` or `genie update` would actually do to the 38 staged candidates is unconfirmed: ship 58 to every recorded agent home, or swap the member wholesale. Whether promotion retains the displaced `skills` member long enough to recover from is also unknown. The code paths were read and nothing was executed. Either outcome is bad.
- Is rendered UI inside Genie's product boundary? Do users run Genie against front-end repos on hosts that can render? Is the absence of rendered-UI vocabulary in the shipped tree a gap or a deliberate scope decision?
- Does a cleanup lane (improving working code on request, outside a lifecycle gate) need its own mutating entry point? No shipped route from 'clean this diff' to an edit was found. On Claude Code the host already ships simplify and code-review built-ins, and the other four runtimes were not checked.
- Does report ('Investigate; never fix') actually trigger for an engineer debugging their own in-flight work? That is shard B's flip condition for systematic-debugging, and the owner's stated reason for keeping it.
- Do any hosts key permission decisions on the `mutates` frontmatter? If so, label drift in review and verify is a privilege path and not only an honesty defect.
- Does fail-closed-shell-state-machines' errexit rule fire often enough in Genie sessions to justify inline placement over a lens?
- Does any candidate contain instruction-shaped text aimed at a downstream rewriter? The shards reported none. Security verified specific hazard lines by grep only and did not re-read all 38 directories.
- Is a read-only Wayback lookup acceptable as default research behaviour, given that it discloses the target URL to a third party? No product policy on third-party disclosure of research targets exists in the repo.
- Would refine, which has an open 'Update' verdict and one-vendor guidance modes, produce runtime-neutral text? The conditions avoid this by making authoring the oracle, but it is untested.
- How many adopter hosts, beyond the dogfood host, carry a personal skill named deslop-writing or another name the intake might ship?
Next action: The council executed nothing. This one step is for the owner, before any `genie install` or `genie update` on this host.

1. Copy the 38 candidate directories from /home/genie/.genie/skills to a backed-up location outside GENIE_HOME/skills and outside every agent skills home. Include the 10 PERSONAL candidates and the 1 DROP candidate.
2. Verify the copy. Only then remove the originals from the delivered tree.
3. Confirm that /home/genie/.genie/skills-install.json still lists an inventory of 20.

All five lenses rank this first. It is independent of the plan decision, and it keeps the intake source and the 'stays user-owned' dispositions safe while the revised table is drawn up. The procedure file is /home/genie/workspace/repos/genie/.genie/wishes/skill-intake/PROCEDURE.md.

Conflict resolution: 1. Decision label. Four lenses returned support-with-conditions and dissent returned oppose. All five require changes to the disposition table itself, to the rewrite mechanism and to the landing sequence. At least two dispositions change unanimously (safe-shell-cli-payloads, claude-design). What is supportable is a different plan from the one assessed, so the decision is 'revise' and not 'proceed-with-conditions'. Dissent's opposition is to the batch size and mechanism, not the direction. Its first-wave cut is adopted as wave one.

2. Dispositions were changed only where lenses converged or where a repo-verifiable fact decided the matter.
   - Unanimous: safe-shell-cli-payloads → MERGE; claude-design → PERSONAL.
   - Unopposed majority: scope-bounded → IMPROVE-EXISTING (4 lenses); delivery-plan-executability → references file; blocked-page-recovery → one rule; humanizer → self-audit loop only.
   - Fact-decided: github-issue-to-pr retargets to wish.js, per my spot-check listed in the consensus. Delivery's 'design preflight already exists' concerns a different check (a preflight on DESIGN.md) from the git-history design-intent check. Architecture's and product's scope of two new clauses therefore stands, and product's hand-dispatch fallback is kept for the four runtimes that do not run the saved workflow.

3. Placement conflict on safe-shell. Three lenses want a work reference plus a verify row, product wants it inline in work and wish, and security wants it in work's body. The unresolved reference-loading unknown and security's 'prohibitions live in bodies' rule both favour the point of use. The rule therefore goes inline where commands are built, with a verify row. A reference is optional.

4. review-feedback-triage has no majority. Product and delivery keep the fix reference, architecture retargets it to review's pr-evidence, and dissent wants one sentence. I did not force this. The common floor is adopted: the untrusted-data fence sentence in fix's SKILL.md body (security), plus the three-way classification in some on-demand file. The file's home is deferred until the reachability probe and the de-duplication against pr-evidence are done. All three positions are preserved in dissent[].

5. Rendered UI. Four lenses keep a single observe-only lens and dissent defers. I kept the merge but made it last and conditional on an explicit product-scope decision, which is dissent's flip condition. The shards contradict each other here, and no usage evidence exists.

6. Cleanup lane. Dissent's dead-end argument rests on shipped text: fix accepts only a FIX-FIRST review, and maintainability and style are non-blocking severities. Two other lenses raised the same point as an unknown. The 'apply-half to fix' wording is dropped, and deslop-code and simplify-code are held out of wave one pending that decision. This does not adopt dissent's DROP of simplify-code, which stays a recorded minority position.

7. deslop-writing. Product supports the ABSORB, security blocks it and dissent calls it weak. It is resolved as held and last, with gates. That honours product's case and the owner's first pick without approving text that has no licence or a colliding name.

8. refine. Dissent wants it removed, architecture and security want it bounded. It is resolved as authoring being the oracle, with refine optional on new text and copies only. That satisfies all three.

9. blocked-page-recovery. Security's read-only Wayback lookup is a minority addition with its own disclosure unknown. It is recorded, not adopted, pending an owner policy decision.

10. Where the evidence was insufficient (reference loading, usage, provenance, the audit verdicts, UI scope), I recorded gaps and did not manufacture agreement.

