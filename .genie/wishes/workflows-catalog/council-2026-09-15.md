# Council: Make the genie repo's own .claude/workflows/ directory the canonical, git-tracked catalog of saved workflows, with council.js as the first path-free example, and have a fork of the dsh_workflow DSH plugin discover and execute those same files from the same paths so one script runs on Claude Code or on a DSH body.

## Lenses
### architecture
Verdict: support-with-conditions
Confidence: medium
Key evidence:
- Catalog half is real and cheap: `.claude/workflows/pm-ledger-verify.js` is already git-tracked (`git ls-files .claude`), `council.js`/`README.md` are untracked but not ignored (`git check-ignore` exit 1), and `bun test scripts/workflows-meta.test.ts` passes 6/6 on both scripts.
- Native contract matches the README: the `workflow-authoring` skill confirms `agent()` null-on-failure, `pipeline` no-barrier `(prev,item,index)`, `parallel` throw->null, one-level `workflow()` nesting, and that `Date.now()/Math.random()/new Date()` throw because they break resume; subagents get CLAUDE.md injected.
- The orphan `~/.claude/workflows/council.js` (Jul 10, 26KB) hard-codes `LENS_ROOT='/Users/feliperosa/.genie/plugins/genie'`; that dir now holds an Orca plugin payload and `references/lenses` does not exist, so the stamped path is dead. It has no `council.js.genie-sync.json` sidecar, so `src/lib/legacy-integration-retirement.ts:579-591,1791+` classifies it `unmanaged` and leaves it, which is why the README's manual-delete step is the only remedy.
- Nothing ships the catalog: `package.json` `files` is `dist/, skills/, plugins/genie/, templates/, README.md, LICENSE`, and `biome.json` ignores `.claude`, so the static test is the sole gate on catalog scripts (no lint, no runtime in CI).
- DSH on this host is `0.1.1-rc.2` for every `@deepseek-ai/*` package (`dsh --version`, `~/.dsh/profiles/node_modules`), below the `>=0.1.2-rc.1` floor `plugins/dsh-genie-board/package.json` declares and the wish pins; `scripts/dsh-genie-board-smoke.ts` pins no version, it uses whatever `dsh` is on PATH. npm does publish `0.1.2-rc.1` and is already at `0.1.6-alpha.1`.
- DSH 0.1.1-rc.2 ships a FIRST-PARTY workflow engine the wish never evaluates: `@deepseek-ai/dsh-workflow` (ctx.workflowEngine seam, `WorkflowStartRequest{script, meta, args, subagentProvider, maxTotalAgents, parent, signal}`), `dsh-tool-workflow` and `dsh-workflow-worker-thread` (node `vm`, bridges `agent()` to `ctx.subagents`). Its guest API is body-style with top-level await, `agent(prompt,{schema,model})`, `pipeline` (no barrier, `(prev,item,index)`, throw->null per item), `parallel` (barrier, throw->null), `phase`, `log`, `args`; it rejects `export const meta` at parse time (`dsh-workflow-worker-thread/lib/index.js:804-814`, code `SCRIPT_PARSE`) and has no saved-workflow catalog, no `budget`, no `workflow()`, no `effort`/`isolation`, no clock ban.
- The first-party tool description states 'Misused hooks (bad arguments, unknown options...) throw errors that ALWAYS kill the script' — DSH's own policy is fail-loud, which contradicts the fork's planned 'drop unknown options with one warning' policy.
- `@dsh-external/workflow` (the wish's stated npm name for the fork target) returns 404 on npm; the upstream is git-only, so 'replace the private snapshot with npm packages' can only apply to the `@deepseek-ai/*` devDependencies, not to the plugin itself.
- `ctx.subagents` at 0.1.1-rc.2 (`dsh-subagent/lib/types/types.d.ts`): `SubagentStartRequest{label, prompt, parent, signal, agentOptions, outputSchema, maxDepth, toolFilter, persona}` gated by `SubagentCapabilities{outputSchema, depthLimit, toolFilter, persona}` with typed rejection rather than silent degradation — a provider lacking `outputSchema` would reject every `schema` agent call council.js makes.
- Name collision: `skills/council/SKILL.md` declares `name: council` and the new workflow is `meta.name: 'council'`. Workflows and skills share the slash namespace (this session lists `pm-ledger-verify` with the workflow's `whenToUse` text, and `council` with the SKILL.md description). `scripts/skills-lint.ts` does not reference workflows, so nothing guards the collision, and the QA criterion '`/council` appears in a session' is already satisfied by the skill.
- `scripts/workflows-meta.test.ts` FORBIDDEN list covers `import`, `require(`, `Date.now`, `Math.random`, `/Users/|/home/x`, but not argless `new Date(`, which the README and the native runtime both forbid.
Risks or objections:
- Long-term cost is concentrated in the fork, not the catalog: it forks a git-only external plugin (QuickJS, `run(wf,args)`) whose test gate is welded to a private snapshot, against an rc series that moved from 0.1.1-rc.2 to 0.1.6-alpha.1 already, while DSH itself ships a first-party engine with the same `agent/pipeline/parallel/phase/log` vocabulary. Two engines on one host with the same words but different meta transport and option policies is the classic drift trap the wish's own conformance suite exists to paper over.
- The unknown-option policy ('drop with one warning') diverges from both bodies: Claude Code accepts `effort`/`isolation`/`agentType` and acts on them; DSH's first-party engine kills the script on unknown options. A workflow that relies on `isolation:'worktree'` for correctness would silently run unisolated on a DSH host that lacks the adapter.
- Version floor is not reproducible on the only DSH host at hand: `0.1.1-rc.2` installed vs `0.1.2-rc.1` pinned; the `dsh-genie-board` 'proven on 0.1.2-rc.1' claim cannot be re-run here without upgrading DSH first, and the fork's Group 1 BLOCKED gate depends on that host.
- Catalog contract has no runtime enforcement anywhere in CI and no lint (biome ignores `.claude`); regressions in script semantics (not syntax) only surface in a paid native run.
- Slash-namespace ambiguity: two `council` entries (skill and workflow) with no lint; whichever wins is undocumented and may differ across Claude Code versions.
- The orphan `~/.claude/workflows/council.js` outlives retirement by design (`unmanaged`, no sidecar) and, in any repo without a project-scope `council.js`, still runs against a dead lens path; the fix is a README sentence only.
- Canonical multi-body catalog lives under a Claude-branded path (`.claude/workflows/`) that the DSH fork must hard-code as a discovery root; acceptable under the stated constraint, but it is a permanent naming coupling to one vendor's convention.
- `ctx.subagents` capability gating (`outputSchema`, `depthLimit`) is provider-dependent; council.js uses `schema` on every call, so a DSH deployment whose default provider lacks `outputSchema` fails all six agents and returns `{ok:false}` rather than degrading.
Required conditions:
- Before fork Group 1 starts, record a written comparison in the `dsh-workflow-fork` wish between forking `omdsh-dev/dsh_workflow` and writing a thin loader plugin over the first-party `ctx.workflowEngine` seam (`@deepseek-ai/dsh-workflow` `WorkflowStartRequest{script, meta, args}`): a loader that reads `.claude/workflows/<name>.js`, splits `export const meta` off the body, and submits `{script, meta, args}` needs no QuickJS, no `run(wf,args)` transform and no snapshot detachment. State explicitly which Claude Code hooks the first-party engine lacks (`budget`, `workflow()`, `effort`, `isolation`, clock ban) and why adding them in a fork is cheaper than adding them over the seam.
- Reconcile the DSH floor with reality: Group 1 must record `dsh --version` on the actual target host (today `0.1.1-rc.2`), and either upgrade that host to `>=0.1.2-rc.1` and re-run `scripts/dsh-genie-board-smoke.ts` there, or lower the floor with the re-proof recorded. `compatibility.json` should name the exact tested version, not just the series.
- Correct the wish's upstream identity: `@dsh-external/workflow` is not on npm (404). Restate the fork target as git-only and scope 'replace the private snapshot with npm packages' to the `@deepseek-ai/*` devDependencies only.
- Make the unknown-option policy explicit per option and prove it in conformance: options that change correctness (`isolation`) must fail loud on a host without the adapter, matching DSH's documented 'misused hooks always kill the script' rule; only cosmetic options (`agentType` when no persona is used) may be dropped with a warning. Add an `unknown-option` fixture to the golden set.
- Resolve the `council` name collision before the catalog wish ships: either rename one surface or add a disjointness assertion (skill `name:` set vs `.claude/workflows/*.js` `meta.name` set) to `scripts/workflows-meta.test.ts` or `skills-lint.ts`, and record which surface `/council` resolves to in the native run evidence.
- Extend `scripts/workflows-meta.test.ts` FORBIDDEN to argless `new Date(` so the static test matches the README and the native runtime.
- Record the shadowing proof, not just the claim: the recorded native `/council` run in this repo must show the tracked script's `meta.description` ('Pressure-test a decision...') rather than the orphan's ('Convene a lens council...'), while the orphan is still present under `~/.claude/workflows/`.
- For the fork's `ctx.subagents` seam check (Group 1), assert the chosen provider reports `SubagentCapabilities.outputSchema === true` and `depthLimit === true` on the pinned DSH version, since every council.js and pm-ledger-verify.js agent call passes `schema`.
Unknowns:
- Whether the fork target's `ctx.subagents` usage (analysed against a `0.0.1-rc.2`-era seam) matches the 0.1.x `SubagentStartRequest`/`SubagentCapabilities` surface; only the 0.1.1-rc.2 typings were inspectable here, no rc.2 baseline to diff against.
- Whether the private `dsh2026/test-icetomoyo` snapshot's API equals any published `@deepseek-ai/*` version; not verifiable without the snapshot.
- Whether third-party DSH plugins may `ctx.inject(['workflowEngine'])`: `runtime-types.d.ts` calls the handles 'host-only', while `dsh-genie-board` proves injection of other host services (`workspaceRegistry`, `webServer`, `connection`).
- The native Workflow tool's project-over-user precedence rule is asserted by the README but not stated in the `workflow-authoring` skill text; it is consistent with `pm-ledger-verify` surfacing from `.claude/workflows/` in this session but not proven for the shadowing case.
- npm dist-tags for the `@deepseek-ai/*` packages: `npm view @deepseek-ai/dsh-workflow version` reports `0.0.1-rc.1` (modified 2026-09-15) while the host runs `0.1.1-rc.2`, so a plain `npm install` without an exact version may not resolve the intended series.
- Which surface Claude Code resolves for `/council` when a skill and a project workflow share the name; not testable from a subagent without the Workflow tool.

### delivery
Verdict: support-with-conditions
Confidence: medium
Key evidence:
- The catalog half is already shippable and cheap to roll back: `.claude/workflows/council.js` (212 lines, untracked), `README.md` and `scripts/workflows-meta.test.ts` are the whole diff; `bun test scripts/workflows-meta.test.ts` passes (6 pass / 0 fail) and `bun run wishes:lint` is OK (88 files). `scripts/*.test.ts` has 30 precedents in the root `bun test` sweep, `knip.json` `project` excludes `scripts/**`, and `biome.json` ignores `.claude`, so the new files join `bun run check` with no gate changes.
- `pm-ledger-verify.js` is already tracked in `.claude/workflows/` (commits a11f785b2, 614219ee8) and its `meta.whenToUse` text is surfaced verbatim in this session's skill listing, which is direct evidence that project-scope `.claude/workflows/*.js` discovery works without any install step.
- Sequencing gap inside the catalog wish: the delivery order is inverted from a testability standpoint. Group 2 (the static test) is declared `depends-on: 1`, but the test is the only CI-checkable spec; landing it first with `pm-ledger-verify.js` as the passing fixture would have made Group 1 red-then-green. The wish's own validation for Group 1 is a hand-rolled `bun -e` parse, i.e. a weaker copy of the Group 2 test.
- The native `workflow-authoring` reference (loaded this session) confirms the contract the README states: `export const meta` pure literal, `agent()` null on skip/terminal error, `parallel` null-on-throw, one-level `workflow()` nesting, `Date.now()/Math.random()/new Date()` throw, resume cache keyed on script + args. It does NOT document project-vs-user scope shadowing; that rule is asserted only in the README.
- Fork-target claims verified read-only at `omdsh-dev/dsh_workflow@44b83c1` (which is still upstream HEAD, last commit 2026-08-13): `validateRestrictedWorkflowSource` requires `async function run(wf, args)` (src/source-policy.ts:81-82); `runtime.ts:413` calls `run(wf, args)`; `src/index.ts:36` `inject = ['subagents','tools']`, `:668` `ctx.inject(['systemPrompt'])`, `:105` `approvalMode` default `generated-and-local`; `tests/engine.spec.ts:43-92` holds the `FakeSubagents` runtime the wish plans to extract.
- One supplied evidence claim is false: the upstream ALREADY bans the clock. `src/runtime.ts:243-255` (inside `GUEST_BOOTSTRAP`) throws on `Math.random`, `Date.now` and argless `new Date` with messages like 'Date.now is disabled; pass a timestamp through args'. The fork's 'clock ban' deliverable is therefore an error-message alignment task, not new machinery, and the analysis the wish calls 'verified at 44b83c1' missed it.
- The private-snapshot claim is confirmed: `package.json` devDependencies are 14 `link:../test-icetomoyo/*` entries, `vitest.config.ts` throws if `DSH_SNAPSHOT_DIR`/`../test-icetomoyo` is absent, `compatibility.json` records `dsh2026/test-icetomoyo` at `0.0.1-rc.2`, and `gh repo view dsh2026/test-icetomoyo` cannot resolve it. Every package the vitest aliases name (`@deepseek-ai/dsh-*`, `cordis`, `schemastery`, `dsh-workflow-worker-thread`, `dsh-tool-workflow`, `dsh-jobs`, `dsh-user-questions`) IS published on npm with a `0.1.2-rc.1` version, so the detach is mechanically possible; API drift from `0.0.1-rc.2` to `0.1.2-rc.1` remains untested.
- `@dsh-external/workflow` is not on npm (E404) despite the package.json name and the wish's 'npm `@dsh-external/workflow`' note, and `automagik-dev/dsh_workflow` does not exist yet; the fork must be bootstrapped from git, and Group 1's `pnpm install --frozen-lockfile` acceptance will need a lockfile regenerated after the `link:` removal.
- The 'proven on DSH 0.1.2-rc.1' evidence proves less than the fork needs: `plugins/dsh-genie-board/src/index.ts:19` injects only `workspaceRegistry`, `webServer`, `connection`. It never touched `ctx.subagents`, `systemPrompt` or `dynamicWorkflows`, which are the seams the fork depends on. The locally installed `dsh --version` is `0.1.1-rc.2`, below the `>=0.1.2-rc.1` floor, so this machine is not yet a valid host for either the plugin floor or the fork.
- The genie static test's forbidden-token list (`import`, `require`, `Date.now`, `Math.random`, quoted absolute path) is strictly narrower than the DSH restricted policy (`source-policy.ts:14-25`: `process`, `\bfs\b`, `fetch`, timers, `globalThis[`, `.constructor`, `__proto__`, `Function(`, `exec(`/`spawn(`). A script can pass genie CI and be rejected at DSH parse time, e.g. any prompt string containing the bare word `fs`. Today `council.js` and `pm-ledger-verify.js` trip none of them (grep-checked), but nothing enforces that.
- Latent bug in the tracked example the static test cannot see: `pm-ledger-verify.js` does `results.filter(Boolean).flatMap((r, i) => ... LENSES[i].key)`, so if any lens returns null the later findings are attributed to the wrong lens. It demonstrates that the catalog contract is static-only and that `agent()`-null handling needs a runtime fixture (the fork's `parallel-null-isolation` conformance fixture is the right place).
- Roadmap tracking: `.genie/roadmap.json` now carries six new cards for the two wishes, all `status: ready`, `blocked_by: null`, and `task_dependencies` is an empty array, although the wishes declare Group 2→1, 3→2, 4→3 and `dsh-workflow-fork depends-on workflows-catalog`. `genie task checkout` would therefore offer `conformance` before `fork-bootstrap`.
- Migration/rollback surface is small and explicit: the only pre-existing state is the orphan `~/.claude/workflows/council.js` (26 KB, dated Jul 10) whose `LENS_ROOT = '/Users/feliperosa/.genie/plugins/genie'` points at a directory whose `references/lenses` no longer exists; genie writes nothing under `~/.claude`, so rollback is `git rm` of three files. Note both wishes flipped DRAFT→APPROVED on disk during this assessment and the test's absolute-path regex was tightened to quoted paths under `/Users|/home|/opt|/var|/tmp`.
Risks or objections:
- Name collision between the `council` skill (`skills/council/SKILL.md`) and the `council` workflow: this session's listing shows only the skill's description under `council`, never the workflow's `whenToUse`, while `pm-ledger-verify` (workflow-only) is listed. `/council` may resolve to the skill rather than the catalog script, so the wish's 'one recorded native `/council` run' criterion could be satisfied by the wrong surface unless the run records how it was invoked.
- The fork wish's analysis is presented as fully verified but contains at least one material error (the clock ban already exists upstream) and one inaccuracy (`@dsh-external/workflow` on npm). Other unverified-here claims (pipeline `(prev,item,index)` parity, null-on-throw parity) should be treated as hypotheses until the conformance fixtures run.
- Parity is asserted by a test that will only exist at the end of the fork (Group 4) while the genie catalog is already canonical from day one; between the two wishes shipping, `.claude/workflows/` scripts may pass genie CI and fail DSH parse (`\bfs\b`, `.constructor`, timers in prompt strings) with no signal in the genie repo.
- The `0.1.2-rc.1` floor is an rc series with no stable release; npm already shows `0.1.5-rc.2` and `0.1.6-alpha.1`, so the fork's peer range `^0.0.1-rc.2` must be rewritten and the `ctx.subagents`/`dynamicWorkflows` seam re-proven on a moving target, on a machine that currently runs `0.1.1-rc.2`.
- The fork's `run(wf,args)` wrapper plus a QuickJS restricted class means the same source runs under two different sandboxes; goldens captured with stub agents prove engine ordering and null semantics but say nothing about `schema` enforcement (native validates at the tool layer, DSH `outputSchema` may not retry), so a script relying on validated structured output may silently get malformed objects on DSH.
- Roadmap cards have no dependency edges, so the board can dispatch `dsh-workflow-fork` groups before the catalog they consume is merged.
- Static test only: `agent()`-null handling, `phase` grouping and schema shapes in catalog scripts are unverifiable in CI; `pm-ledger-verify.js` already carries an index-misattribution bug that proves the gap.
Required conditions:
- Record the native `/council` execution evidence with the invocation surface named (Workflow tool by name vs `scriptPath`, run id, transcript dir) and confirm which surface `/council` resolves to when a skill and a workflow share the name; if the skill wins, rename one before declaring the catalog canonical.
- Wire the six roadmap cards' `task_dependencies` to match the wishes' declared order (catalog groups 1→2, fork groups 1→2→3→4, and the fork's first card blocked by the catalog's last) before either wish is worked.
- Extend `scripts/workflows-meta.test.ts` (or a sibling) to mirror the DSH restricted-source policy tokens (`process`, `fs`, `fetch`, timers, `globalThis[`, `.constructor`, `__proto__`, `Function(`, `exec(`/`spawn(`) so a script that cannot parse on DSH fails genie CI; keep the list in one place the fork can import or copy with a parity test.
- Amend the fork wish's Summary and Group 2 deliverable 4: the clock ban exists upstream (`runtime.ts:243-255`); the work is aligning error messages with Claude Code's, and the `forbidden-clock` golden must be captured natively first to know the target string.
- Group 1 (fork bootstrap) must produce a recorded run of the upstream engine/runtime/catalog/plugin specs on npm `0.1.2-rc.1` packages with the peer range rewritten from `^0.0.1-rc.2`, and must state the tested host version; upgrade the dogfood host from `dsh 0.1.1-rc.2` to the floor before the integration run, or lower the floor with a recorded reason.
- Fix the `pm-ledger-verify.js` lens attribution (`filter(Boolean)` before indexing into `LENSES`) or record it as a known defect the `parallel-null-isolation` fixture must catch; do not let the first two catalog entries ship with a semantic bug the static test blesses.
- Add a `schema`-mismatch conformance fixture so the fork proves DSH `outputSchema` either validates-and-retries like native or the shim rejects the malformed object, since every genie catalog script depends on validated structured output.
Unknowns:
- Whether Claude Code's saved-workflow registry actually shadows `~/.claude/workflows/<name>.js` with `<repo>/.claude/workflows/<name>.js` (README asserts it; the native reference does not document scope precedence; only project-scope discovery is evidenced).
- Whether the `ctx.subagents` service and `subagentType` provider contract at DSH `0.1.2-rc.1` matches what `dsh_workflow@44b83c1` was written against (`0.0.1-rc.2`); all peer packages exist on npm at that version but no spec has been run against them.
- The exact native error strings for `Date.now()`, `Math.random()` and argless `new Date()` (needed for the `forbidden-clock` golden); the upstream strings differ in wording.
- How the fork will surface `args` given native takes a JSON value while DSH commands take a string (pm-ledger-verify already tolerates stringified args; council normalizes both), and whether `/workflow council {...}` on DSH parses the object or hands over a string.
- Whether DSH's `outputSchema` path validates and retries structured output the way the native `StructuredOutput` tool does, or returns the first response unvalidated.
- Why the two wishes flipped to APPROVED mid-assessment and whether the recorded native council run required by the catalog wish exists yet (no Review Results block was present when read).

### product
Verdict: support-with-conditions
Confidence: medium
Key evidence:
- The catalog already exists in practice: `.claude/workflows/pm-ledger-verify.js` is git-tracked (commits a11f785b2, 614219ee8) and is discovered natively by project scope — it appears in this session's slash listing with its `whenToUse` text. Declaring the directory canonical formalizes a working path rather than inventing one.
- The new `council.js` is path-free and honours the native contract: it starts with a pure-literal `export const meta`, takes everything through `args` (string or `{decision, constraints?, evidence?, unknowns?}`), filters null lenses, refuses to synthesize below three responders, and mirrors the five lenses in `skills/council/SKILL.md` (architecture, delivery, product, security, dissent). `bun test scripts/workflows-meta.test.ts` passes 6/6, and this very council run was produced by that script's lens prompt, so it executes natively.
- The failure the wish cites is real: `~/.claude/workflows/council.js` (Jul 10, 700 lines) stamps `LENS_ROOT = '/Users/feliperosa/.genie/plugins/genie'`; the root exists but `skills/architecture/SKILL.md` and `references/lenses/questioner.md` under it do not. It carries no `.genie-sync.json` sidecar, so `src/lib/legacy-integration-retirement.ts:660` classifies it 'ownership unprovable, left in place' — the README's manual-delete step is the only remedy, which matches the wish's Decision 4.
- Scope discipline holds: no install step, no root `workflows/` directory, no tarball packaging, and `pm-ledger-verify.js` untouched. The DSH fork wish stages conformance fixtures into a temporary project's `.claude/workflows/` (Decision 7), keeping the genie catalog free of fixtures as the constraint requires.
- The DSH adapter scope is grounded in a real catalog entry: `pm-ledger-verify.js:89` passes `effort: 'high'`, so the effort adapter is required to run an existing script, not a speculative one.
- `plugins/dsh-genie-board` (package `@automagik/genie-dsh-board`, `engines.dsh >=0.1.2-rc.1`, smoke via `scripts/dsh-genie-board-smoke.ts`) confirms genie already has a proven DSH 0.1.x delivery path, so a second DSH plugin is a familiar shape for maintainers, not a new platform.
- `biome.json:55` ignores `.claude`, so catalog scripts are never linted by the repo gate; the static test is the sole contract enforcement, exactly as the wish's risk table states.
Risks or objections:
- Name collision on `/council`: the `council` skill is installed at both `~/.claude/skills/council` and `~/.agents/skills/council`, and the session's slash listing shows `council` with the skill's description (not the workflow's `whenToUse`). The wish's QA criterion '`/council` appears in a Claude Code session opened in this repo' does not say which surface wins, and the two have already diverged (skill `agents/openai.yaml` default prompt names four perspectives; the workflow runs five). Users may invoke the prompt-driven skill believing they ran the deterministic workflow, or vice versa.
- Two sources of truth for the council roster: the lens briefs are inlined in `council.js` (Decision 2) and also prose in `skills/council/SKILL.md`. Nothing ties them together, so the next edit to either drifts silently — the same class of problem the 'no second catalog directory' constraint was meant to avoid, moved one level down.
- User value in this phase is limited to people working inside a genie checkout: the catalog is git-tracked but shipped through neither the skills.sh channel nor the release tarball (both explicitly OUT), so a genie user who installs via `npx skills add automagik-dev/genie` gets the `council` skill but never the `council` workflow. This is a deliberate deferral with a stated trigger, but it means 'one script runs on Claude Code or a DSH body' is true only for contributors until the install channel lands.
- The project-over-personal shadowing rule the README relies on is asserted by the README, not by the native `workflow-authoring` reference, which documents no `~/.claude/workflows` personal scope at all. If the native tool lists both or prefers user scope, the dead orphan is not shadowed and the README is wrong on the one point it uses to defer the delete.
- The DSH fork adds the personal root `~/.claude/workflows/` by design, so on any host still carrying the dead stamped orphan, `/workflow list` on DSH outside a genie checkout would surface a `council` that fails at lens resolution — the same orphan problem replicated onto a second body, with no `.genie-sync.json` sidecar for either body's retirement code to act on.
- The DSH wish's 'runs unmodified' promise is engine-level only; the QA criterion 'returns a report in the same shape as the native run' is a shape check, and the constraints already concede the worker model differs. Operators may read 'same script, both bodies' as output parity it does not deliver until the successor benchmark wish.
Required conditions:
- Resolve and document the `/council` name collision before marking the catalog wish done: state in `.claude/workflows/README.md` (and in the recorded native run) which surface `/council` invokes when both the `council` skill and `council.js` are present, or rename one of them. If the workflow is meant to be the canonical council, have `skills/council/SKILL.md` point at it so the skill no longer competes.
- Add a drift guard between `skills/council/SKILL.md` and `council.js`: at minimum, a test in `scripts/workflows-meta.test.ts` or a sibling asserting the five lens keys in `council.js` match the numbered lens list in the skill, so Decision 2's inlining does not become a second unsynchronized roster.
- Verify the project-over-personal shadowing claim empirically on this host (the orphan is present and the tracked file is not yet committed): record in the wish which `council` the native listing resolves to from a genie checkout and from a non-genie cwd, and correct the README if user scope is not shadowed.
- Record the one native `/council` run the wish promises under Review Results with its outcome, since it is the only runtime evidence CI cannot produce; the static test alone does not prove the script executes.
- Keep the deferred install-channel trigger ('a second repository needing genie workflows without a genie checkout') visible in `.genie/INDEX.md` alongside the pair, so the limited-reach state of the catalog is understood as a phase, not an oversight.
Unknowns:
- Which surface the native `/council` slash resolves to when the `council` skill and `.claude/workflows/council.js` coexist, and whether the native tool actually implements a `~/.claude/workflows` personal scope with project-first shadowing (the native reference is silent on both).
- Whether the DSH 0.1.x `ctx.subagents` seam is compatible with the fork's rc.2-era engine specs — the fork wish's Group 1 is the only planned probe, and it can end BLOCKED.
- Whether the private DSH snapshot (`link:../test-icetomoyo/*`) can be replaced by npm packages at `0.1.2-rc.1` without API drift; only `plugins/dsh-genie-board` (a Host plugin, not a workflow engine consumer) has been proven on that series.
- Whether the fork's discovery of `~/.claude/workflows/` will resurrect the dead stamped orphan on DSH hosts outside a genie checkout, and whether any retirement path will ever be able to remove a sidecar-less orphan.

### security
Verdict: support-with-conditions
Confidence: medium
Key evidence:
- Trust baseline already exists: the repo already tracks executable `.claude/` content that runs automatically in every session — `.claude/settings.json` wires `bash .claude/hooks/git-safety.sh` as a PreToolUse hook on every Bash call, and `.claude/workflows/pm-ledger-verify.js` has been git-tracked since commit a11f785b2. Tracking `council.js` widens no boundary that is not already open (verified via `git ls-files .claude/`).
- Native execution is gated: the `workflow-authoring` reference states `meta.description` is "shown in permission dialog", and the native script sandbox has "No filesystem or Node.js API access", so a catalog script's only reach is through subagents the user approves.
- `council.js` (/Users/feliperosa/workspace/repos/genie/.claude/workflows/council.js) is genuinely path-free and clock-free: no `/Users/`, `Date.now`, `Math.random`, `import` or `require` anywhere; the assess-only guarantee is prompt-level ("Do not edit files, change configuration, or execute the proposed plan") with no tool restriction option set. The orphan it supersedes, `~/.claude/workflows/council.js`, still stamps `const LENS_ROOT = '/Users/feliperosa/.genie/plugins/genie'` (line 23) — confirmed on this host.
- `scripts/workflows-meta.test.ts` passes (6/6) but is a hygiene lint, not a security boundary: it evaluates the `meta` block with `new Function(...)()` (executes contributor code in the test process rather than parsing a literal), does not check argless `new Date()` although README lists it as forbidden, and its token regexes are trivially bypassable (`globalThis['Date']['now']`, string concatenation of a home path). The forbidden-token scan runs only on the body after `meta`, never on `meta` itself.
- `biome.json` `files.ignore` includes `.claude`, so catalog scripts receive no lint; the wish accepts this ("Accepted" row in workflows-catalog Assumptions/Risks).
- The DSH fork wish (dsh-workflow-fork/WISH.md, Group 2 deliverable 1 and Decision 3) classifies every `.js` under a Claude root as the restricted `capability-generated` QuickJS class, never `trusted-local`, and keeps `approvalMode: generated-and-local` for interactive use — least-privilege by construction. The same wish, however, drops `disallowedTools` and `bashCommandClamp` "with one warning per option per run" (line 134), sets `scriptWallTimeoutMs` to `0` (disabled) and raises `maxAgents` to 1000 with the deployment ceiling raised to match (deliverables 2 and 5), and injects repo `AGENTS.md`/`CLAUDE.md` into every DSH subagent's system prompt (Group 3).
- `plugins/dsh-genie-board/README.md` documents an existing, deliberately narrow DSH trust model (fixed argv, `shell: false`, env allow-list, loopback + same-origin, no autonomous agent runner). The workflow fork is a much larger trust surface than that plugin, so the "proven on 0.1.2-rc.1" evidence transfers only to install/restart compatibility, not to the security posture of an agent runner.
Risks or objections:
- Guardrail options are silently weakened on the DSH body: if any catalog script ever relies on `disallowedTools` or `bashCommandClamp` to keep a lens read-only, the fork's drop-with-warning policy turns a hard restriction into nothing. A script that is assess-only on Claude Code could mutate the repo on DSH while claiming parity.
- Cost/DoS abuse surface on DSH: disabling the wall timeout and raising the agent ceiling to 1000 for the dialect removes DSH's two runaway backstops for a class of scripts that any PR contributor can edit and that DSH executes with real (paid) subagents. Claude Code has a shared token budget and user-visible kill; the wish OUT-scopes live `send`/`stop` from the contract and names no equivalent hard ceiling for DSH runs.
- Prompt-injection via tracked content is the real boundary, not code execution: the catalog scripts' prompts are the payload handed to subagents that hold full tool access (Read/Grep/Bash and, on Claude Code, Edit/Write). A malicious or careless PR changing a prompt string passes the static test unchanged; PR review is the only control. The DSH fork also injects `AGENTS.md`/`CLAUDE.md` from the checked-out repo into every subagent system prompt, extending the same exposure to a second body.
- The static test claims to enforce a "pure literal" `meta` but actually executes it with `new Function`; contributor-supplied code in the `meta` block runs on every `bun test` locally and in CI. Baseline exposure is unchanged (any test file already does this), but the README's enforcement claim is stronger than the mechanism, and a reviewer relying on it would be misled.
- `~/.claude/workflows/` as a DSH personal root is a user-writable, unreviewed directory that other agents and skill installers already write next to (release-publish.yml notes 55 `~/.claude/*` directories created by the skills channel). Any process running as the user can plant a workflow the DSH body will list; project-shadowing-personal also means an untrusted cloned repo can shadow a user's personal workflow by name on both bodies. Native behaviour already has this property; the fork doubles the executors reading it.
- `approvalMode: never` is required for conformance runs; if it leaks into a shared config file or default profile, every dialect script under both roots runs unapproved on DSH.
- Git-worktree isolation adapter creates worktrees under a run directory that `src/store.ts` prunes as runs; deleting worktree directories without `git worktree prune` leaves dangling `.git/worktrees/*` metadata in the operator's real repository, and a worktree created from a dirty or secret-bearing checkout copies nothing but does expose the repo to a subagent with write access.
- Supply chain of the fork: replacing a private DSH snapshot with npm `@deepseek-ai/*` at an rc series means the engine specs will be re-validated against a moving pre-release line; without an exact-version pin plus `--frozen-lockfile` and a recorded integrity hash, a later `rc` could silently change the `ctx.subagents` seam under the conformance goldens.
- Minor, out of this decision's scope but worth logging: `pm-ledger-verify.js` indexes `LENSES[i]` after `results.filter(Boolean)`, so when one lens returns null the remaining findings are attributed to the wrong lens — a correctness bug in an audit tool, not a security hole.
Required conditions:
- DSH fork: treat `disallowedTools`, `bashCommandClamp` and any future tool-restriction option as fail-closed — refuse to run the script (or require an explicit per-run override) instead of dropping with a warning. Parity that silently removes a guardrail is a security regression, not a compatibility shim.
- DSH fork: keep a finite default wall timeout and a per-run agent ceiling for the dialect in interactive profiles; allow `scriptWallTimeoutMs: 0` and the 1000-agent ceiling only in the conformance driver's profile. Document the DSH-side stop mechanism for a running dialect script in the fork README.
- Confine `approvalMode: never` to the conformance driver (`scripts/run-conformance.mjs`) via an explicit temp profile/config it creates and destroys; add a spec asserting the shipped default config keeps `generated-and-local` for both Claude roots.
- Catalog README: state plainly that the static test is a hygiene lint and that PR review of prompt text is the security control for catalog scripts; either make the test parse `meta` without executing it (regex/AST over a literal, or a JSON5-style parse) or drop the word "pure-literal" from the enforcement claim. Add the argless `new Date()` check the README already promises.
- Add a CODEOWNERS (or equivalent required-reviewer) entry for `.claude/workflows/**`, `.claude/settings.json` and `.claude/hooks/**`, since these files are executed or injected into sessions automatically and are not linted.
- Fork bootstrap (Group 1): pin exact `@deepseek-ai/*` versions (no `^`/`~`), commit the lockfile, run `pnpm install --frozen-lockfile` in CI, and record the resolved integrity hashes alongside `{npmSeries, version, testedAt}` in `compatibility.json`.
- Worktree adapter: dispose with `git worktree remove` followed by `git worktree prune`, never plain directory deletion, and cover it with the adapter spec; make the adapter opt-in per run when the host repo has a dirty working tree.
Unknowns:
- Whether the DSH 0.1.x `ctx.subagents` seam exposes a tool-restriction hook at all; if it does not, the fail-closed condition above can only be implemented as "refuse to run", which may block `pm-ledger-verify`-style scripts if they later adopt `disallowedTools`.
- Whether DSH's `capability-generated` approval dialog shows the script's prompt text or only the manifest (`name`/`description`); if only the manifest, the operator approves a prompt payload they never see, which is weaker than the Claude Code dialog implied by `workflow-authoring`.
- Whether the native Claude Code Workflow tool honours `disallowedTools`/`bashCommandClamp` for catalog scripts at all (the wish lists them as native options; the `workflow-authoring` reference loaded here does not document them), which determines how much of the guardrail-parity risk is theoretical.
- Whether DSH's run store prunes `.dsh/workflow-runs` on a schedule that could delete a live worktree directory mid-run.
- Which npm package names and exact versions the fork will pin (`@deepseek-ai/*` is named, the exact list is not), and whether those packages ship provenance/attestations that `pnpm` can verify.

### dissent
Verdict: oppose
Confidence: medium
Key evidence:
- Name collision the repo already ruled out: .genie/wishes/council-workflow/WISH.md Decision 8 (EXECUTED 2026-07-10) deleted skills/council/ because 'Skill-vs-workflow precedence for one name is undocumented — avoid the collision'. Today skills/council/SKILL.md exists again (restored b0e77cd15), .claude/workflows/council.js declares meta.name 'council', and ~/.claude/workflows/council.js (700 lines, meta.name 'council', LENS_ROOT stamped to /Users/feliperosa/.genie/plugins/genie) is still on disk. In this session's tool listing the tracked pm-ledger-verify workflow IS discovered, but 'council' resolves to the SKILL.md description, not council.js — the success criterion '/council returns five lens sections' is currently undecidable.
- The 'native project-scope rule' is not in the native contract: the loaded workflow-authoring reference documents session-persisted scriptPath and 'a saved workflow (same registry as {name})' only; no mention of <repo>/.claude/workflows/ or project-over-personal shadowing. The claim traces to one line in .genie/brainstorms/council-workflow/DESIGN.md:58 (July). The README's entire orphan mitigation ('the tracked file always wins') rests on that unverified line.
- DSH evidence is weaker than stated: this host runs @deepseek-ai/dsh@0.1.1-rc.2 (dsh --version), below the 0.1.2-rc.1 floor in plugins/dsh-genie-board/package.json and the fork wish; npm is already at 0.1.5-rc.2 / 0.1.6-alpha.1 with no stable release. plugins/dsh-genie-board/src/index.ts uses only ctx.workspaceRegistry, ctx.effect, ctx.webServer.register, ctx.connection.requestRejection — it proves nothing about the ctx.subagents / ctx.inject seams the fork depends on and the wish itself calls 'the one step that can end BLOCKED'.
- The static contract overclaims: .claude/workflows/README.md says the test enforces no filesystem/process access, no timers, no argless new Date(), and a pure-literal meta with no calls/spreads/interpolation; scripts/workflows-meta.test.ts checks only import/require/Date.now/Math.random/quoted absolute path and evaluates meta with new Function (so 'a'+'b' or String(x) pass). The retired lint:council-workflow (council-workflow WISH success criteria) banned new Date(, process., fs. — enforcement regressed.
- The orphan is genie-owned surface, not a manual chore: src/lib/legacy-integration-retirement.ts:579 classifyClaudeWorkflow already models ~/.claude/workflows/<TARGET_NAME>; without the ownership sidecar it classifies 'unmanaged' and is never removed. With the 'no install step' constraint, every repo other than genie keeps executing the dead-path stamped council on /council.
- The second catalog entry does not run on the DSH body today: .claude/workflows/pm-ledger-verify.js passes effort: 'high' (and uses throw, not the README's top-level return); the fork wish Simplicity Case admits 'the engine fails preflight on both [effort, worktree] today'. 'One script runs on both bodies' is false for half the catalog until fork Group 3 lands.
- Parity is stub-only by design (constraint 3) and the benchmark is deferred to a successor wish; the fork is medium appetite (groups scored 3/5/4/3) to run two scripts on a body whose value is unmeasured. Options with no DSH equivalent (agentType, disallowedTools, bashCommandClamp, isolation:'remote') are 'dropped with one warning' — that is divergent semantics under a shared path, not parity.
- 'No second catalog directory' holds only inside genie: the fork keeps DSH's own .dsh/workflows/ capsule root plus two Claude roots, and a DSH-root/Claude-root name collision becomes a 'listing error' (fork wish Group 2) — an operator-facing failure mode created by sharing the path.
- Process signal: .genie/wishes/dsh-workflow-fork/WISH.md flipped DRAFT→APPROVED during this council with both HIGH risks (private test-icetomoyo snapshot weld; 0.1.x vs 0.0.1-rc.2 seam drift) still open and unverified on this host.
Risks or objections:
- Three artifacts named 'council' (plugin skill, project workflow, stamped user workflow) with undocumented precedence: the recorded native run may exercise the wrong one, and the QA criterion '/council appears in a Claude Code session' cannot distinguish them.
- Canonicalizing a proprietary, unversioned harness contract (Claude Code workflow scripts: resume cache, budget, 16-slot cap, agentType registry) as the multi-body format binds the DSH executor to Anthropic's product semantics; the fork wish already concedes goldens depend on harnessVersion. Every native change becomes a silent fork regression.
- Pinning to a pre-release DSH line (0.1.x rc/alpha only, host already behind the declared floor) means the fork's green gate can rot between releases; there is no stable series to pin to.
- The stamped ~/.claude/workflows/council.js keeps winning outside the genie checkout, so the 'no install step' phase leaves a known-broken command on every other repo, with a manual delete as the only remedy despite genie owning a retirement surface for exactly this file.
- The README-vs-test gap lets a script with process./fs./setTimeout/argless new Date() or a non-literal meta pass CI, then fail at native runtime or diverge on DSH — the exact 'silent divergence' the conformance suite exists to prevent.
- Value is unproven: the DSH worker is a different model, parity is engine-level with stubs, and the benchmark is a successor wish. Medium appetite is spent before any evidence that a DSH council is useful.
- Shared-path discovery adds a DSH-only failure (DSH-root vs Claude-root name collision as a listing error) and drops native-only options with warnings — same file, different behavior, discoverable only at run time.
Required conditions:
- Resolve the 'council' name collision before recording the native run: either rename the workflow (e.g. council-workflow / lens-council) or delete/rename skills/council and the stamped ~/.claude/workflows/council.js, and record which surface /council actually invoked (transcriptDir/journal.jsonl) as the wish evidence.
- Prove the project-scope discovery and project-over-personal shadowing empirically in this repo (a session where both ~/.claude/workflows/<x>.js and <repo>/.claude/workflows/<x>.js exist, showing which runs) and cite that run in the README instead of the July DESIGN.md line.
- Make scripts/workflows-meta.test.ts enforce what the README claims, or shrink the README to what the test enforces: argless new Date(, process., fs., setTimeout/setInterval, and a real pure-literal meta check (AST or a JSON-shaped parse), restoring the retired lint:council-workflow floor.
- Route the orphan through the existing claude-workflow retirement surface (or a genie doctor warning) rather than a README manual step; the decision must not leave a dead-path /council on every non-genie repo.
- Gate the fork on Group 1 BLOCKED-or-green evidence on the actual DSH version the target host runs (this host: 0.1.1-rc.2; declared floor 0.1.2-rc.1) — reconcile the floor before APPROVED, not after.
- Do not call it 'one script runs on both' until pm-ledger-verify.js (effort: 'high') runs on the fork; until Group 3 lands, document that only clock-free, option-free scripts are portable.
- Define a minimal benchmark trigger inside this decision (even one seeded council run compared by shape and by a scorer) so the medium-appetite fork has a kill criterion, rather than deferring all value measurement to a successor wish.
Unknowns:
- Which surface Claude Code invokes for /council when a plugin skill, a project workflow and a user workflow share the name — undocumented, and this session shows the skill, not the workflow.
- Whether the native tool documents project-scope .claude/workflows/ discovery and shadowing anywhere authoritative; the loaded workflow-authoring reference does not.
- Whether the DSH 0.1.x ctx.subagents / ctx.inject seams match the fork's rc.2-era engine specs (stated unknown; dsh-genie-board exercises neither).
- Whether @deepseek-ai/* npm packages at 0.1.2-rc.1 (or the 0.1.5/0.1.6 line) can replace the private test-icetomoyo snapshot without API drift (stated unknown).
- Whether the stamped ~/.claude/workflows/council.js carries the genie ownership sidecar (managed-clean → removable by genie update) or classifies unmanaged (never removed); not checked here.
- Whether the stub-agent goldens survive a Claude Code harness upgrade — the fork wish records harnessVersion in provenance but has no policy for a golden that no longer reproduces.

## Synthesis
Decision: proceed-with-conditions
Consensus: All five lenses agree on the facts: (1) the catalog half is already real and cheap — `.claude/workflows/pm-ledger-verify.js` is git-tracked and natively discovered from project scope, `council.js` is genuinely path-free and clock-free, `scripts/workflows-meta.test.ts` passes 6/6, `biome.json` ignores `.claude`, and rollback is a `git rm` of three files. (2) The orphan `~/.claude/workflows/council.js` stamps a dead `LENS_ROOT`, has no ownership sidecar, classifies `unmanaged` in `legacy-integration-retirement.ts`, and the README's manual delete is currently the only remedy. (3) There is a live `council` name collision: `skills/council/SKILL.md`, `.claude/workflows/council.js` (`meta.name: 'council'`) and the orphan all share the name; this session's slash listing resolves `council` to the skill, so the catalog wish's own `/council` success criterion is undecidable today. (4) The README's project-over-personal shadowing rule is asserted, not documented in the native `workflow-authoring` reference, and unproven on this host. (5) The static test enforces less than the README claims (no argless `new Date(`, no `process.`/`fs.`/timers, `meta` evaluated via `new Function` rather than parsed as a literal) and is narrower than DSH's restricted-source policy, so a script can pass genie CI and fail DSH parse. (6) The DSH floor is not reproducible here: host runs `0.1.1-rc.2`, floor is `>=0.1.2-rc.1`, npm is at `0.1.5-rc.2`/`0.1.6-alpha.1` with no stable series, and `dsh-genie-board` proves install/restart compatibility only — never the `ctx.subagents`/`ctx.inject` seams the fork depends on. (7) The fork wish contains verified errors: the upstream already bans the clock (`runtime.ts:243-255`), and `@dsh-external/workflow` is not on npm (404). (8) `pm-ledger-verify.js` has a latent lens-misattribution bug (`filter(Boolean)` before `LENSES[i]`) that the static gate cannot see. (9) Roadmap cards for both wishes carry no dependency edges despite declared group ordering. (10) Both wishes flipped DRAFT→APPROVED during the council with HIGH risks open.
Dissent:
- **dissent:** Oppose. Canonicalizing Claude Code's proprietary, unversioned workflow contract (resume cache, budget, 16-slot cap, agentType registry) as the multi-body format binds a DSH executor to Anthropic product semantics; every native change becomes a silent fork regression, and goldens already depend on harnessVersion with no policy for a golden that stops reproducing. The fork spends medium appetite (groups scored 3/5/4/3) to run two scripts on a body whose value is unmeasured — parity is stub-only by design, the benchmark is deferred to a successor wish, and there is no kill criterion. 'One script runs on both bodies' is false for half the catalog until fork Group 3 lands (pm-ledger-verify passes effort:'high', which fails DSH preflight). Options with no DSH equivalent dropped with a warning are divergent semantics under a shared path, not parity, and the DSH-root vs Claude-root name collision as a 'listing error' is a new operator-facing failure created by sharing the path. The orphan is genie-owned surface (classifyClaudeWorkflow already models the path) and should route through retirement or a doctor warning, not a README chore; with no install step every non-genie repo keeps a dead-path /council. The repo already ruled out this exact collision in the council-workflow wish (Decision 8, EXECUTED 2026-07-10) and has now recreated it.
- **architecture:** The fork target may be the wrong seam: DSH 0.1.1-rc.2 ships a first-party `@deepseek-ai/dsh-workflow` engine (`ctx.workflowEngine`, `WorkflowStartRequest{script, meta, args}`) with the same agent/pipeline/parallel/phase/log vocabulary. A thin loader over that seam (read `.claude/workflows/<name>.js`, split `export const meta`, submit) would need no QuickJS, no `run(wf,args)` transform and no snapshot detachment. Two engines on one host with the same words but different meta transport and option policies is a drift trap. Also: the canonical multi-body catalog living under a Claude-branded path is a permanent naming coupling to one vendor's convention.
- **security:** The fork's guardrail policy is a security regression, not a compatibility shim: dropping `disallowedTools`/`bashCommandClamp` with a warning turns a hard read-only restriction into nothing, so an assess-only script on Claude Code could mutate the repo on DSH while claiming parity. Disabling the wall timeout and raising the agent ceiling to 1000 removes DSH's runaway backstops for scripts any PR contributor can edit and DSH runs with paid subagents. The real boundary is prompt-injection via tracked prompt text handed to full-tool-access subagents (extended to a second body by injecting AGENTS.md/CLAUDE.md), and PR review is the only control — the static test is a hygiene lint whose 'pure-literal meta' claim is stronger than its mechanism.
- **product:** User value in this phase reaches only contributors inside a genie checkout: the catalog ships through neither the skills.sh channel nor the tarball, so a user who installs via `npx skills add automagik-dev/genie` gets the `council` skill but never the `council` workflow. The lens roster is now duplicated (inlined in council.js and prose in SKILL.md) with nothing tying them together — the same class of drift the 'no second catalog' constraint was meant to prevent, moved one level down. The DSH fork's discovery of `~/.claude/workflows/` replicates the dead orphan onto a second body outside genie checkouts.
- **delivery:** Delivery order inside the catalog wish is inverted for testability (Group 2's static test should have landed first with pm-ledger-verify as the passing fixture; Group 1's `bun -e` validation is a weaker copy of it). Native `schema` validation vs DSH `outputSchema` is an unproven parity: goldens with stub agents say nothing about validate-and-retry, so a script relying on validated structured output may silently get malformed objects on DSH. The fork's analysis is presented as fully verified but contains material errors, so its other unverified-here parity claims should be treated as hypotheses until fixtures run.
Conditions:
- Resolve the `council` name collision before the catalog wish is marked done: rename one surface (workflow or skill) or make `skills/council/SKILL.md` delegate to the workflow, and add a disjointness assertion (skill `name:` set vs `.claude/workflows/*.js` `meta.name` set) to `scripts/workflows-meta.test.ts` or `scripts/skills-lint.ts`. Note the repo already executed this exact avoidance in the council-workflow wish (Decision 8, 2026-07-10).
- Prove project-scope discovery and project-over-personal shadowing empirically, not from the July DESIGN.md line: run a native session in this repo with the orphan still present, record which `council` surface ran (invocation surface, run id, transcript dir, `meta.description` text observed), and repeat from a non-genie cwd; correct the README if user scope is not shadowed.
- Record the one native `/council` run the catalog wish promises under Review Results, with the invocation surface named; the static test alone does not prove execution.
- Make `scripts/workflows-meta.test.ts` enforce what the README claims or shrink the README to what the test enforces: add argless `new Date(`, `process.`, `fs.`, timers, and mirror the DSH restricted-source token list (`fetch`, `globalThis[`, `.constructor`, `__proto__`, `Function(`, `exec(`/`spawn(`) so a script that cannot parse on DSH fails genie CI; parse `meta` as a literal (AST/JSON-shaped) instead of `new Function`, or drop the 'pure-literal' enforcement claim; scan `meta` as well as the body. State in the README that the test is a hygiene lint and PR review of prompt text is the security control.
- Fix the `pm-ledger-verify.js` lens attribution bug (`filter(Boolean)` before `LENSES[i]`) or record it as a known defect the fork's `parallel-null-isolation` fixture must catch; do not ship the first two catalog entries with a semantic bug the static gate blesses.
- Wire the six roadmap cards' `task_dependencies` to the declared order (catalog 1→2, fork 1→2→3→4, fork's first card blocked by the catalog's last) before either wish is worked; today `genie task checkout` would offer `conformance` before `fork-bootstrap`.
- Before fork Group 1 starts, record in the `dsh-workflow-fork` wish a written comparison between forking `omdsh-dev/dsh_workflow` (QuickJS, `run(wf,args)`, private snapshot weld) and a thin loader over DSH's first-party `ctx.workflowEngine` seam (`WorkflowStartRequest{script, meta, args}`), stating which Claude Code hooks the first-party engine lacks (`budget`, `workflow()`, `effort`, `isolation`, `export const meta`) and why adding them in a fork is cheaper than over the seam; also confirm whether third-party plugins may `ctx.inject(['workflowEngine'])`.
- Reconcile the DSH floor with reality before the fork's APPROVED state stands: record `dsh --version` on the actual target host (today `0.1.1-rc.2` vs floor `0.1.2-rc.1`), either upgrade and re-run `scripts/dsh-genie-board-smoke.ts` there or lower the floor with a recorded reason; pin exact `@deepseek-ai/*` versions (no `^`/`~`), commit the lockfile, `pnpm install --frozen-lockfile` in CI, and record the exact tested version plus integrity hashes in `compatibility.json`.
- Amend the fork wish's factual errors: the upstream already bans the clock (`runtime.ts:243-255`), so Group 2 deliverable 4 is error-message alignment (capture the native `forbidden-clock` golden first); `@dsh-external/workflow` is git-only (npm 404), so 'replace the private snapshot with npm packages' scopes to the `@deepseek-ai/*` devDependencies only, and Group 1's `--frozen-lockfile` acceptance needs a regenerated lockfile.
- Make the unknown-option policy explicit per option and fail-closed for anything that changes correctness or restricts tools (`isolation`, `disallowedTools`, `bashCommandClamp`): refuse to run or require an explicit per-run override, matching DSH's own 'misused hooks always kill the script' rule; only cosmetic options may be dropped with a warning. Add an `unknown-option` golden fixture.
- Fork Group 1 must assert the chosen `ctx.subagents` provider reports `SubagentCapabilities.outputSchema === true` and `depthLimit === true` on the pinned DSH version (every catalog agent call passes `schema`), and add a `schema`-mismatch conformance fixture proving DSH `outputSchema` validates-and-retries like native or the shim rejects malformed output.
- Keep finite defaults on DSH: a default wall timeout and per-run agent ceiling in interactive profiles; allow `scriptWallTimeoutMs: 0` and the 1000-agent ceiling only in the conformance driver's profile; confine `approvalMode: never` to a temp profile the conformance driver creates and destroys, with a spec asserting the shipped default stays `generated-and-local` for both Claude roots; document the DSH-side stop mechanism.
- Worktree isolation adapter disposes via `git worktree remove` + `git worktree prune`, never plain directory deletion, covered by the adapter spec; opt-in per run when the host repo is dirty.
- Add a CODEOWNERS (or required-reviewer) entry for `.claude/workflows/**`, `.claude/settings.json` and `.claude/hooks/**`, since these execute or inject into sessions automatically and are not linted.
- Address the orphan beyond a README sentence: either route `~/.claude/workflows/council.js` through the existing `classifyClaudeWorkflow` retirement surface (with a documented ownership proof) or add a `genie doctor` warning naming it, so non-genie repos do not keep a dead-path `/council` indefinitely.
- Add a drift guard tying the five lens keys in `council.js` to the numbered lens list in `skills/council/SKILL.md` (or remove one roster), so Decision 2's inlining does not create a second unsynchronized source of truth.
- Do not describe the result as 'one script runs on both bodies' until `pm-ledger-verify.js` (effort:'high') runs on the fork; until fork Group 3 lands, document that only clock-free, option-free scripts are portable.
- Define a minimal benchmark/kill criterion inside the fork wish (e.g. one seeded council run compared by shape and by a scorer) rather than deferring all value measurement to a successor wish; if Group 1 ends BLOCKED, the fork stops and the catalog stands alone.
Evidence gaps:
- Which surface Claude Code invokes for `/council` when a plugin skill, a project workflow and a user workflow share the name — undocumented; this session shows the skill.
- Whether the native Workflow tool implements a `~/.claude/workflows` personal scope with project-first shadowing at all; the loaded `workflow-authoring` reference documents neither, and the only evidence is project-scope discovery of `pm-ledger-verify`.
- Whether DSH 0.1.x `ctx.subagents` (`SubagentStartRequest`/`SubagentCapabilities`) and `ctx.inject` seams match the fork's rc.2-era engine specs; only 0.1.1-rc.2 typings were inspectable, no rc.2 baseline exists, and the private `dsh2026/test-icetomoyo` snapshot cannot be resolved.
- Whether third-party DSH plugins may `ctx.inject(['workflowEngine'])` (`runtime-types.d.ts` calls the handles host-only; `dsh-genie-board` injects other host services).
- Whether DSH's `outputSchema` path validates-and-retries like the native `StructuredOutput` tool or returns the first response unvalidated.
- Whether the DSH 0.1.x subagent seam exposes any tool-restriction hook; if not, fail-closed for `disallowedTools` can only mean refuse-to-run.
- Whether DSH's `capability-generated` approval dialog shows the script's prompt text or only the manifest.
- Whether the native Workflow tool honours `disallowedTools`/`bashCommandClamp` for catalog scripts (the wish lists them; the reference does not document them).
- Exact native error strings for `Date.now()`, `Math.random()` and argless `new Date()` needed for the `forbidden-clock` golden.
- How `args` cross bodies: native takes a JSON value, DSH commands take a string; whether `/workflow council {...}` on DSH parses the object.
- npm dist-tag resolution for `@deepseek-ai/*` (`npm view @deepseek-ai/dsh-workflow version` reports `0.0.1-rc.1` while the host runs `0.1.1-rc.2`), and whether those packages ship verifiable provenance.
- Whether stub-agent goldens survive a Claude Code harness upgrade; provenance records harnessVersion but no policy exists for a golden that stops reproducing.
- Whether DSH's run store prunes `.dsh/workflow-runs` on a schedule that could delete a live worktree mid-run.
- Why both wishes flipped to APPROVED mid-assessment and whether the recorded native council run required by the catalog wish exists yet.
Next action: Run one native Claude Code session in this repo, with the orphan `~/.claude/workflows/council.js` still present and `skills/council` installed, invoke `/council` and a same-named user/project workflow pair, and record in `.genie/wishes/workflows-catalog/WISH.md` Review Results which surface actually ran (invocation surface, run id, transcript dir, observed `meta.description`) — this single probe decides the name-collision condition, the README's shadowing claim, and the catalog wish's own success criterion before any fork work begins.

Conflict resolution: Four lenses (architecture, delivery, product, security) returned support-with-conditions; dissent returned oppose. The decision was split along the proposal's own seam. For the catalog half, the dissent's strongest objections — the `council` name collision, the unverified shadowing rule, and the README-vs-test enforcement gap — were independently corroborated by every majority lens, so they were promoted from dissent to hard conditions rather than treated as a minority view; the majority's 'cheap and already real' evidence (tracked `pm-ledger-verify`, 6/6 test, trivial rollback) justified proceed rather than stop. For the fork half, the majority's own evidence (host below the declared floor, fork wish with verified-false claims, `dsh-genie-board` proving none of the required seams, first-party DSH engine never evaluated, wishes flipped APPROVED with HIGH risks open) is closer to the dissent's position than to unconditional support, so the conditions gate the fork on Group 1 evidence, a written seam comparison, and a floor reconciliation before its APPROVED state stands, and require a kill criterion — a partial adoption of the dissent's 'value unproven' objection. Where architecture and security disagreed with the fork wish on unknown-option policy (drop-with-warning vs fail-loud), both lenses and DSH's own documented policy align on fail-closed for correctness/guardrail options, so that was adopted as a condition. Dissent positions that rest on judgment rather than contested fact — vendor-contract coupling, that the orphan must be genie-owned retirement rather than a README step, that medium appetite is premature — are preserved as attributed dissent; the orphan point was softened into a condition (retirement route or doctor warning) because the majority agrees the README-only remedy is weak. Product's install-reach and roster-duplication concerns and delivery's schema-parity gap were not disputed by any lens and became conditions. No lens's finding was dropped because others disagreed; where evidence was insufficient (native precedence, DSH seams, shadowing) it is listed as a gap rather than resolved by vote.

