# skill-audit-sweep — first live run (2026-09-16)

Run `wf_dd37d1db-bf6` of `.claude/workflows/skill-audit-sweep.js` (Opus pinned): 6 agents (1 signals, 4 characterizer shards, 1 judge), 361,002 subagent tokens, 37 tool uses, 358 s wall clock, `ok: true`, 4/4 shards, 20/20 skills judged, `notConvened` empty, `unjudged` empty. Assess-only: nothing was mutated. The verdicts below are proposals; deleting or merging a skill is a caller decision taken after this report.

Measured bill for the council condition (instrument the first conversion): the by-hand skill-audit reads twenty SKILL.md files (~62 KB) plus lint/parity/doctor output into the orchestrator; this run kept all of that in six Opus agents and returned a ~75 KB report. Per-agent cost including CLAUDE.md/AGENTS.md injection averaged ~60k tokens.

Unjudged: []; notConvened: []; reasons rejected: 0

---
# Skill audit sweep

- Focus: First live run of the converted sweep: audit the twenty shipped skills after the 2026-09-15 category/mutates stamping, the five absorbed skills, and the new workfly and skill-audit front doors.
- Timestamp: 2026-09-16T00:30:00Z
- Search pass: use — no new skill is being proposed; the sweep is the dogfood of the skill-audit-sweep conversion
- Roster: 20 skill(s) from inventory
- Shards: 4/4 responded over 20 skill(s)

## Signals

- bun scripts/skills-lint.ts — exit 0, 0 attributed finding(s), 0 unattributable
- bun scripts/skills-inventory-parity.ts — exit 1, catalog block current, 0 missing, 20 extra
- doctor skills lines — exit 0, recorded ref v5.260915.6 (stale against this binary)

Inventory parity: on disk but never published:
- authoring
- brainstorm
- council
- docs
- dream
- fix
- genie
- genie-hacks
- merge
- omni
- quick
- refine
- report
- research
- review
- skill-audit
- verify
- wish
- work
- workfly

Doctor skills lines:
- skills: claude — 14/14 @ v5.260915.6 (stale, binary is v5.260915.8)
- skills: agents — 14/14 @ v5.260915.6 (stale, binary is v5.260915.8)
- skills: goose — 14/14 @ v5.260915.6 (stale, binary is v5.260915.8)
- skills: windsurf — 14/14 @ v5.260915.6 (stale, binary is v5.260915.8)
- skills: agent dirs — 57/57 recorded homes complete @ v5.260915.6

Recorded homes:
- claude: 14/14
- agents: 14/14
- goose: 14/14
- windsurf: 14/14

Summary: Lint is clean: exit 0, 60 files scanned, zero missing commands, resource, metadata, banned-token or mutates violations, so the 2026-09-15 category/mutates stamping holds across the tree. Parity exits 1 for one upstream reason — the skills CLI --list named no skills at all (parser drift or an upstream CLI failure) — so all twenty on-disk directories, workfly and skill-audit included, are reported as unpublished extras; treat that as a single tooling signal, not twenty per-skill defects. Doctor exits 0 but the install record is stale: all four known homes read 14/14 @ v5.260915.6 against a v5.260915.8 binary, with 57/57 recorded agent dirs complete and no retirement warnings.

## Verdicts
| Skill | Verdict | Reason | Lines |
|---|---|---|---|
| `authoring` | Update | Lines 16-25 assert that frontmatter 'carries exactly four keys, in order, and nothing else' with all four mandatory, but the 2026-09-15 stamping makes category and mutates optional, so a legal two-key skill reads as a violation of the one document that defines the contract; line 14 asserts a shipped shape (SKILL.md + agents/openai.yaml + references/*.md) for a delivery path this skill is not currently part of, and line 10 defers the pre-write search in prose without naming the workflow front door council and workfly now route through. The catalogue search is covered by skill-audit and the lint gate at lines 57-59 is covered by review's independent-verification contract; nothing else states the frontmatter contract, so the facts must be refreshed here rather than moved. | lines 10, 14, 16-25 |
| `brainstorm` | Update | Line 48 still carries an idempotent reconcile of a legacy single-file .genie/brainstorm.md that the repository moved past before the roadmap-board model, and lines 46-55 describe the Raw/Simmering/Ready/Poured index as authoritative when placement truth is now the roadmap card's lane and INDEX.md is only lint-checked against it by doctor's 'jar: index-lane drift' check; line 26 treats .genie/brainstorms/<slug>/ as a mere draft location rather than the point where the lifecycle slug becomes card identity. Doctor covers the drift check and the board covers placement; classifying an ambiguous request and driving the decision tree to a reviewed DESIGN.md is brainstorm's own and stands. | lines 26, 46-55 (migration clause at 48) |
| `council` | Merge into review | Line 12 anchors the whole skill to '.claude/workflows/council.js in the genie repository's canonical workflow catalog' plus 'run the native Workflow tool with the saved name council' — a host-root path into another repository that an installed skill cannot resolve, and a single vendor runtime named inside a document the voice rules require to stay runtime-neutral; that same line makes the body an admitted duplicate surface of an asset packaging does not publish, leaving neither door delivered. What remains (lines 14-53) is a fixed-shape, assess-only, multi-lens verdict with evidence, which is exactly review's contract and its lens table at review lines 42-50, so review covers the same need with a delivered entrypoint. — carry across: The four fixed lenses (technical, product, risk, dissent) as a named multi-lens mode inside review rather than a second document; the fixed-shape per-lens verdict block; the synthesize-a-decision-without-mutating rule; brainstorm line 24's escalation path repointed at review's multi-lens mode; and a retired-name row in genie's route table reading council -> review (multi-lens mode). | lines 12-53 (front-door pointer at line 12; lens protocol 14-53) |
| `docs` | Update | Lines 20-27 keep the Surfaces table generic ('in-repo, a submodule, or a separate site') so an auditor cannot tell which case applies, while this repository's docs path is a fixed symlink into the .docs-vendor submodule with a documented commit-PR-then-bump workflow; line 23 presents AGENTS.md/CLAUDE.md overlay maintenance as an open question after the repository settled AGENTS.md as canonical and CLAUDE.md as the Claude-only overlay; line 24 names ARCHITECTURE.md as a surface although architecture content now lives under docs/_internal/ and in CLAUDE.md's Architecture section. The report shape at lines 41-43 restates review's assessment contract and should point there; the docs-vs-product drift audit and the contributor test are docs' own. | lines 20-27 (surfaces table), 23, 24 |
| `dream` | Improve | Lines 29-36 restate work's dispatch, claim, review and completion protocol for the batch case, line 56 restates the repair budget as 'per-group budget B (default 2)' where the contract is to resolve budgets.maxEscalationsPerGroup from config (and the shipped default is not 2), line 18 re-implements the INDEX-vs-placement drift gate that doctor's 'jar: index-lane drift' check now owns, lines 14 and 32 carry the standalone/Orca branch inline in every execution paragraph although orchestration.mode is configured once and never inferred, and line 51 pins 'genie board --wish <slug>' as a cached command surface. Work covers per-wish execution, fix covers the budget, doctor covers the drift check, setup covers the mode; what only dream owns is wish selection, cross-wish dependency ordering into DREAM.md and the wake-up report, and the rewrite should leave those standing alone. — Execute and review — lines 14, 18, 29-36, 51, 56 → about 55 lines, down from 83 | lines 14, 18, 29-36, 51, 56 |
| `fix` | Update | Line 31 fixes the ceiling at 'at most two escalation attempts per group' in prose while line 14 of the same file deliberately resolves it with genie config get budgets.maxEscalationsPerGroup — the document contradicts itself and silently drifts from any host that configured 3; lines 41-43 hard-code the genie task comment <task-id> --worker orchestrator handoff, which exists only in standalone mode and has no local card under Orca orchestration, without saying so. Config covers the number and orchestration.mode covers the branch; the bounded diagnose-dispatch-rereview loop with attempt accounting is fix's alone, and every other skill that names a budget should point back at it. | lines 14, 31, 41-43 |
| `genie` | Update | Line 31 routes skill authoring to a skill named 'authoring' while the delivered catalogue advertises it as skill-management, so a reader following the row by name finds nothing; line 59's operations guidance still presents genie task / genie board as the standalone pair with no mention of genie task sync or the git-tracked .genie/roadmap.json snapshot that is now the canonical board. Work covers the APPROVED/IN_PROGRESS entry conditions the Resume table at lines 46-55 restates and skill-audit covers corpus stocktaking that line 32 hands off; the front-door routing decision itself is genie's alone, which is why this sweep's two merges must land here as retired-name rows — council -> review (multi-lens mode) and research -> report (question mode). | lines 31, 59 (route table around 29-34, Resume table 46-55) |
| `genie-hacks` | Update | Line 34 instructs contributors to 'open the PR against dev, never main', while the repository's documented docs-submodule workflow branches inside .docs-vendor and creates the PR with --base main, so following the skill produces a PR against the wrong base; line 16 cites the published page as https://docs.automagik.dev/genie/hacks where the public Mintlify site is automagik.dev/genie/...; line 43 keeps a v4-era compatibility clause for a CLI whose v4 cleanup is itself a retirement path. The docs skill owns the automagik-dev/docs PR workflow these lines restate and should be the pointer; the list/search/show front door over references/catalog.md is genie-hacks' own. | lines 16, 34, 43 |
| `merge` | Update | Line 48 names bun run check as the whole gate and calls a passing merged tree proven, but the repository's gate now also chains skills:lint, wishes:lint and lint:complexity-budget, so check alone is no longer the proof this section claims; line 70's provenance comment pins an upstream commit and a third-party mirror path that nothing re-verifies after the body was rewritten. Verify owns 'which single command proves the claim' and should be the pointer for the gate; reading both sides' intent across every conflict hunk and finishing the in-progress merge or rebase is merge's alone and no other skill covers it. | lines 48, 70 |
| `omni` | Update | Lines 43-49 restate the OMNI_* configuration table inline and have already drifted from the canonical environment-variable reference — OMNI_APPROVE_TOKENS and OMNI_DENY_TOKENS are missing — so the copy is now actively wrong rather than merely duplicated; line 51 documents the compatibility default agent: claude as live behaviour while calling it a silent mis-route, with no retirement marker or removal condition. The central env-var list covers the table and should be linked instead of copied; the four-phase handshake, instance bind, chat routes, then serve-and-verify-a-real-round-trip walk is omni's own, and the plugin bundle's omni-setup/omni-ops sit in a different authority domain. | lines 43-49, 51 |
| `quick` | Update | Lines 10 and 40 make the completion oracle a read-back from a deployed dev environment serving a revision, but this repository's dev channel is a GitHub Release/tag pipeline (version.yml -> release.yml) with no deployed service, so the gate has no referent here; frontmatter line 5 declares mutates: repo while Deliver (lines 35-40) pushes a remote branch, opens and merges a PR and reads dev — an external write; line 22's 'never requests or manufactures authority' and line 36's 'never bypass or weaken checks' describe the refusal path more softly than release-guard now enforces (a human dev dispatch is rejected outright), and line 28's 'the runtime's native delegation surface' names nothing concrete after the MCP and ui-bridge retirements. Work covers the full lifecycle and verify covers the proof command; the admitted 60-minute bypass for an already-decided tiny change is quick's own and stands once the oracle and the mutates key tell the truth. | lines 5 (frontmatter), 10, 22, 28, 36, 40 |
| `refine` | Update | Line 24's 'Checked: 2026-09-15' is the entire freshness contract for two vendor URLs, two model baselines and two bundled guides, and it is already behind this sweep's 2026-09-16 caller timestamp; lines 18-19 pin 'GPT-6 Astra' and 'Claude Fable 5.1' plus platform.claude.com / developers.openai.com in a table the file itself calls drift-prone; line 45 writes text-mode output to /tmp/prompts/, which research line 35 explicitly rejects as a destination ('notes written there are lost before anyone reads them'), so the catalogue currently ships two contradicting placement rules. The investigation family owns the output-placement rule and should set it; routing a prompt to provider guidance and validating the rewritten body back into place is refine's own. | lines 18-19, 21-27 (Checked line at 24), 45 |
| `report` | Improve | Line 40 hard-codes 'three repair attempts' and pre-empts the escalation ceiling where fix resolves budgets.maxEscalationsPerGroup from config and never names it in prose; line 49 offers genie task create --title "bug: ..." with no --agent/--why pair, which the board now requires as a paired declaration; line 23 dispatches a read-only 'scout' through 'the runtime's native delegation surface', a role name outside the shipped roster (claude\|codex\|pi\|hermes\|prime). Fix covers the budget and the CLI covers the assignment pair; report survives as the catalogue's single investigation entrypoint, so the same rewrite must fold research's question-scoped half into the finding block at lines 29-36 instead of leaving two near-identical evidence formats in the tree. — Investigate and hand off — lines 23, 29-36, 40, 49 — plus a new question-scoped mode absorbed from research → about 70 lines, up from 54, after absorbing research's 53 as a mode rather than a second document | lines 23, 29-36, 40, 49 |
| `research` | Merge into report | Lines 39-45 carry a finding block that is a near-sibling of report's lines 29-36, wrapping the same read-only investigate-cite-hand-over activity and differing only in whether the subject is a failure or a question — and the catalogue README's precedent already collapsed investigation into one report entrypoint. Line 35 tells the author to match 'the convention already in place' while naming no path, although placement is now the fixed .genie/brainstorms/<slug>/ thread keyed by tasks.wish; line 53 pins an upstream attribution commit that nothing re-verifies; line 14 still frames delegation as runtime-optional after the MCP retirement removed one of the surfaces it implied. Report covers the same activity with a live citation contract and a delivered front door. — carry across: The question-scoped entry (an open question, not a failure, opens the investigation); delegate-the-reading-by-source; the cite-every-finding-to-a-URL-or-path:line rule; the rejection of /tmp as a note destination (line 35) stated once for the whole catalogue so refine line 45 can point at it; note placement into .genie/brainstorms/<slug>/ keyed by tasks.wish instead of 'the convention already in place'; and a retired-name row in genie's route table reading research -> report (question mode). | lines 14, 35, 39-45, 53 |
| `review` | Improve | Line 77 restates fix's budget as 'preserving its budget B (default 2)' where the contract is to resolve budgets.maxEscalationsPerGroup and never name the constant in prose; line 10 pins the card-comment command with --worker orchestrator without the 4000-byte, no-control-character bound the CLI now enforces; lines 79-81 describe Orca dispatch through 'Orca's current worker protocol' rather than the closed orca orchestration ... --json boundary that src/lib/orca-orchestration-adapter.ts owns, and predate orchestration mode being explicit and never inferred; line 28 requires a design-evidence helper for the reviewed-sha256 that is named but located nowhere and ships nowhere under skills/review/. Fix covers the budget and the adapter covers the Orca boundary; independent assess-only verdict with evidence is review's own, which is why council merges here and why this rewrite must land the four fixed lenses as a mode beside the existing lens table. — Lens table and Handoff — lines 28, 42-50, 77-81 — plus a council-derived multi-lens mode → about 90 lines, up from 82, with council's 53 absorbed as a mode rather than copied | lines 10, 28, 42-50, 77-81 |
| `skill-audit` | Keep | The two residual defects are narrow and did not bite this run: lines 40-42's 'Without a workflow surface' fallback restates the four-stage roster and shard/quorum shape in prose that scripts/skill-audit-workflow-parity.test.ts does not pin (it pins only the path and saved-name strings), and line 42's 'three or four shards' stands in for the caller-supplied shardCount already declared at line 12 — this run dispatched four shards of four, inside the stated range. Nothing else covers catalogue-scoped stocktaking: review assesses one artifact, genie routes one live request, workfly authors a workflow, authoring writes a single skill. The first live run produced a complete twenty-row verdict set with cross-shard consolidation, so the front door stands as written. | lines 12, 26-38, 40-42 |
| `verify` | Update | Line 33 hard-codes bun run check as the repository-green proof and line 34's 'Types are clean' row points only at 'the typecheck stage inside that gate', but the gate in package.json now chains typecheck, lint, dead-code, skills:lint, wishes:lint, lint:complexity-budget, lint:orca-bundle and tests, so the table under-specifies what a green claim requires and every skill pointing here inherits the gap. The package.json script is the live list and should be read rather than cached in prose, and review owns the SHIP/FIX-FIRST/BLOCKED vocabulary that lines 43-47 relay; the pre-claim gate itself — one command, run fresh, exit code read, nothing mutated — is verify's alone and is the anchor work, quick, merge and fix all point at. | lines 33-34 (What proves what table), 43-47 |
| `wish` | Update | Line 59 guards the linter with grep -q '"wishes:lint"' package.json and line 62 treats an unavailable project linter as merely reportable, while wishes:lint is a non-optional stage of both check and check:fast, so the skill describes a looser contract than the gate enforces; lines 64-72 present standalone genie task create / genie context as the default path with Orca demoted to an appendix at line 74, although lifecycle authority is an explicit configured mode either way and the document never resolves orchestration.mode before branching. Verify owns the gate command, orchestration.mode owns the branch, work owns the coordinator protocol restated at lines 74-77 and review owns the persistence rules at lines 54-63; the design preflight, template scaffold, dependency-ordered groups and base-SHA handoff are wish's own. | lines 54-63 (linter guard at 59, optional framing at 62), 64-77 |
| `work` | Improve | Line 49 restates the per-group repair budget as 'B (default 2)' where fix resolves budgets.maxEscalationsPerGroup (schema default 2, ceiling 5) and never names it, lines 48-50 re-specify reviewer dispatch and evidence placement that review owns, line 51 restates the validation policy verify's tables already encode, and lines 14-22 repeat the group contract (goal, deliverables, criteria, validation, owned files) that wish's template defines; separately, line 43 still offers 'legacy task rows' as a fallback surface that no longer exists now that state is genie.db plus the git-tracked roadmap.json snapshot, and line 63 asserts roadmap.json publishes on every commit although publication is the hook-installed pre-commit/post-merge/post-rewrite three-way task sync, which does not hold on a clone without hooks. Fix, review, verify and wish each cover one restatement; deriving ready waves from the document DAG and running the card conversation are work's own and should be what remains. — Dispatch and Complete a group — lines 14-22, 43-51, 63 → about 65 lines, down from 80 | lines 14-22, 43, 48-51, 63 |
| `workfly` | Improve | Line 51 embeds a dated one-off incident ('a stale user copy shadowed the project council.js on 2026-09-15') inside a standing rule, so it reads as stale the moment that host condition clears and teaches nothing durable about shadowing; lines 45-47 restate all five stages and every agent brief in prose that scripts/workfly-workflow-parity.test.ts does not pin beyond the path, the saved name and the meta.phases block, leaving the paragraph free to drift from .claude/workflows/workfly.js; line 16's claim that catalogDir 'is the only directory the contract suite covers' is an assertion about scripts/workflows-meta.test.ts held in skill prose rather than read from the suite. The parity tests and the script cover the stage roster, and skill-audit covers the merge/retire verdicts whose execution line 42 performs; discovery, design, draft, adversarial verification and bounded repair for a new saved workflow are workfly's own — this sweep is its proof — and should be all that remains. — Without a workflow surface (lines 45-47), the catalogDir coverage claim (line 16), and the dated rule at line 51 → about 46 lines, down from 55 | lines 16, 45-47, 51 |

## Cross-shard overlap
- council + review + brainstorm → survivor review: Independent, assess-only, multi-lens judgement of a proposal returned as a fixed-shape verdict with evidence and no mutation. Council dispatches four lenses and synthesizes; review holds the same seven-lens table at lines 42-50 and returns SHIP/FIX-FIRST/BLOCKED without applying a fix; brainstorm line 24 escalates consequential disagreements into that need.
- research + report → survivor report: Read-only investigation that delegates the reading, cites every finding to a URL or path:line, and hands over a document — report's finding block (lines 29-36) and research's (lines 39-45) are near-identical and differ only in whether the subject is a failure or an open question.
- fix + work + dream + review + report + quick → survivor fix: The bounded-repair escalation budget, restated as a prose constant ('default 2', 'at most two', 'three repair attempts', 'at most one bounded correction') in five documents instead of resolved once with genie config get budgets.maxEscalationsPerGroup; fix even contradicts itself between its line 14 and line 31.
- verify + work + review + quick + merge → survivor verify: The pre-claim evidence rule — which single command proves a completion claim, run fresh, exit code read, nothing mutated. Work line 51, quick line 33, merge line 48 and review's gate language each restate it, and three of them cache a stale command list.
- authoring + skill-audit + workfly → survivor skill-audit: The search-the-catalogue-before-you-write consolidation pass: authoring line 10 defers it explicitly, skill-audit's 'Before authoring' interview (lines 14-24) performs it, and workfly runs the workflow-shaped equivalent during discovery.
- wish + work + dream → survivor wish: The group contract — goal, deliverables, success criteria, validation and owned files — defined by wish's template and repeated in work's Dispatch (lines 14-22) and again inside dream's batch execution paragraphs.
- dream + work + quick → survivor work: Dispatch a scoped worker against an approved unit, gate the claim against tool output, route the review verdict through repair, and complete. Dream restates it for the batch case (lines 29-36) and quick restates a miniature of it (lines 28-40).
- docs + genie-hacks → survivor docs: The automagik-dev/docs contribution workflow — branch, commit path, PR base and the submodule pointer bump. Genie-hacks line 34 carries its own copy and has already drifted to the wrong base branch (dev instead of main).
- genie + skill-audit → survivor genie: Authority over which skill covers a need. Genie decides the route for one live incoming request; skill-audit only stocktakes the corpus and proposes verdicts, and genie line 32 hands the stocktaking over — the boundary holds but the two must not both advertise routing.
- refine + research + report → survivor report: Where a generated document may be written. Refine line 45 writes text-mode output to /tmp/prompts/ while research line 35 rejects that destination outright ('notes written there are lost before anyone reads them'), so the catalogue currently ships two contradicting placement rules that must be stated once in the investigation entrypoint.

## Unjudged
- (none — every roster skill carries a verdict)

## Judge note
Three things the characterizations could not settle. (1) The "not published / absent from the published inventory" signal appears in every shard-4 characterization and in the parity block for all twenty directories, but the signals summary attributes parity exit 1 to a single upstream cause — the skills CLI --list named no skills at all — so it is one tooling defect, not twenty per-skill defects, and I gave it no weight in any verdict. Doctor nevertheless reports 14/14 per home against twenty on-disk directories at a stale record (v5.260915.6 vs a v5.260915.8 binary), so six skills genuinely reach no installed agent home; nothing in the shards identifies which six, and the stale record makes "the six newest" (workfly, skill-audit and kin) plausible but unproven. That gap needs a working --list or a fresh install record before any skill is judged on delivery, and it is the single most consequential open item from this run. (2) Shard 2 reports that genie line 31 names the authoring skill while the delivered catalogue advertises skill-management. No shard characterized a skill-management document, so I cannot tell whether authoring and skill-management are the same document under two names — in which case this is a rename to reconcile, not a routing bug — or two documents with one unpublished. I verdicted authoring on its own text only and flagged the name mismatch under genie. (3) Review's strongest overlap claim is against the seven lens skills (architecture-ousterhout, code-quality-hejlsberg, qa-beck, perf-gregg, dx-docs-procida, repo-hygiene-chacon, supply-chain-lorenc) and specialist-panel, none of which were in any shard and none of which I verdicted; whether review's references/lenses/*.md duplicate those shipped skills or merely index them is unverified, so the review row is scoped to defects inside review's own text plus absorbing council. Also unresolved by design: five "absorbed" skills were named in the focus line but no shard characterized them, so they carry no verdict row here."

This sweep created, modified and moved no file. Confirm every retirement and merge with the caller before any file moves.

_Every agent responded this run._
