# docs-audit — first live run (2026-09-16)

Run `wf_8dcc346c-59a` of `.claude/workflows/docs-audit.js` (Opus pinned): 6 agents (1 locate, 4 surface auditors, 1 consolidator), 454,040 subagent tokens, 104 tool uses, 513 s wall clock, `ok: true`, 4/4 surfaces, quorum 3, 69 checks run. Assess-only: nothing was mutated. Findings are proposals; the write half and the contributor test stay with the docs skill front door.

notConvened: []; rows dropped or flagged by the fabrication gate: 3

---
# Documentation audit

- Focus: First live run of the converted docs audit: README, AGENTS.md plus the CLAUDE.md overlay, docs/ and architecture pages, and runtime DX, checked against the product at branch wish/workfly after the workfly and skill conversions.
- Timestamp: 2026-09-16T02:00:00Z
- Docs home: `.docs-vendor/genie` (a submodule), published at https://automagik.dev/genie; the same tree also reads as `genie/ inside the automagik-dev/docs repository`, `docs/ (symlink -> .docs-vendor/genie)`, `docs`. Fixes route through: `git -C /Users/feliperosa/workspace/repos/genie submodule update --init --recursive .docs-vendor   # REQUIRED FIRST in this checkout: .docs-vendor is empty, so docs/ is currently a dangling symlink` → `cd /Users/feliperosa/workspace/repos/genie/.docs-vendor && git checkout -b feat/<topic>` → `$EDITOR /Users/feliperosa/workspace/repos/genie/docs/<page>.mdx   # the symlink follows into .docs-vendor/genie` → `cd /Users/feliperosa/workspace/repos/genie/.docs-vendor && git add genie/<page>.mdx && git commit -m "docs(genie): ..."` → `git -C /Users/feliperosa/workspace/repos/genie/.docs-vendor push -u origin feat/<topic>` → `cd /Users/feliperosa/workspace/repos/genie/.docs-vendor && gh pr create --base main   # PR lands in automagik-dev/docs, NOT automagik-dev/genie` → `git -C /Users/feliperosa/workspace/repos/genie submodule update --remote .docs-vendor   # after the docs PR merges` → `git -C /Users/feliperosa/workspace/repos/genie add .docs-vendor && git -C /Users/feliperosa/workspace/repos/genie commit -m "chore: bump .docs-vendor to docs main"` → `bun run lint:docs-markdown && bun run lint:docs-links   # the docs-lint.yml gate; only passes with the submodule populated`
- Surfaces: 4/4 responded (quorum 3) over the roster readme, agent-instructions, docs-architecture, runtime-dx
- Verified this run: readme (24 checks), agent-instructions (12 checks), docs-architecture (12 checks), runtime-dx (21 checks) | Not checked this run: (nothing)

## Drift, ranked by reader impact
| # | Severity | Surface | Location | Documented | Actual | Fix |
|---|---|---|---|---|---|---|
| 1 | onboarding-blocker | runtime-dx | `README.md`:20-24 | ## Install\n\n```bash\ncurl -fsSL https://raw.githubusercontent.com/automagik-dev/genie/main/install.sh \| bash\n``` | wc -l README.md AGENTS.md CONTRIBUTING.md -> "wc: CONTRIBUTING.md: open: No such file or directory". The README's only install path is the signed-binary curl\|bash; no from-source path is written anywhere reader-facing, and the footer (README.md:310-315) links Docs/Releases/Discord/License with no contributing link. The only place build/test entry points appear is CLAUDE.md:8-15, an agent-instruction overlay. | Ordinary genie PR: add CONTRIBUTING.md (or a README section above the footer) with the from-source path — clone, `bun install`, `bun run check`, `bun test <file>` — and link it from the README footer. This is the first place the written path stops for a human who came to change code; the one path they can discover, `curl \| bash`, installs a release binary and teaches nothing about the checkout. |
| 2 | onboarding-blocker | docs-architecture | `docs (symlink -> .docs-vendor/genie)`:symlink target; package.json:19 names the pages | CLAUDE.md's docs workflow opens with `$EDITOR docs/installation.mdx`; package.json:19 `"lint:docs-markdown": "ls docs/incident-response/canisterworm.mdx docs/installation.mdx docs/release-notes.mdx > /dev/null && markdownlint-cli2 ..."` | git submodule status -> "-41eb2dd6f9b915e497bc8dcfb5ddfd524b52308d .docs-vendor" (leading '-' = uninitialized); ls .docs-vendor -> empty; ls docs/installation.mdx docs/release-notes.mdx docs/incident-response/canisterworm.mdx -> No such file or directory for all three (exit=1); find docs/ -> "docs/: No such file or directory". | Two halves. Documentation: the CLAUDE.md "Docs" workflow must name `git -C <repo> submodule update --init --recursive .docs-vendor` as its first step, since today the documented first command ($EDITOR docs/installation.mdx) edits a dangling symlink (ordinary genie PR). Operationally: run that init before any docs page can be read, fixed, or linted — until it runs, every published reference/architecture page is unreadable and `bun run lint:docs-markdown` / `lint:docs-links` cannot pass locally. This row also carries the docs-architecture auditor's second blocker (the empty resolved docs home, `ls -la .docs-vendor/genie` -> No such file or directory): every Diataxis kind has zero readable pages at wish/workfly, so classification of the published set is deferred, not clean. |
| 3 | onboarding-blocker | readme | `profiles/hermes/genie/README.md`:53-61, 74 | GENIE_TUI_DISABLE=1 GENIE_NO_V1_PROMPT=1 genie --no-interactive --no-tui spawn engineer --provider claude --team <purpose-or-wish-slug> --role <focused-role> --cwd <repo-or-workspace> --prompt '<brief ...>'; and "- `genie ls`, `genie log`, `genie observe`, or cost/runtime proof" | Error (genie): error: unknown command 'spawn' (exit=1); Error (genie): error: unknown command 'observe'. `genie --help` lists exactly 16 commands: board, config, context, doctor, help, idea, init, install, mcp, omni, setup, shortcuts, task, ui-bridge, uninstall, update — no spawn/ls/log/observe, and no --no-tui/--provider/--team/--role/--prompt flags anywhere. | Ordinary genie PR (this README is native to automagik-dev/genie, not the docs submodule): rewrite the 'Canonical lane for Claude Code work' block and the evidence contract onto the v5 surface (`genie context --wish <slug>`, `genie task`, `genie board`), or delete profiles/hermes/genie/README.md's v4 command guidance outright — it is the only README in the repo still describing the retired v4 spawn harness. |
| 4 | drift | agent-instructions | `CLAUDE.md`:8 | bun run check        # Full gate: typecheck + lint + dead-code + test | package.json line 28: "check": "bun run typecheck && bun run lint && bun run dead-code && bun run skills:lint && bun run wishes:lint && bun run lint:complexity-budget && bun run lint:orca-bundle && bun test" — eight steps, four undocumented here. CLAUDE.md contradicts itself at line 181: "Drift is enforced by `bun run lint:complexity-budget` in `check`, `check:fast`, pre-push, and CI." | Ordinary genie PR: rewrite the comment as `# Full gate: typecheck + lint + dead-code + skills/wishes lint + complexity budget + orca-bundle lint + test`, and add `bun run skills:lint`, `bun run wishes:lint`, `bun run lint:complexity-budget` to the Commands fence so a contributor whose gate fails on one sub-gate can re-run just that step. This is the first command a contributor runs, so it is the highest-impact drift after the blockers. |
| 5 | drift | agent-instructions | `AGENTS.md`:13 | The full gate runs type checking, Biome, dead-code analysis, skill/wish/council linting, and tests. | package.json line 28 chains typecheck, lint, dead-code, skills:lint, wishes:lint, lint:complexity-budget, lint:orca-bundle, bun test. There is no council lint: `grep -rn council package.json scripts/*.ts \| grep -i lint` returns nothing; package.json defines only "skills:lint" (line 24) and "wishes:lint" (line 25). | Ordinary genie PR on the canonical AGENTS.md: replace "skill/wish/council linting" with "skill and wish linting, the cognitive-complexity budget, and the Orca bundle lint". Same root gate as rank 4 but a separate edit in the file the CLAUDE.md overlay must not contradict — "council linting" sends a contributor hunting for a script that does not exist. |
| 6 | drift | readme | `README.md`:221-226 | BK=~/.genie/state-backups/skills-retirement-<timestamp>\ncp -a --no-preserve=mode "$BK/." "$HOME/"\n# or, equivalently:\nrsync -a --no-perms "$BK/" "$HOME/" | cp: illegal option -- - / usage: cp [-R [-H \| -L \| -P]] [-fi \| -n] [-aclpSsvXx] source_file target_file — BSD cp (macOS, the platform this checkout runs on) has no --no-preserve; only the rsync form works there. The README presents the two forms as 'equivalently' with no platform note. | Ordinary genie PR on README.md:216-228 — lead with the portable `rsync -a --no-perms` form and mark `cp -a --no-preserve=mode` as GNU coreutils only (or `gcp` on macOS). Ranked above the remaining drift because this block is a recovery procedure: a darwin operator restoring a retirement backup hits 'illegal option' on the first command they paste. |
| 7 | drift | agent-instructions | `CLAUDE.md`:81 | \| `config` \| Read the resolved global config: `config get <dotted.key>` prints one schema key's value; `--json` adds `{key, value, source}` \| — introduced at line 72 as one of "Sixteen top-level commands". | The genie on PATH (/Users/feliperosa/.genie/bin/genie, 5.260915.6) rejects it: `Error (genie): error: unknown command 'config'`, and its `genie --help` lists fifteen commands with no `config` row. It is registered in the checkout (src/term-commands/config.ts:55) at repo version 5.260915.8 — present in source, not in a shipped binary. | Ordinary genie PR: state the version floor in the CLAUDE.md table (`config` — available from 5.260915.7) or hold the row until the release ships. Load-bearing beyond the table: the line-202 gotcha tells the fix skill to resolve the repair budget with `genie config get budgets.maxEscalationsPerGroup` "rather than naming the constant", and on the installed binary that exact command errors — the documented no-drift mechanism is itself the one that breaks. |
| 8 | drift | agent-instructions | `CLAUDE.md`:202 | `genie doctor` echoes the resolved value as one read-only `budgets: maxEscalationsPerGroup=<n> (<default\|file>)` line and never writes a config file. … doctor observes it through a SECOND line, `skills: agent dirs <n>/<total> recorded homes complete @ <ref>` (warn-level …) | A full `genie doctor` run on the installed 5.260915.6 binary prints neither line: the skills block is exactly four rows (`skills: claude — 14/14 @ v5.260915.6` … `skills: windsurf — 14/14 @ v5.260915.6`), no `skills: agent dirs` row, and no `budgets:` row anywhere in the 22-check output. Both exist in the checkout (src/genie-commands/doctor.ts:474 and :939-940) — unreleased, not removed. | Ordinary genie PR: mark both doctor lines with the version that introduces them, or move the paragraph to an "unreleased" note until a binary carries them. Both are safety claims — the `skills: agent dirs` line is described as the check that would have caught the 2026-09-15 loss of 57 recorded homes, and an operator verifying it against a shipped `genie doctor` finds nothing and concludes the guard is absent. |
| 9 | drift | agent-instructions | `CLAUDE.md`:55-68 | The Architecture fence lists `src/genie.ts`, `src/lib/...`, `src/term-commands/   CLI command handlers (board, context, init, omni, shortcuts, task, ...)`, `skills/`, `.genie/` — and no other source directory. | `src/genie-commands/` exists with twelve non-test modules — doctor.ts, doctor-modes.ts, doctor-worktrees.ts, install.ts, install-promote.ts, update.ts, uninstall.ts, setup.ts, shortcuts.ts, legacy-v4.ts, local-delivery-repair.ts, auxiliary-trees.ts. The string `genie-commands` appears zero times in CLAUDE.md and zero times in AGENTS.md. src/genie.ts:26 imports the shortcuts handlers `from './genie-commands/shortcuts.js'`, not from src/term-commands/. | Ordinary genie PR: add `src/genie-commands/    Lifecycle + diagnostic command handlers (doctor, install, update, uninstall, setup, shortcuts)` to the CLAUDE.md Architecture fence and drop `shortcuts` from the `src/term-commands/` row; mirror the split in AGENTS.md line 19 ("`src/term-commands/` owns `init`, `context`, MCP, Omni, task, and board commands"). Six of the sixteen documented top-level commands live in a directory neither governing file admits exists. |
| 10 | drift | agent-instructions | `CLAUDE.md`:95-113 | ### Task subcommands — the fenced list runs `create`, `list`, `checkout`, `move`, `heartbeat`, `status`, `set-wish`, `assign`, `adopt`, `report`, `comment`, `delete`, `done`, `export`, `export --write`, `import`, `sync` and nothing else. | `genie task --help` lists four further subcommands the block never names: `block [options] <id>  Place an enforced block on a card (refuses checkout until cleared)`, `unblock <id>`, `release <id>  Release a claim, returning the card to the ready queue`, `link [options] <id>  Link an existing task to a wish (appends a wish event)`. | Ordinary genie PR: add the four rows to the CLAUDE.md task-subcommand fence. `block`/`unblock` gate checkout and `release` returns a claimed card to the ready queue, so an orchestrator reading only this block has no documented way to park or hand back a card; `link` overlaps `set-wish` and needs one disambiguating line. |
| 11 | drift | readme | `README.md`:182 | \| `genie shortcuts` \| Manage terminal keyboard shortcuts \| | `genie --help` prints "shortcuts            Manage tmux keyboard shortcuts" (exit 0), and src/genie.ts:214 registers `program.command('shortcuts').description('Manage tmux keyboard shortcuts')`. CLAUDE.md:36 already agrees with the runtime. | Ordinary genie PR on README.md:182 — restore the runtime's own word: `\| `genie shortcuts` \| Manage tmux keyboard shortcuts \|`. "terminal" reads as terminal-agnostic and sets up a reader without tmux to expect a surface that does not exist for them. Reported independently by the readme and runtime-dx auditors at the same file and line; both graded it drift, so no severity conflict to settle. |
| 12 | drift | agent-instructions | `CLAUDE.md`:150 | \| `GENIE_CONFIG_FILE` \| Override the resolved genie config path \| | No occurrence of GENIE_CONFIG_FILE anywhere in src/ or scripts/; src/lib/genie-config.ts:22 resolves the path unconditionally: `return join(genieHome(), 'config.json');` | Ordinary genie PR: delete the `GENIE_CONFIG_FILE` row from the CLAUDE.md Environment Variables table, or implement the override in src/lib/genie-config.ts. Today a reader who sets it silently gets `<GENIE_HOME>/config.json` with no warning, so the table is lying about a config-relocation knob. |
| 13 | drift | agent-instructions | `CLAUDE.md`:148 | \| `GENIE_TEAM` \| Default team when `--team` not provided \| | GENIE_TEAM appears in 0 files under src/, and no `--team` flag is defined anywhere in src/ (`grep -rn "'--team" src` returns nothing). `genie board --help` and `genie task --help` expose `--board`, `--wish`, `--json`, `--worker`, `--agent` — no `--team`. | Ordinary genie PR: remove the `GENIE_TEAM` row from the CLAUDE.md Environment Variables table. It documents a flag that no longer exists and sends an agent hunting for a `--team` option on board/task commands that never had one. |
| 14 | drift | readme | `README.md`:158 | **Small.** 16 CLI commands, 4 runtime dependencies (`@inquirer/prompts`, `commander`, `zod`, `nats`) — `nats` initializes only when the omni runner starts. | package.json "dependencies" holds seven entries: @inquirer/prompts 7.10.1, @nats-io/transport-node 3.4.0, @sigstore/bundle 5.0.0, @sigstore/protobuf-specs 0.5.0, @sigstore/verify 4.1.0, commander 12.1.0, zod 3.25.76. There is no package named `nats`; the runner imports `const nats = await import('@nats-io/transport-node');` (src/lib/omni-runner.ts:1151). | Ordinary genie PR on README.md:158 — say '7 runtime dependencies (`@inquirer/prompts`, `commander`, `zod`, `@nats-io/transport-node`, and three `@sigstore/*` verification packages)' and rename `nats` to `@nats-io/transport-node` in the lazy-init clause. CLAUDE.md's Build section restates the same four-name list and needs the same edit (cited by the readme auditor, not separately audited). |
| 15 | drift | readme | `README.md`:85-86 | So the plugin is published as a **tree-only git ref whose root *is* `plugins/genie`** — symlink-free, ~132 files, ~1.3 MB: | find plugins/genie -type f \| wc -l => 10; du -sh plugins/genie => 140K; find plugins/genie -type l \| wc -l => 0. Symlink-free as documented, but 10 files / 140 KB, not ~132 files / ~1.3 MB — plugins/genie/README.md itself records that 'the committed `skills/` mirror' and the other payloads 'have all been removed'. | Ordinary genie PR on README.md:85-86 — restate as 'symlink-free, ~10 files, ~140 KB', or drop the counts and keep only the Orca limit the sentence is arguing against (2000 files / 50 MB), since `scripts/orca-manifest-parity.test.ts` is the enforceable guard and the prose number adds nothing. CLAUDE.md's Gotchas section repeats '~132 files, ~1.3 MB' and needs the same edit. |
| 16 | drift | readme | `.claude/workflows/README.md`:41-47 | ## Entries — a five-row table: `council`, `docs-audit`, `pm-ledger-verify`, `skill-audit-sweep`, `workfly`. | ls .claude/workflows/ => council.js, docs-audit.js, pm-ledger-verify.js, research-sweep.js, skill-audit-sweep.js, workfly.js — six workflow scripts on disk; research-sweep.js (56,700 bytes, Sep 16 11:43) has no row in the Entries table. | Ordinary genie PR: add the `research-sweep` row ('question + frozen source list, one planner, a bounded reader fan-out, one synthesizer, mechanical citation gate; assess-only. `args`: `{question, sources[], notesHint?, maxReaders?, model?, timestamp?}`') in the same shape as the others, and consider extending `scripts/workflows-meta.test.ts` to assert one table row per `<name>.js` so the catalog cannot fall behind the directory again. |
| 17 | drift | docs-architecture | `src/lib/v5/TAXONOMY.md`:109-110, 351 | "The pass is not a background job: it runs only inside CLI `--json` board reads, and MCP `genie_board` and the human render serve stored lanes untouched." and "\| `TaskRow` \| — (frozen) \| laneless board `--json`, MCP tools, `task export` tasks \|" | grep -rn 'genie_board' src/ returns only the TAXONOMY.md line itself — no implementation; ls src/mcp-tools.ts src/mcp-server.ts src/bridge-watcher.ts -> No such file or directory for all three; the only `mcp` symbol in src/genie.ts is `import { registerMcpCommand } from './term-commands/mcp.js'` (src/genie.ts:37), the retirement stub that writes a stderr diagnostic and exits 1. | Ordinary genie PR (TAXONOMY.md is in the genie repo, not .docs-vendor): at 109-110 rewrite the sentence to name only surfaces that exist — the human board render — and drop the MCP clause; at 351 remove "MCP tools" from the `TaskRow` Serialized-by cell. The cell is stated as the contract ("Which one a caller maps through IS the contract"), so listing a retired consumer overstates what freezing `TaskRow` protects. Merged from the two rows the docs-architecture auditor filed at 109-110 and 351: one root claim (the MCP tool surface still exists), one edit pass. — DEFECT: the fix edits TAXONOMY.md, outside the resolved docs home (.docs-vendor/genie) |
| 18 | drift | docs-architecture | `src/lib/v5/TAXONOMY.md`:262-263 | "go through `hasOperationalState(db, { includeHireRoster: false })` (`roadmap-sync.ts:151` and `:183`), which counts boards, tasks, task_events, stage_log, and wish_groups." | grep -n 'hasOperationalState(db' src/lib/v5/roadmap-sync.ts -> "272:    if (!hasOperationalState(db, { includeHireRoster: false })) return { action: 'none' };" and "313:  if (fileChanged && (!dbChanged \|\| !hasOperationalState(db, { includeHireRoster: false }))) {" | Ordinary genie PR: the two cited line numbers are stale by ~120 lines. Replace the hard line references with the enclosing function/branch names (the `dbChanged && !fileChanged` publish branch and the `fileChanged` import branch of `syncRoadmap`) so the prose cannot drift again with every edit above them. |
| 19 | misfiling | docs-architecture | `src/lib/v5/TAXONOMY.md`:1-6, 145-434 | "# Genie v5 State Taxonomy" … "## Schema reference (`user_version = 1`)" … "### The scoped board aggregate (`board --board <ref> --json`)" | No ARCHITECTURE.md exists (ls ARCHITECTURE.md -> No such file or directory; root .md files are AGENTS.md, CHANGELOG.md, CLAUDE.md, README.md, SECURITY.md), and the 434-line architecture explanation plus the full board/task JSON schema reference live inside the source tree at src/lib/v5/TAXONOMY.md, which is not in the docs home and is published nowhere on automagik.dev/genie. | TAXONOMY.md mixes two Diataxis kinds in a source-tree file: the schema tables and the scoped-board-aggregate JSON contract are REFERENCE a consumer needs, the documents-in-git/state-in-SQLite rationale is EXPLANATION. Route the consumer-facing halves through the docs home: `git -C <repo> submodule update --init --recursive .docs-vendor`; `cd .docs-vendor && git checkout -b feat/state-taxonomy`; author docs/architecture/state-taxonomy.mdx (explanation) and docs/reference/board-json.mdx (reference), leaving the engineering-internal residue under docs/_internal/; commit in .docs-vendor, PR against automagik-dev/docs, then bump the superproject pointer. — DEFECT: the fix edits TAXONOMY.md, outside the resolved docs home (.docs-vendor/genie) |
| 20 | misfiling | agent-instructions | `AGENTS.md`:57 | Release tarballs contain the binary, the `plugins/genie` Orca payload, the `plugins/dsh-genie-board` DSH payload, `skills/`, `templates/`, and `VERSION`. | `templates/` ships an agent-instruction file, src/templates/AGENTS.md, whose frontmatter declares `model: opus`, `promptMode: append`, `color`, `effort`, `thinking`, `permissionMode` and whose body opens with `@HEARTBEAT.md`. Neither AGENTS.md nor CLAUDE.md names `templates/` in its Architecture section, and no src module outside src/templates/ references it (src/genie-commands/update.ts:3037 only mirrors the directory wholesale). | Ordinary genie PR for the two root files: add a `templates/` row to the CLAUDE.md Architecture fence saying what it scaffolds and which contract governs its frontmatter, and link it from the AGENTS.md architecture bullets. The one agent-instruction file a host actually receives is documented on no surface, and its frontmatter contract lives only in docs/_internal — unreadable here (see rank 2). If the frontmatter spec also needs a fix, that half routes through .docs-vendor. |
| 21 | misfiling | readme | `README.md`:319 | > **Channel migration (2026-07):** the `homolog` channel was retired. Configs pinned to `homolog` are migrated to **stable** automatically on next run; `genie update --homolog` no longer exists — use `--stable` or `--dev`. | Two probes. Placement: tail -3 README.md shows this blockquote as the final line of the file, below the closing centered footer (Docs · Releases · Discord · MIT License) and below the `<sub>You describe the problem. Genie does the rest.</sub>` tagline — after the README has visibly ended. Runtime: `bun src/genie.ts update --homolog` -> "Error (genie update): error: unknown option '--homolog'" (exit 1, stdout empty) — correct rejection, but no pointer to the remedy the README states. | Settled severity: misfiling, not polish — the content is correct and the runtime rejects the flag correctly, so the defect is that the note sits past the end of the README instead of on the release-notes page. Docs half: `git -C <repo> submodule update --init --recursive .docs-vendor`, branch in .docs-vendor, edit docs/release-notes.mdx, commit `docs(genie): record the 2026-07 homolog channel retirement`, PR against automagik-dev/docs, then bump the pointer; the README deletion is a separate ordinary genie PR. Optional runtime polish the runtime-dx auditor asked for and worth keeping: give `update` a retired-flag branch printing `--homolog was retired with the homolog channel; use --stable or --dev (a config still pinned to homolog migrates to stable automatically)`, exit 1 — otherwise deleting the README sentence leaves someone who typed the flag with no route forward. |
| 22 | polish | runtime-dx | `CLAUDE.md`:99 | genie task move <id> --to <lane>      # Move a card to a lane defined by its board (--to, never --lane) | $ bun src/genie.ts task move 999 --lane Done -> "Error (genie task): error: required option '--to <lane>' not specified" (exit 1) — the `--lane` the operator actually typed is never mentioned, and `genie task move --help` renders only "--to <lane>       Target lane name". | In-repo fix: the confusion is documented ("--to, never --lane"), yet the runtime answers a different question — it names the flag omitted, never the flag typed. Add an unknown-option hint on `task move` (commander's `showSuggestionAfterError` or a pre-parse check) so `--lane` returns `error: unknown option '--lane' — did you mean --to <lane>?`. Grading: what failed, yes; why, no; what to do next, only by inference. |
| 23 | polish | runtime-dx | `src/term-commands/v5-task.ts`:763 | .option('--why <reason>', 'Why the declared agent was assigned (requires --agent)') — rendered in `genie task create --help` as "--why <reason>    Why the declared agent was assigned (requires --agent)" | $ bun src/genie.ts task create --title 'probe' --why 'reason' -> "Error: Assignment requires both halves: a declared agent AND the reason it was assigned (--agent requires --why)." (exit 1) — the `--why`-alone path is told the inverse rule it did not break. | In-repo fix: src/lib/v5/task-state.ts:450 hardcodes one parenthetical for both directions. Make it reflect the missing half — `(--why requires --agent)` when only `--why` is present, `(--agent requires --why)` when only `--agent` is — and name the roster values (`claude\|codex\|pi\|hermes\|prime`) on the missing-agent path so the message also answers "what to do next". Scores 1/3 on the grading questions today. |

## Cross-surface duplicates
- readme + runtime-dx → survivor rank 11 — readme, README.md:182 (both auditors graded it drift; no severity conflict, and the fix is the single README cell): README.md:182 describes `genie shortcuts` as "Manage terminal keyboard shortcuts" while the command's own description, in `genie --help` and at src/genie.ts:214, is "Manage tmux keyboard shortcuts" — the feature is tmux-specific, and CLAUDE.md:36 already matches the runtime.
- readme + runtime-dx → survivor rank 22 — readme, README.md:319, severity settled as misfiling; the runtime-dx remedy (a retired-flag branch on `update` that prints the README's own guidance) is folded into that row's fix rather than dropped: The 2026-07 homolog-channel retirement note at README.md:319 — the readme auditor filed it as misfiling (the blockquote sits below the closing footer, after the README has visibly ended, when its Diataxis home is the release-notes page); the runtime-dx auditor filed the same sentence as polish (`genie update --homolog` exits 1 with `unknown option '--homolog'` and no pointer to the documented remedy).

## Not audited
- (none — every surface of the roster responded)

## Could not read
- `docs/installation.mdx (dangling symlink — .docs-vendor uninitialized)` (docs-architecture)
- `docs/release-notes.mdx` (docs-architecture)
- `docs/incident-response/canisterworm.mdx` (docs-architecture)
- `docs/security/key-rotation.mdx` (docs-architecture)
- `docs/_internal/** (all engineering-internal architecture deep-dives, observability internals, agent-frontmatter contracts, CLI reference dumps, spawn-flow runbooks, detector specs)` (docs-architecture)
- `the entire .docs-vendor/genie tree — every reference and architecture page the docs home lists` (docs-architecture)
- `automagik-dev/docs .mintignore (the file declaring the **/_internal/ exclusion; not present in this checkout)` (docs-architecture)
- `docs/installation.mdx (dangling symlink: docs -> .docs-vendor/genie; `git submodule status` shows `-41eb2dd6f9b915e497bc8dcfb5ddfd524b52308d .docs-vendor`, uninitialized) — any runtime-DX claim the published installation page makes is unverified here` (runtime-dx)
- `docs/release-notes.mdx — same, unreadable in this checkout` (runtime-dx)
- `docs/security/key-rotation.mdx — same` (runtime-dx)
- `docs/incident-response/canisterworm.mdx — same` (runtime-dx)
- `https://automagik.dev/genie (README.md:311 footer Docs link) — not fetched; out of the read-only local probe matrix` (runtime-dx)

## Could not probe read-only
- curl -fsSL https://raw.githubusercontent.com/automagik-dev/genie/main/install.sh | bash (README.md:23) — network fetch plus a mutating install; only verified that install.sh exists at the repo root (readme)
- genie install / genie update staging the local folder `~/.genie/plugins/genie` (README.md:92) — both commands mutate the host; the payload allowlist src/lib/install-promotion.ts:57 lists a `plugins` member, but the installed path was not confirmed read-only (readme)
- npx skills add automagik-dev/genie (README.md:33, 207) and `-g --all` — network fetch and writes agent skill homes (readme)
- genie doctor / genie doctor --fix (README.md:42, 251, 255) — not executed; the `skills: <agent> <present>/<total> @ <ref>` and `not detected` strings were verified against src/genie-commands/doctor.ts:359,388 instead (readme)
- genie omni serve / handshake (README.md:278-279) — resident NATS process and ed25519 key registration (readme)
- genie setup --orchestration-mode orca|standalone (README.md:51,65) — writes configuration; the Orca `1.4.192` floor was verified against plugins/genie/orca-runtime.ts:9 instead (readme)
- genie uninstall (README.md:252) — destructive (readme)
- git push / gh pr create steps in .github/workflows/orca-plugin-ref.yml (README.md:94-97) — CI-only, would mutate refs (readme)
- bun run check (mutating/slow full gate — composition read from package.json line 28 instead of executed) (agent-instructions)
- genie install / genie update / genie uninstall / genie init / genie setup --orchestration-mode (all mutate host or repo state; the skills-channel, retirement, collision-snapshot and convergence gotchas in CLAUDE.md lines 185-201 were read but not exercised) (agent-instructions)
- genie omni serve / handshake / test-approval --live (network plus resident process) (agent-instructions)
- docs/_internal agent-frontmatter contract referenced by src/templates/AGENTS.md — docs/ is a dangling symlink in this checkout (.docs-vendor uninitialized), so the frontmatter keys could not be checked against their spec (agent-instructions)
- bun run lint:docs-markdown (requires the populated submodule; its own `ls` guard exits 1 here) (docs-architecture)
- bun run lint:docs-links (markdown-link-check over docs/*.mdx — same dependency, and it makes network requests) (docs-architecture)
- the published Mintlify site at https://automagik.dev/genie (no network read performed) (docs-architecture)
- git submodule update --init --recursive .docs-vendor (mutates the working tree; read-only brief) (docs-architecture)
- the .github/workflows/docs-lint.yml retired-terminology gate (only meaningful against populated docs/) (docs-architecture)
- install.sh via `curl -fsSL ... | bash` (README.md:23) — network fetch and host mutation (runtime-dx)
- genie install / genie update / genie update --rollback / genie update --sync-only (README.md:32,103-115) — mutate the host and the skills homes (runtime-dx)
- genie init (README.md:42,130) — scaffolds per-repo state and rewrites .mcp.json (runtime-dx)
- genie setup --orchestration-mode orca|standalone (README.md:51,65) — writes config atomically (runtime-dx)
- genie uninstall (README.md:252) — deletes recorded skill dirs and the binary (runtime-dx)
- genie doctor (README.md:42,251) — skipped as not provably read-only in this checkout (opens/initializes .genie/genie.db and probes the Orca runtime); its documented `skills: <agent> <present>/<total> @ <ref>` line is therefore unverified (runtime-dx)
- genie omni handshake / serve / test-approval --live (README.md:278-279) — registers a keypair and opens NATS (runtime-dx)
- genie task checkout/report/comment/done/sync/import (CLAUDE.md:95-110) — all write to genie.db (runtime-dx)

## Rows the gate dropped (not in the table above)
- `readme` README.md:158 — a second row for one surface, file and line — first mention wins

## Rows the gate kept with a defect (in the table above)
- `docs-architecture` src/lib/v5/TAXONOMY.md:109-110, 351 — the fix edits TAXONOMY.md, outside the resolved docs home (.docs-vendor/genie)
- `docs-architecture` src/lib/v5/TAXONOMY.md:1-6, 145-434 — the fix edits TAXONOMY.md, outside the resolved docs home (.docs-vendor/genie)

## Judge note
Four things the findings could not settle.

1. The published documentation was never read. `.docs-vendor` is uninitialized at wish/workfly, so docs/ is a dangling symlink and every page the docs home publishes — installation.mdx, release-notes.mdx, security/key-rotation.mdx, incident-response/canisterworm.mdx and the whole docs/_internal/ tree — is unread on three surfaces (docs-architecture, agent-instructions, runtime-dx all listed them). No Diataxis classification of the published set exists in this run, and no row here asserts anything about a published page's content. Rank 2 is the precondition; re-run the docs-architecture and runtime-dx surfaces after the submodule is populated before treating the published docs as clean. Rank 2 also absorbs a second docs-architecture row filed at the same root (the empty resolved docs home, `ls -la .docs-vendor/genie` -> No such file or directory) — merged intra-surface, not dropped.

2. Two other intra-surface merges, recorded here because crossSurfaceDuplicates only carries cross-surface ones: rank 18 merges the docs-architecture rows at TAXONOMY.md:109-110 and :351 (one root claim — the retired MCP tool surface still named — one edit pass). Rank 4 (CLAUDE.md:8) and rank 5 (AGENTS.md:13) share a root (the composition of `bun run check` vs package.json:28) but are deliberately kept as two rows: they are two different files needing two different edits, and AGENTS.md is the canonical file the overlay must not contradict. Collapsing them would erase the AGENTS.md line the auditor reported.

3. Ranks 7 and 8 are version-skew, not permanent drift: `genie config` and the two `genie doctor` lines exist in the checkout (5.260915.8) and are absent from the installed binary (5.260915.6). Whether the right fix is a version floor in the prose or simply shipping the release is a call the caller owns; if 5.260915.7+ ships before these are edited, both rows resolve themselves and should be re-probed rather than patched.

4. Nothing mutating was probed on any surface. Unprobed across the four auditors: install.sh via curl|bash, `genie install`/`update`/`uninstall`/`init`/`setup --orchestration-mode`, `npx skills add`, `genie omni handshake|serve|test-approval --live`, every `genie task` write path, `bun run check` as a whole, `bun run lint:docs-markdown`/`lint:docs-links`, and the published Mintlify site. runtime-dx additionally skipped `genie doctor` as not provably read-only in this checkout, so the README's `skills: <agent> <present>/<total> @ <ref>` line is verified only against source (src/genie-commands/doctor.ts:359,388), never against a run. The skills-channel, retirement, collision-snapshot and convergence gotchas at CLAUDE.md:185-201 were read but never exercised — no row claims they are correct or incorrect.

This audit read and probed read-only: it created, modified and moved no file. The contributor test (clone to the first passing check) and the write half were not performed here and no verdict is claimed for either.

_Every agent responded this run._
