# G7 — real-host dogfood (C3), khal-labs, 2026-08-30

**Host:** khal-labs (umask 0077), previously stable v5.260830.16 with the full plugin-era surface: Codex plugin generation 1.0.0 enabled (`[plugins."genie@automagik"]` + 3 hook trust rows), 7 `~/.codex/agents/genie-*.toml` role profiles, Claude marketplace plugin (caches 5.260814.5 + 5.260816.2, `installed_plugins.json`, `known_marketplaces.json`, `settings.json` enabledPlugins), council stamp `~/.claude/workflows/council.js` + sidecar, Hermes link + config refs, pi extension link.

**Release under test:** dev v5.260830.20 (tag 7fd4290eb, contains #2868 merge 18b85341b).

## Run 1 — `(umask 022 && genie update --dev --yes)`

- `Update available: 5.260830.16 → 5.260830.20` → `✔ Genie v5.260830.20 verified`
- `skills: installed 22 skill(s) from automagik-dev/genie@v5.260830.20 into 4 agent dir(s)`
- 17 `retired <surface>: <path>` lines: codex-plugin-registration (config.toml), codex-plugin-cache 1.0.0, 7× codex-role-agent, claude-plugin-registry, 2× claude-plugin-cache, claude-marketplace-registration, claude-enabled-plugin, claude-workflow (council.js), hermes-plugin-link, pi-extension-link
- `retirement backups: ~/.genie/state-backups/integration-retirement-2026-08-30T19-51-49-454Z`

## Run 2 — idempotence

- `✔ Already up to date (v5.260830.20, channel dev)`
- `skills: installed 22 skill(s) … into 4 agent dir(s)` (re-assertion, no churn)
- `nothing to retire`

## After-state assertions

- `grep -cE 'genie@automagik' ~/.codex/config.toml` → 0; diff vs before shows **only** the genie plugin table + hook trust rows removed, everything else byte-identical
- `~/.codex/plugins/cache/automagik/` → gone (family dirs pruned); `~/.codex/agents/genie-*` → gone; `~/.claude/workflows/` → empty; `installed_plugins.json` genie entry → gone; `settings.json` enabledPlugins genie key → gone
- `~/.agents/skills` = 43 entries (21 pre-existing + 22 genie), `~/.claude/skills` = 55 (33 pre-existing + 22) — within the four recorded homes each genie skill name appears exactly once (plugin caches removed, so no `$genie:*` duplicates); see HIGH-1 for the 53 unrecorded homes where this was not verified
- `genie doctor`: `skills: claude 22/22 @ v5.260830.20`, `skills: agents 22/22`, `skills: goose 22/22`, `skills: windsurf 22/22`, **`legacy integrations: retired`** — all pass
- No unmarked user asset was touched; foreign skills in the same dirs intact

## Findings the review of this evidence produced (recorded, not hidden)

- **HIGH-1 — 57 homes written, four recorded, one misreport.** Measured (`find /home/genie -maxdepth 6 -type d -name skills` filtered to dirs whose `wish/SKILL.md` exists, stat birth-time on each): skills.sh 1.5.23 wrote all 22 genie skills into **57 agent skill homes** on this host in this run (birth 2026-08-30 19:52) — `~/.hermes/skills` (47=25+22), `~/.pi/agent/skills` (41=19+22), plus 51 more full real-directory installs (`.qwen`, `.openclaw`, `.roo`, `.kilocode`, `.trae`, `.zencoder`, …; the CLI's global registry names **77** agents, of which 57 passed its detection gate here; a 58th home, `.prime/agent/skills`, is plugin-era 2026-08-18, not this run). `skills-install.json.agentDirs` records **4**, and the operator-facing line `skills: installed 22 skill(s) … into 4 agent dir(s)` is therefore an **incorrect report**, not just an incomplete record. `genie uninstall` driven by the record would orphan 22 × 53 = **1,166 skill directories** (threatens C8), and Hermes can reach the same skill names twice (its untouched `skills.external_dirs: [~/.genie/skills]` plus its own home). **Re-scoped Wish B pre-condition: a fixed candidate table cannot track a 77-agent self-discovering CLI — genie must record what the CLI actually wrote (parse its output or enumerate the registry's `globalSkillsDir` set post-install), and the `installed … into N agent dir(s)` line must report the real N; the Hermes external_dirs duplication needs a decision.** C3's "once per agent" clause is scoped to the four recorded homes only; across all 57 homes it was not verified.
- **Doctor delta 4 → 7 warns** (`ok:true` throughout): `Codex Genie role agents` (absent=7), `agent sync: claude role agents` (7 missing), `agent sync: hermes` (link absent), `agent sync: pi` (link absent) flipped pass→warn as direct consequences of retirement; `agent sync: claude` now reports `0/21 source skills current; council.js absent`. All four checks are Wish B deletions; expected on a post-plugin host, recorded here rather than filtered.
- **Residues, deliberate keeps:** `~/.codex/agents/.genie-role-agents.json` (genie-owned registry naming the removed profiles — not a retirement surface; Wish B backlog), `[marketplaces.automagik]` in `~/.codex/config.toml` (out of `removeCodexPluginRegistration` scope; Wish B backlog), Hermes `plugins.enabled/entries/toolsets` YAML keys now dangling after the link removal (user-owned config, correctly untouched; noted for the operator).
- **Zero `kept (…)` lines** occurred because the hermes config surfaces classify `absent` (no genie markers on this host's YAML shape) — the "lists unmarked ones" clause was not exercisable here.
- **Backup caveat:** the three plugin-cache generations are backed up as file-listing manifests, not payloads (deliberate); the two Claude generations are not re-obtainable from the local-directory marketplace, so cache rollback is listing-only.

## `codex exec` probe (channel liveness)

`codex exec --sandbox read-only --skip-git-repo-check 'Use $wish to state, in one line, what the wish skill does; then stop.'` → exit 0, answer: "The wish skill converts an idea into an executable plan with clear scope, acceptance criteria, dependencies, validation, and execution groups." — served from the skills.sh-installed copy (plugin retired). Log: scratchpad `g7/codex-exec-probe.log`.

Raw logs: session scratchpad `g7/` (update-run1.log, update-run2.log, doctor-before.json, doctor-after.json, config.toml before/after).

**Verdict basis:** retirement (the surface half of C3) holds fully and idempotently; the skills half holds on the four recorded homes with HIGH-1's 57-home recording/report gap carried as a hard Wish B pre-condition; channel liveness proven by the `codex exec` probe.
