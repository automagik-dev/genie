# Genie Repo Profile

> Durable, cross-audit knowledge store for the specialist panel. Recalled anchors are HYPOTHESES — re-verify before relying on them; report drift as a finding. Single writer: `/specialist-panel`.

**Last panel:** 2026-07-09 · branch `nmstx/routing-matrix` @ `1430def4` · verdict **BLOCKED** (one proven injection; clears to FIX-FIRST once the two golden edits land).
**Knowledge refresh:** 2026-07-15 · anchors updated against origin/dev @ `23dc7a00`/`67b5c5ff` (commits 24f50043 + 67b5c5ff landed the install-transaction engine + release-chain v2 direct-to-dev).
**Local checkout note (routing-delivery-fix):** local commits 5c29b2b1/127e08e3 (flat-agent transaction core, doctor truth-telling) are content-superseded on origin/dev via the #2583/#2588–#2591 delivery chain plus 24f50043's further rework of the same files — local checkout is stale relative to dev on agent-sync.ts/uninstall.ts/doctor.ts.

## Architecture (Ousterhout)
- **Module map:** 3 downward layers — entry `src/genie.ts` → handlers `{genie-commands/*, hooks/*, term-commands/*}` → libs `{lib/*, lib/v5/*}`. Two independent stores: per-repo `genie-db.ts`→`task-state.ts`(+`mcp-tools` read-only); global `global-db.ts`→`omni-queue.ts`(+`omni-runner`); shared code ONLY `sqlite-open.ts`. `launch.ts` fans tasks→worktrees→`warp-launch.ts` (pure emitter).
- **NEW subsystem — install transaction engine (dev, 24f50043/67b5c5ff):** src/lib/install-transaction.ts (FFI renameat2 x64=316/arm64=276 + Darwin renameatx_np no-clobber rename, per-arch libc allowlist, musl loader candidates, raw-syscall fallback, unknown arch fails closed; physical inode identity), install-promotion.ts (~2,049-line staged-payload admission + ordered publication + journal recovery, shared by install+update), install-link.ts (canonical symlink guard), hidden CLI `genie __install-promote` (genie.ts, hidden:true; src/genie-commands/install-promote.ts). Single mutation authority: install.sh delegates promotion to the verified staged binary under a borrowed lifecycle lease — shell no longer swaps/rolls back/backs up. build-tarballs.yml runs `__install-promote --self-test` on all 3 platform legs. Absent from CLAUDE.md Architecture tree (doc drift).
- **DX — `genie update --rollback` semantics REVERSED on dev:** rollbackBinaryAt (update.ts:860) now ALWAYS throws 'Automatic rollback is disabled...' with signed-reinstall guidance; .previous backups are read-only legacy evidence. In-transaction rollback lives inside promoteStagedInstall instead.
- **Agent-sync recovery roots (two coexist):** ~/.genie-recovery/agent-sync-* (sibling of GENIE_HOME, survives full uninstall; agent-sync.ts:6910) and ~/.genie/state-backups (in-home replacements; agent-sync.ts:6954). CLAUDE.md's gotcha mentions only state-backups — minor doc drift.
- **DEBT — mega-module concentration:** agent-sync.ts now 7,143 lines / 89 exports consumed by 6+ command modules; uninstall.ts 3,521 lines / 59 exports; update.ts 2,990. Change-amplification hotspots — split candidates: lifecycle lease/locks, flat-agent fan-out, codex retirement, manifest state. Helper duplication: VERSION_PATTERN/versionToken ×2, cString ×2, isNodeError ×4, three dlopen blocks.
- **Invariants VERIFIED INTACT (2026-07-09):** (a) `genie-db` ⊥ `global-db`, share only `sqlite-open` (genie-db.ts:16-20 vs global-db.ts:19-22); (b) hooks fork-and-exit, sole daemon `omni serve` (dispatch-command.ts:99-118 process.exit; hooks/index.ts:44 `server.listen` is a STALE DOC COMMENT, not live); (c) no upward/cyclic imports; (d) v4/v5 port-not-import firewall (task-state.ts:492 "Ported not imported").
- **Deep/exemplary modules:** `warp-launch.ts`, `claimTask` (task-state.ts:398-438, atomic conditional UPDATE + typed conflict), `sqlite-open.ts`, `mcp-tools` read-only-degrade.
- **DEBT anchors:** (1) `wish_groups` (task-state.ts:581/650/688) + `task_dependencies` (`addDependency` :345) are DOCUMENTED-LIVE (TAXONOMY.md:93-136) but production-DEAD — zero non-test callers; no `--depends-on` flag; `blocked` unreachable; `recomputeReady` :367 a permanent no-op. Only real DAG = WISH.md prose. (2) `genie launch` (launch.ts:354-375) selects all `ready` tasks, ignores deps → contradicts work/SKILL.md:83. (3) `completeTask` (task-state.ts:441-456) rejects only `blocked`; no claimant/reviewer/lease/fencing guard. (4) workspace middleware dead-but-WIRED (interactivity.ts:45-159; `installWorkspaceCheck` still called genie.ts:145; `workspace.ts` survives only for `genieHome()`). (5) CLAUDE.md Architecture block lists REMOVED modules (transcript.ts/codex-logs.ts/claude-logs.ts).

## Code Quality & Gates (Hejlsberg)
- **Gates (re-verified all-green 2026-07-15 on dev @ 23dc7a00):** typecheck/lint/dead-code/complexity-budget/skills:lint/wishes:lint/build all exit 0; suite grew 719→1961 tests across 80 files (~60s); bundle grew 1.33MB/216 → 1.95MB/232 modules; biome now checks 191 files. Aggregate `bun run check` = typecheck+lint+dead-code+skills:lint+wishes:lint+test.
- **Known false positives (do NOT report):** knip biome/commitlint/husky devDeps (absent on this branch anyway); knip "redundant entry pattern" hint (benign).
- **Complexity policy:** biome noExcessiveCognitiveComplexity warn-level, maxAllowedComplexity=25 for src, off for scripts/plugins/tests. lint:complexity-budget (0/7, 0/42, 0/8) is now structurally blind to file accretion: agent-sync.ts 847→7,143 lines (18% of the 39,987-line non-test src), uninstall.ts 389→3,521, plus new install-promotion.ts 2,043 — budget 'intact' while single files ballooned 8-9x. Documented ledger `.genie/wishes/complexity-budget-simplification/` still MISSING.
- **Type discipline:** Still zero any/@ts-ignore in prod src on dev; the one new 'as unknown as' (agent-sync.ts:5477) is behind an exhaustive runtime journal validator. New strict-boundary pattern worth citing as exemplary: exact-keys + canonical-JSON round-trip parsers in install-promotion.ts parseJournal/parseReceipt and install-transaction.ts parsePhysicalPathIdentity. TREND: 21 SQLite row casts in task-state.ts remain the unvalidated-trust concentration.
- **NEW: install transaction engine quality anchor:** src/lib/{install-transaction,install-promotion,install-link}.ts + src/genie-commands/install-promote.ts (hidden __install-promote command in genie.ts:40): typed bun:ffi renameat2/renameatx_np no-clobber rename with musl-loader candidates and syscall fallback, injectable test seams, journaled promotion/rollback. Two DELIBERATE physical-identity systems: install-transaction inode-exact (TOCTOU) vs agent-sync content/mode (ownership) — distinction currently undocumented in code; four separate dlopen wrapper sites.

## Repo Hygiene (Chacon)
- **Census:** 5453 commits, 1206 tags, 484 remote-tracking / 314 merged into origin/main, 3.03MB/295 tracked files, 41.35MiB pack.
- **`[auto-version]` bot-filter INVALIDATED:** no longer a reliable bot-commit marker — hand-authored feature/hardening mega-commits 24f50043 (+12,538/−2,879, 63 files) and 67b5c5ff carry it as an undocumented release-suppression flag (version.yml:64 is the enforcement point; suppresses the release trigger), with generic `fix:` subjects, direct-to-dev, no feature PR (only bulk promotion #2587). The old '~16% filterable bot commits' census now needs an author check; history/metrics filters on that marker hide real work. Unreleased as of v5.260715.1. Also: install.sh:571 has a mangled duplicated comment artifact.
- **Process — dev branch protection:** Wish-finalization batches can land by direct push to dev with wish-ledger-only review (24f50043: 63 files, no feature PR, review recorded at stable-release-security-gate/WISH.md:135 with exact SHA + reviewer id) — dev does not require PRs; watch for recurrence and weigh requiring delivery-branch PRs.
- **`.genie/` ignore contract (VERIFIED sound both ways):** docs-in-git (wishes/, brainstorms/, INDEX.md, agents/, attic/, release-readiness-5x.md) + state-in-SQLite ignored (genie.db/-wal/-shm .gitignore:71-73, workers.json). ZERO db leakage.
- **Config→enforcement:** biome.json→lint+CI; commitlint.config.ts→.husky/commit-msg+commitlint.yml; knip.json→dead-code; tsconfig→typecheck; scripts/{skills,wishes,complexity}-*.ts→check; husky commit-msg/pre-commit(CI-red block)/pre-push all 755+exec. NEW: +5 release scripts wired into release-publish.yml/sign-attest.yml with colocated tests exercised via release-docs.test.ts; +lint:docs-links/lint:docs-markdown wired into docs-lint.yml (markdownlint-cli2 0.23.0, markdown-link-check 3.14.2, exact-pinned).
- **CLI surface — `--rollback` semantics changed:** repurposed from restore-previous-binary to read-only legacy-state check + signed-reinstall guidance (security narrowing per stable-release-security-gate); dx-docs lane should sweep public docs for the old semantics.
- **Documented tradeoffs (NOT defects):** daily CalVer auto-release per CI-green dev push (version.yml `5.YYMMDD.N`, cosign/SLSA signed → never rewrite history); docs/→.docs-vendor submodule; statusline.sh deliberately kept (v5-demolition WISH:199); AGENTS.md removed from tracking + auto-scaffolded by plugins/genie/scripts/first-run-check.cjs:26; .well-known channel pointers tracked.
- **Hygiene debt:** .gitguardian.yml allowlist points at 5 DELETED files; AGENTS.md+.codex/ untracked AND unignored; dead scripts/sync.js + plugins/genie/{index,genie}.ts + scripts/uninstall.js; duplicate `.worktrees/` (.gitignore:56,60); root package.json omits `"license"`.
- **KEEP (verified live):** legacy-v4.ts — importers doctor.ts:30, install.ts:13, uninstall.ts:16, update.ts:23.

## Tests (Beck)
- **Runner / census:** `bun test` (single process). Suite at dev@67b5c5ff: **1,961 pass / 0 fail / 0 skip, 6,955 assertions across 80 files, ~60-65s** (was 719/37 files/~17s on 2026-07-09). Growth driven by stable-release-security-gate + routing-delivery-fix; scripts/*.test.ts now part of bun test. update.test.ts lost 847 command-level lines in the update.ts rewrite, replaced by lib-level suites (install-promotion +821, install-transaction +450, install-promote +257, install-link +172). NOT in the suite: shell E2E `tests/e2e/v5-lifecycle.sh` (separate CI job) + 46 Hermes Python tests (uncollected).
- **Blast-radius behaviors:** NEW TOP: installer/updater one-generation promotion engine (src/lib/install-{promotion,transaction,link}.ts + src/genie-commands/install-promote.ts), owned by a 314-test/1018-assertion focused set incl. real-native-path and crash-boundary fixtures; install.sh steal-protocol digest pin ROTATED to 022718a5 with pid_is_live_or_unknown added to the protected list; install-swap.test.ts pins install.sh's .steal protocol functions byte-for-byte and drives real tar/bash promotion fixtures through the actual promoter module. Existing: fail-closed hook dispatch (LOCKED by dispatch-fail-closed-regression.test.ts); atomic claim + 15-min steal; zero-daemon substrate; branch-guard merge-law.
- **Release chain:** Release CI logic extracted into fixture-tested shell scripts (release-guard 35 tests, reconcile-release-assets 9, release-generic-provenance 4, release-native-predicate 4, release-immutability 3, reconcile-channel-manifests 6); workflow YAML wiring itself remains CI-only verifiable.
- **Toolchain:** engines bun>=1.3.10 (NO packageManager field); CI pins 1.3.11 (ci.yml:52/:95), 1.3.10 (version.yml:126).
- **Conventions / isolation:** colocated `*.test.ts`; hooks under `src/**/__tests__/`; `process.env.GENIE_HOME`→tmpdir isolates BOTH genie.db files; `mkdtempSync`+afterEach rm; real git repos in /tmp; dispatch-regression rebuilds bundle first.
- **Confirmed gaps:** (1) hook file_path→execSync injection unowned [PROVEN LIVE PoC]; (2) stale-worker completeTask-after-steal unowned, no fencing token; (3) E2E is substrate-only — no skill discovery/execution/review/worktree-convergence test anywhere; the E2E even asserts `blocked`==0 as PASS (v5-lifecycle.sh:274); (4) 46 Hermes Python tests not in CI; (5) NEW: full install.sh→real-binary promotion never executed by any test (fake promoter in fixtures; only --self-test smoke); (6) NEW: pid_is_live_or_unknown fail-closed branch unexecuted (digest-pin-only ownership); (7) NEW: update.test.ts header still claims deleted atomicBinarySwap coverage. Note: the old updater swap/journal engine (atomicBinarySwap, schema-v4 journal recovery tests) was deleted in dev@24f50043; journals are now read-only legacy fail-closed (rollbackBinaryAt tested), promotion re-owned by install-promotion suites. SUITE-QUALITY: audit-context.test.ts/freshness.test.ts happy paths are doubly-conditional.

## Performance (Gregg)
- **Product:** zero-daemon CLI; pays cold start PER invocation AND PER hook fork. **Real shipped artifact = compiled binary** (build-binary.sh), not dist/genie.js — warm within 3ms, cold differs 15×.
- **Artifact weight (trend to watch):** dist/genie.js 1.33MB (2026-07-09) → 1.84MB at PR base cf59c948 → **1.95MB at 67b5c5ff** (+47% in 6 days; routing-delivery-fix PR contributed +111KB). Binary 62MB · binary-gzip 22.4MB · tarball ≈22.5MB vs 80MB CI budget.
- **Startup floor (re-measured):** `bun dist/genie.js --version` base 39.92ms vs head 41.38ms median (interleaved A/B, 40 pairs, Apple silicon, GENIE_HOME isolated). Hook Bash dispatch medians 43.5ms → 47.4ms (20 runs). Compiled-binary numbers (49.8ms warm from 07-09) NOT re-measured — re-baseline on next binary build.
- **NEW — install lifecycle engine:** install-transaction/install-promotion/install-link/install-promote (~3,800 lines) are eagerly imported via genie.ts:15 but all heavy work is gated: dlopen lazy+memoized, lease fail-fast, digests scoped to INSTALL_PAYLOAD_MEMBERS (never the 1GB bin dir), recovery = readdir when journal empty. Update path pays ≥5 full-payload sha256 passes + per-file fsync sweep + repeated staged --version execs.
- **NEW — SessionStart sync path:** `genie update --sync-only` measured 0.44-0.49s wall (base and head identical) on the inspect-only path; inspection-only by design and lease-guarded fail-fast, no hang risk under the CC hook timeout. Full clean-convergence cost unmeasured.
- **Suite scale/time:** 1961 tests / 80 files, ~61s wall in sandbox (was 719/17s on 07-09; PR base 1795/44s). scripts/ release shell harness alone = 24.4s. update.test.ts rewrite in 24f50043 eliminated 80 environment-dependent failures (82→2, remaining 2 are sandbox git-nesting artifacts).
- **By-design, NOT flagged:** Omni approval 110s/275-poll (human phone-approval wait); 30.8s = correctness-retry ceiling under pathological contention.
- **Perf debt:** core.bare probe (9.7ms×100%), eager module import on hook path, 3× execSync git shell-forks on file hooks (+2.7ms/call), 745ms cold binary first-exec (Gatekeeper), CI blind to startup.

## Security & Supply Chain (Lorenc)
- **Boundary map:** (1) hook stdin→`genie hook dispatch` fork [builtin handlers reach execSync w/ file_path — INJECTION]; repo `.claude/settings.json:15-23` routes `"*"`, shipped plugin `hooks.json:26-36` routes SendMessage-only. (2) Omni NATS inbound→approve+`claude -p` spawn [unauthenticated NATS, OFF by default]. (3) Omni outbound HTTP bearer OMNI_API_KEY + ed25519. (4) `genie update` GitHub-Release chain — re-verified after the 984-line update.ts refactor at dev tip 67b5c5ff: mandatory cosign/attestation verification intact and non-bypassable, but the identity pin is now ANCHORED (`^...sign-attest.yml@refs/heads/main$`) instead of a prefix match — all releases ≤ v5.260715.1 are signed @refs/tags/v* and FAIL the new pin; verified-safe status holds for integrity, with an OPEN cutover/availability hazard until a new-chain release exists. update.ts VERIFIED-SAFE entry needs re-verification (delegates to install-promotion engine; only the promotion journal authorizes release mutation now); cosign.pub sentinel doc tightened (anchored escaped identity-regexp, stale `genie sec` reference removed). (5) CI [no pull_request_target; workflow_run gated]. (6) SessionStart smart-install [degraded root script can be copied over shipped by build:plugin].
- **NEW — Release chain v2:** Trust model at dev 67b5c5ff: admit jobs in sign-attest.yml/release-publish.yml bind caller to release.yml@refs/heads/main (stable, workflow_dispatch, production env w/ 2-person rule) or version.yml@refs/heads/main (dev/homolog, workflow_run); anchored cosign SAN @refs/heads/main; native SLSA predicate binds channel/source_sha/source_branch/source_ci_run_id; manifests advance monotonically via reconcile-channel-manifests.sh pushed with RELEASE_MANIFESTS_DEPLOY_KEY through the release-manifests environment. Stable publication guard-gated at all 4 entry points: release.yml `guard` job (needs:-wired), sign-attest.yml + release-publish.yml `admit` jobs, build-tarballs.yml standalone dispatch REMOVED (workflow_call+PR only). Guards extracted to fixture-tested scripts (release-guard.sh 401 lines + release-guard.test.ts, reconcile-release-assets.sh, release-generic-provenance.sh, release-native-predicate.sh, release-immutability.sh). install.sh cosign identity pin tightened to `@refs/heads/main$`. production env: stable-only, prevent_self_review, no admin bypass; v* tags immutable ruleset. AS OF 2026-07-15 NEVER EXECUTED: version.yml+release.yml disabled_manually, deploy-key secret nonexistent, org policy blocks repo deploy keys (wish qa/github-settings-evidence-20260714.json).
- **NEW — Installer mutation authority:** install.sh no longer swaps/backs up/rolls back; the cosign-verified staged binary itself runs `genie __install-promote` under a borrowed hard-link lifecycle lease and proves it IS the staged artifact (execPath physical-identity compare, install-promote.ts:160-167); all moves go through install-transaction.ts native no-clobber renames (renameat2/RENAME_NOREPLACE, renameatx_np/RENAME_EXCL, raw-syscall fallback 316/276, musl loader candidates) which never unlink. VERIFIED-SAFE additions to re-verify on change: install-transaction.ts constants/syscall table; uninstall ownership-proof removal (managed-clean only, uninstall.ts:1063+).
- **NEW — CI surface, version.yml executes no repo code:** the write-capable version workflow no longer runs bun install or package scripts from the dev checkout; six version fields rewritten by trusted jq/sed with symlink rejection and exact staged-delta allowlist, commit --no-verify. Regression here (reintroducing repo-script execution under contents:write) would reopen a code-to-write-token escalation — treat as a LOCKED CONTRACT.
- **NEW — Stable manifest pointer drift:** Observed 2026-07-15: GH latest release v5.260714.3 (non-prerelease) vs main .well-known/latest.json = 5.260711.6; the (dev,homolog,stable) manifest commit 0563c9db for v5.260714.3 is stranded on dev. Pre-existing chain defect; should disappear after cutover — re-check next audit.
- **Credential inventory:** ed25519 host key `~/.genie/keys/`; Omni bearer OMNI_API_KEY; CI GITHUB_TOKEN; cosign keyless OIDC (no long-lived key). All at-rest creds user-local.
- **Trust delegations (scope-checked OK):** approval authority = post-access to config.approvalChat on config.instance; release trust = cosign cert-identity-regexp + OIDC + SLSA source-uri; hook fail-closed w/ AskUserQuestion empty-response carve-out (HOLDS).
- **VERIFIED-SAFE list (re-verify if these files change):** fail-closed dispatch + regression gate; omni-approval fail-safe (timeout→ask, never auto-allow); CI trigger gating; ed25519 outbound signing; install-transaction.ts constants/syscall table; uninstall ownership-proof removal. (update.ts chain: pending re-verification, see boundary 4.)
- **Security debt:** F1 execSync file_path injection (audit-context.ts:20, freshness.ts:24, :72); F2 build.js:106 restores degraded installer; F6 stale .gitguardian; F7 unauthenticated Omni NATS approval. RESOLVED at dev tip (24f50043+67b5c5ff): F3 (all actions SHA-pinned incl. signing path; explicit SLSA v2.1.0 exception), F4 (SECURITY.md rewritten — 5.x posture, CanisterWorm incident, keyless-only pin, six-witness contract, verify-release.sh as sole operator verifier), F5 (permissions blocks added to commitlint/docs-lint; ggshield digest-pinned docker composite).

## DX / Docs / Delivery (Procida)
- **Docs topology:** `docs/` → `.docs-vendor/genie` submodule (automagik-dev/docs@main; UNINITIALIZED locally → dangling). Operator pages public (Mintlify); `docs/_internal/` excluded via `**/_internal/`. FIX CLASSES: CLAUDE.md/README.md/SECURITY.md = THIS repo; public pages = SUBMODULE.
- **Live interface:** Version now 5.260715.1 (dev). update flags changed: --rollback re-purposed to a read-only always-exit-1 legacy check ('Check legacy rollback state and print signed-version reinstall guidance'); new hidden --post-delivery-converge; --skip-maintenance/--no-verify probes. New hidden 15th command `__install-promote` (staging-root/expected-version/self-test) invoked by install.sh:562 as a staged-binary re-entry contract — CLAUDE.md documents the analogous GENIE_UPDATE_SYNC_ONLY contract but not this one.
- **'SECURITY.md genie sec 404s' debt:** in-repo half FIXED by 24f50043: SECURITY.md rewritten around scripts/verify-release.sh with a guard test (scripts/release-docs.test.ts:338-357) banning 'genie sec verify-install' from repo files. Remaining drift moved to the PUBLIC docs submodule: genie/security/verifying-installs.mdx (phantom command, exit-3 stage, four-channel cross-check with dead greps), security/index.mdx ('four independent channels' vs shipped three-channel/six-witness contract), release-process.mdx ('atomic binary swap with rollback', pre-draft-first pipeline).
- **NEW install-transaction subsystem absent from CLAUDE.md Architecture tree** (24f50043 + 67b5c5ff, direct-to-dev): src/lib/install-transaction.ts, install-promotion.ts (2049 lines), install-link.ts, src/genie-commands/install-promote.ts; uninstall.ts grew +1447.
- **Suite size:** origin/dev@23dc7a00: 1961 pass / 0 fail across 80 files (~65s), full `bun run check` exit 0.
- **Diátaxis:** Reference/How-to/Explanation solid; **TUTORIAL MISSING** (no guided "your first wish end-to-end") — root cause of onboarding blockers.
- **What works:** engineering onboarding <30min; `genie init` exemplary; lifecycle chain document-coherent; best-in-class hand-authored errors (launch, task-not-found); install.sh security posture strong+honest.
- **DX debt / stumble log:** README onboarding doesn't install plugin/run init/wire hooks (README:39,43-50); doctor prints "All checks passed" without init/plugin/hook checks; 3-way task-done contradiction (work/SKILL.md:82,112 vs launch.ts:313,318 vs README:102); README table "12 commands"/"~0.9MB" stale; CLAUDE.md 3 deleted modules + unwired complexity claim; docs submodule-init undocumented; setup.ts dead tmux config; commander-default errors double-prefixed. NEW stumbles: `genie update --rollback` always fails with unactionable guidance (no version-pin path exists in update or install.sh; success branch update.ts:2870 dead); bare `genie __install-promote` prints a raw Bun stack; missing-install rollback says 'not a physical directory' for ENOENT; README quickstart now carries a channel-authority explanation paragraph (Diátaxis misfiling) on top of the existing caveat wall; update.ts:306 banner comment still claims .previous rollback.

## Open cross-lane findings (2026-07-09) — see panel synthesis
BLOCKED: hook injection (3 sites). FIX-FIRST: launch-ignores-deps, review-promise-unenforced (task-done + completeTask), onboarding-doesn't-assemble, phantom-graphs+false-confidence-tests, core.bare-probe (3-lane), build:plugin-installer, CLAUDE.md+complexity drift. MED/LOW: Hermes-tests-not-in-CI, E2E-substrate-only, perf-floor, doctor-validation, .gitguardian, hygiene-bundle. (SECURITY.md-v4 and CI-hardening items resolved at dev tip 2026-07-15 — re-verify then retire.)
